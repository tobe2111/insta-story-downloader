"""달력이 시키는 매매 — 가설 우선 3호(만기 주간)·4호(FOMC 사전 표류).

2026-08-23, 사장님 "얘기해준 거 다 하자" 지시의 후보 등록분. 둘 다 신호의
재료가 **달력**이고 가격을 아예 안 본다 — 수급 가설(만기 결제·롤오버·헤지
되감기 / 발표 전 위험 보상·포지션 정리)이 참인지 오디션이 판정한다.

같은 라운드에 검토한 **지수 편입·편출 수급은 반려**했다: 가설은 충분하나
(지수 펀드는 편입일에 가격 불문 사야 한다), 과거 편입·편출 종목과 시점의
**시점별 이력이 유료 데이터**라 검증 가능한 재료가 없다. 재료 없이 링에
세우면 다중검정 문턱만 올린다 — 반려 사유를 여기 남긴다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2026-08-26 수집 라운드 — **등록 0건.** 검토한 둘 다 반려했고 사유를 남긴다.
억지로 채우지 않는 것이 방침이다(후보 하나가 곧 다중검정 문턱 비용이다).

① **월말 채권 인덱스 듀레이션 연장 — 중복으로 반려.**
   가설은 튼튼하다: 채권 지수는 매달 마지막 영업일에 재조정되고, 지수를
   따라가는 펀드·보험사는 그날 만기가 긴 채권을 가격과 무관하게 사야 한다
   (뉴욕 연준 실측: 그날 국채 순매수가 그 달의 어느 날보다 크다). 2026-08-19
   확장으로 채권 ETF 4종(TLT·IEF·LQD·TIP)을 담고 있어 대상도 있다.
   그런데 **신호가 1호(turn_of_month)의 부분집합**이다 — 1호는 달력일
   25일~다음 달 3일을 보유하는데 마지막 영업일은 항상 그 안이다. 메커니즘은
   다르지만(월급 적립금 vs 지수 재조정) 나오는 매매가 거의 같다. 1호가 먼저
   "월말 창에 무엇이라도 있다"를 말해 준 뒤에 좁힌 판을 세우는 것이 순서다.

② **국민연금 리밸런싱 수급 — 재현 불가로 반려.**
   메커니즘은 교과서적이나 (a) "오늘 파는가"를 무료 데이터로 그날 알 수 없고
   (보유 비중은 시차 공개), (b) 규칙 자체가 2026년에 바뀌었다(10영업일 최대
   50bp → 20영업일 최대 25bp, 국내주식 목표비중 14.9% → 20.8%). 과거 구간과
   지금이 다른 제도라 백테스트가 둘을 섞는다. 등록 조건 (a) 결정성 실패.
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

지켜야 할 약속:
- 만기 주간 = [셋째 금요일−4, 셋째 금요일] — 순수 달력 계산, 항상 같은 달.
- FOMC 포지션은 발표일 D의 **D−1·D−2 봉**에 선다(체결 규약상 그 포지션이
  D−1일·D일 수익을 번다 — 문헌의 발표 전 구간).
- 가격이 판단을 못 바꾼다. 미래 봉이 과거 판단을 못 바꾼다.
- FOMC 달력(2020~2026) 밖의 해는 관망 + **경고**(조용한 관망은 고장이다).
- 롱 전용. 달력 날짜는 전부 그 시점 이전에 공표된 정례 일정이다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2026-08-29 수집 라운드 — **등록 0건.** 검토한 하나를 반려하고 사유를 남긴다.

③ **연말 절세 매도(tax-loss selling)와 1월 되돌림 — 표본 부족으로 반려.**
   가설 자체는 이 방침이 찾는 모양 그대로다: 과세 투자자는 그해 실현이익을
   상계하려고 **손실 난 종목을 12월 안에** 판다. 파는 이유가 "싸다·비싸다"가
   아니라 **세법의 마감일**이므로 가격에 둔감한 매도이고, 이유가 사라지는
   1월에 되돌아온다.

   그런데 이 저장소에서는 세 가지가 막는다.

   · **표본이 1년에 한 번이다.** 종목당 관측이 3년치라도 3건이다. 지금
     결승 관문은 홀드아웃 120봉 위에 서는데, 이 규칙은 그 창에 사건이
     **0건 들어가는 해가 대부분**이다. 재는 자가 없는 규칙은 링에 세워도
     판정이 안 나고, 시도 수만 한 칸 올려 **다른 후보의 문턱을 높인다.**
   · **우리 유니버스가 대상이 아니다.** 효과는 소형·저유동 종목에서 크고,
     대형주에서는 거래량만 늘고 가격은 안 움직인다는 관측이 있다. 지금
     40종목은 대형주와 ETF다.
   · **시장마다 제도가 다르다.** 한국 개인은 상장주식 양도차익이 대체로
     비과세라 애초에 이 압력이 없고, 코인은 또 다른 제도다. 한 규칙을 전
     시장에 세우면 절반은 메커니즘이 아예 없는 상태로 참여한다.

   등록 조건 (a) 결정성은 충족하나 **검정 가능성**이 없다 — 이 저장소에서
   후보 하나는 곧 모든 후보의 문턱이므로, 판정이 안 나는 후보는 순비용이다.
   나중에 소형주를 담게 되거나 홀드아웃 창이 연 단위로 길어지면 다시 본다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2026-08-30 수집 라운드 — **등록 0건.** 검토한 하나를 반려하고 사유를 남긴다.

④ **국채 입찰 사이클(딜러 강제 인수) — 중복 + 표본으로 반려.**
   가설은 이 방침이 찾는 모양 그대로다: 프라이머리 딜러는 국채 입찰에
   **입찰 의무**가 있어, 최종 투자자 수요가 모자라면 남는 물량을 자기
   장부로 떠안는다. 사는 이유가 "싸다"가 아니라 **계약**이다. 그래서
   입찰 전에는 가격이 눌리고(양보), 딜러가 재고를 푸는 며칠에 걸쳐
   되돌아온다. 우리는 채권 ETF 4종을 담고 있어 대상도 있다.

   그런데 실측이 셋 다 막는다.

   · **월간 입찰(2·5·7년)은 기존 후보와 겹친다.** 통상 그달 마지막 주에
     몰리는데, 1호(turn_of_month)가 이미 달력일 25~다음 달 3일을 본다.
     2023-01~2026-08 달력으로 세어 보면 입찰 창 후보일 219일 중 **201일
     (92%)이 1호 창 안**이다. 같은 날을 두 번 사는 후보를 세우면 얻는 것
     없이 다중검정 문턱만 올린다.
   · **겹치지 않는 부분은 표본이 없다.** 안 겹치는 것은 분기 리펀딩
     (2·5·8·11월 둘째 주)뿐인데 **연 4회**다. 결승 홀드아웃 120봉(약 6개월)
     에 들어가는 사건이 기대값 **약 2건**이라, ③번(절세 매도)과 같은 이유로
     판정 자체가 안 난다.
   · **날짜가 결정적이지 않다.** 실제 입찰일은 재무부가 공표하며 휴일에
     밀린다. 달력으로 근사하면 규칙은 결정적이지만 **재는 대상이 근사치**가
     되고, 그러면 져도 이겨도 가설에 대해 말할 수 있는 것이 없다.

   등록 조건 (b) 중복 아님이 무너지고, 겹치지 않는 쪽은 검정 가능성이 없다.
   무엇이 바뀌면 다시 볼 것인가 — **재무부 공표 입찰일을 무료로 받는 경로**가
   생기고, 국채 노출이 지금(40종목 중 4종목)보다 넓어질 때다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2026-09-01 수집 라운드 — **등록 0건.** ⑤와 같은 계열이지만 막은 자리가 다르다:
가설이 자기 창(窓)을 못 지켰다.

⑥ **자사주 매입 블랙아웃 — 창을 옮기면 부호가 뒤집혀 반려.**
   가설: 미국 시장의 최대 단일 순매수 주체는 기업 자신이고(자사주 매입),
   기업은 밸류에이션이 아니라 **정해진 프로그램대로** 산다 — 가격에 둔감한
   수급이다. 그런데 내부자거래 규제(SEC Rule 10b-18 안전항) 때문에 실적
   발표 **약 4~5주 전부터 발표 48~72시간 후까지** 그 매입을 멈춘다. 그
   기간에는 시장에서 가장 큰 무차별 매수자가 사라지므로 수익률이 낮아야
   한다.

   두 관문은 **통과한다**:
   · **표본** — 종목당 분기 1회, 12종목 × 25~37회 = 300창 이상.
   · **중복 아님** — `pead`는 발표 **뒤** 표류를 보고 방향이 서프라이즈
     부호를 따른다. 이건 발표 **앞** 창이고 방향이 무조건 회피다.

   막은 것은 **가설이 스스로 지정한 창**이다. 문헌이 말하는 창은 발표 전
   4~5주다. 그 창에서 재 보면 아무것도 없고, 창을 넓히거나 좁히면 효과가
   나타나는데 **두 시장에서 부호가 반대**다(스냅샷 실측, 블랙아웃 창 −
   그 외, 연율):

       창              미국(6종목)              한국(5종목)
       −49~−3일   중앙 **+11.5**%p 음수 1/6   중앙 **−50.2**%p 음수 4/5
       −35~−3일   중앙 **+0.1**%p  음수 3/6   중앙 **−3.0**%p  음수 3/5  ← 가설의 창
       −21~−3일   중앙 **+17.6**%p 음수 0/6   중앙 **−39.4**%p 음수 5/5

   ① 가설이 지정한 창(−35~−3일)에서는 **양쪽 다 사실상 0**이다.
   ② 효과가 보이는 창에서는 **미국이 플러스**다 — 가설은 마이너스를
      예측한다. 부호가 반대다.
   ③ 그리고 마이너스가 나오는 쪽은 **한국**인데, 한국은 자사주 매입이
      미국만큼 지배적인 수급이 아니다. 메커니즘이 가장 약해야 할 시장에서
      예측된 부호가 나오는 것은 그 패턴이 자사주 매입이 **아니라는** 뜻이다.
   ④ 미국의 플러스는 NVDA 하나가 끌고 간다(−49~−3일 +84%p, −21~−3일
      +100%p). 중앙값을 쓴 이유다.

   ⚠️ **정직한 한계**: 이건 스냅샷 위 인샘플 훑기이고 위험 조정도 다중검정
      보정도 없다. 창이 넓어 "분기 전반 vs 후반"에 가깝다는 점도 그대로
      적는다. 그러니 이 표는 "효과가 없다"의 증명이 아니라, **"창을 조금만
      옮겨도 부호가 뒤집히는 것을 링에 세울 수는 없다"**는 판단의 근거다.
      진짜 강제 수급이라면 메커니즘이 지정한 창 근처에서 견고해야 한다.

   문헌도 같은 방향이다 — 블랙아웃 기간의 **거래량** 변화(분기 1개월 →
   2개월 매입량 +42%)는 확인되지만 **수익률** 효과는 확인되지 않는다.
   다만 이 반려의 근거는 문헌이 아니라 위 표다(문헌만으로 반려하면 그
   문헌이 틀렸을 때 우리에게 남는 것이 없다).

   무엇이 바뀌면 다시 볼 것인가 — 기업별 **실제 자사주 매입 집행량**
   (미국 10-Q의 분기 매입 주식 수)이 붙으면, "블랙아웃"이라는 달력 대리
   변수 대신 **그 종목이 실제로 얼마나 사고 있었나**를 직접 쓸 수 있다.
   그때는 가설이 달력이 아니라 수급 그 자체를 가리키므로 다시 검토한다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2026-08-31 수집 라운드 — **등록 0건.** 이번 것은 앞의 셋과 반려 사유가 다르다:
중복도 표본도 아니고, **가설이 자기 예측에서 떨어졌다.**

⑤ **레버리지 ETF의 종가 리밸런싱 — 가설 고유 예측이 데이터에서 안 보여 반려.**
   가설은 이 방침이 찾는 것 중 가장 강한 축에 든다: 2배·3배 ETF는 **매일
   종가에** 배수를 되맞추라는 계약이 있다. 그날 오른 자산은 종가 무렵 더
   사야 하고 내린 자산은 더 팔아야 한다 — 가격을 보고 하는 매매가 아니다.
   그래서 종가가 과도하게 밀리고(overshoot), 그건 정보가 아니라 압력이므로
   다음 날 되돌아온다.

   앞선 반려들을 막았던 두 관문은 **통과한다**:
   · **표본** — 사건이 매일 있다(연 1~4회짜리가 아니다).
   · **중복 아님** — 내 후보가 켜진 날 중 기존 후보도 켜져 있던 비율(실측):
     bollinger(reversion) 61.1% · connors_rsi2 47.9% · mean_reversion 46.9% ·
     rsi 38.2%. 가장 가까운 것도 10일 중 4일은 안 겹친다.
     ⚠️ 처음에 신호 전체를 대조해 "50~87% 일치"라는 숫자를 얻었는데 그건
        **틀린 셈법**이었다 — 신호가 대부분 0이라 '둘 다 관망'이 일치로
        잡힌다. 켜진 날만 세야 한다.

   막은 것은 **가설 자신의 예측**이다. 강제 매매가 *종가에* 몰린다면, 큰
   하락일 중에서도 **종가가 저가에 더 붙은 날**(더 밀린 날)이 다음 날 더
   크게 되돌아와야 한다. 그냥 "큰 하락 뒤 반등"은 일반적인 단기 되돌림이라
   이 가설의 증거가 못 된다. 실측(스냅샷, 큰 하락일 = 하루 −2% 초과):

       QQQ    +0.13%p     NVDA  **−0.64%p**
       GLD    +0.78%p     TSLA  **−0.68%p**
       BTC    +0.41%p     삼성전자  −0.49%p
       종목 평균 −0.08%p · 플러스 3/6 · t = −0.33

   가설이 참이면 전부 플러스여야 하는데 방향이 갈리고, 하필 **레버리지 ETF
   복합체가 가장 크게 붙은 NVDA·TSLA에서 반대**로 나온다.

   ⚠️ **정직한 한계**: 이건 스냅샷 위에서 인샘플로 훑어본 것이지 정식 검정이
      아니다. 그리고 일봉으로는 **종가 경매 자체를 못 본다** — 그러니 이
      결과는 "메커니즘이 없다"가 아니라 **"지금 가진 재료로는 이 가설을
      확인도 기각도 할 수 없다"**는 뜻이다. 그 상태로 링에 세우면 이기든
      지든 가설에 대해 말할 수 있는 것이 없고, 그건 문턱 비용만 내는 일이다.

   무엇이 바뀌면 다시 볼 것인가 — **분봉(특히 종가 30분) 데이터**가 붙거나,
   기초자산별 **레버리지 ETF 잔고**를 받을 수 있게 될 때다. 둘 중 하나만
   있어도 "그냥 되돌림"과 "종가 압력"을 갈라 볼 수 있다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2026-09-02 수집 라운드 — **등록 0건.** 셋을 검토해 하나는 실측으로, 둘은
재료·중복으로 반려했다.

⑦ **월중 급여일(15일) 퇴직연금 자금 유입 — 가설의 예측 부호가 데이터에서
   반대로 나와 반려.**
   가설: 미국은 급여를 **한 달에 두 번**(15일·말일) 주는 회사가 많고,
   퇴직연금(401k) 적립금은 급여일에 자동으로 떼어 **다음 영업일에 주식으로
   들어간다** — 가격을 보고 사는 돈이 아니다. 말일치는 1호(turn_of_month,
   25일~3일)가 이미 본다. **15일치는 아무도 안 본다** — 그래서 16일(또는
   그다음 영업일)의 수익률이 평일보다 높아야 한다.

   이 가설은 **위약(placebo)이 붙어 있어** 검정하기 좋다: 한국은 급여가
   대개 월 1회(25일 전후 — 1호 창 안)라 16일에 아무 압력이 없어야 하고,
   코인은 급여 적립이 없다. 미국에서만 플러스가 나오면 가설 편이다.

   두 관문은 통과한다 — 표본은 월 1회(종목당 39~41회), 창(16일)은 기존
   후보 어느 것과도 안 겹친다.

   막은 것은 **가설 자신의 부호**다. 스냅샷 실측(2026-09-02, 42종목, 약
   3.2년; 16일째 봉 수익률 − 1호 창·16일을 뺀 평일 수익률, bp):

       시장         16일째 봉         16~17일 2봉        위약(9~10일)
       미국(25)   중앙 **−6.3** 양수 8/25   −16.2 양수 6/25   +11.4 양수 21/25
       한국(12)   중앙 −5.9 양수 5/12       −28.7 양수 2/12   +21.8 양수 10/12
       코인(5)    중앙 −22.3 양수 1/5       −24.9 양수 1/5    −21.8 양수 2/5

   ① 가설이 플러스를 예측한 자리(미국 16일)가 **마이너스**이고 25종목 중
      17종목이 마이너스다. ② 압력이 없어야 할 **위약 날짜(9~10일)가 오히려
      플러스**이고 21/25가 양수다 — 즉 "16일이 특별하다"는 신호가 없고,
      있다면 반대 방향이다. ③ 위약 시장(한국·코인)과 미국이 구별되지 않는다.

   ⚠️ **정직한 한계**: 인샘플 훑기이고 위험 조정·다중검정 보정이 없다.
      이 표는 "효과가 없다"의 증명이 아니라, **가설이 지정한 자리에서
      가설이 예측한 부호가 안 나오는 것을 링에 세울 수는 없다**는 판단의
      근거다. 급여 적립 자금이 실제로 16일에 들어오는지(운용사 자금 유입
      일별 자료)를 받게 되면, 달력 대리 변수 대신 자금 그 자체로 다시 본다.

⑧ **국채 결제일(15일·말일) 지급준비금 유출 — 간접 경로 + 중복으로 반려.**
   재무부 국채 결제일에 은행 지급준비금이 빠져 단기자금이 마르고, 그것이
   위험자산을 누른다는 경로. 그러나 이 경로에서 **우리 종목을 가격 불문
   파는 주체가 없다** — 유동성이 마르면 *누군가* 판다는 것이지 "누가 왜"를
   말할 수 없다(방침의 첫 관문). 그리고 말일치는 1호 창 안이고, 15일치는
   ⑦과 같은 날짜라 ⑦의 표가 그대로 답이다.

⑨ **코인 월물 옵션 만기(마지막 금요일) 최대고통(max pain) 끌림 — 결정성
   실패로 반려.** 딜러 헤지가 만기 전 가격을 특정 행사가로 끌어당긴다는
   가설. 그런데 **어느 방향으로 끌리는가는 그날의 미결제약정 분포**(유료
   또는 거래소 API 실시간 자료)를 봐야 알 수 있어 달력만으로는 규칙이
   안 선다. 대상도 BTC·ETH 둘뿐이고 월 1회다. 동료 검토 연구도 없고 회자
   되는 사례는 무반응이 많다. 재료가 붙고 대상이 넓어지면 다시 본다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2026-09-03 수집 라운드 — **등록 0건.** 둘을 반려했고, 반려 사유의 **패턴**에서
나온 관찰 하나를 함께 남긴다(이쪽이 오늘의 진짜 소득이다).

⑩ **원자재 선물 롤 사이클(골드만 롤) — 대상이 사실상 1종목이라 반려.**
   가설은 이 방침이 찾는 모양이다: 지수를 따라가는 원자재 펀드는 만기가 오면
   **정해진 날짜에** 근월물을 팔고 차월물을 사야 한다(예: 매월 5~9영업일).
   파는 이유가 "비싸다"가 아니라 **계약**이다.

   그런데 우리 유니버스에서 실제로 선물을 굴리는 것은 `DBC` 하나다. `GLD`·
   `SLV`는 현물을 보관하고 `XLE`는 주식 바스켓이라 롤이 없다. 한 종목짜리
   규칙은 **패널에 설 수 없고**(전 종목에 같이 서는 설정만 담긴다), 종목별
   관문 하나로만 판정되면 그것이 바로 패널을 만든 이유였던 상태로 돌아간다.

⑪ **배당 재투자 수급 — 표본 + 중복으로 반려.**
   배당을 자동 재투자하는 펀드는 지급일 직후 **가격 불문 매수**한다. 그러나
   ETF 배당은 분기 1회이고(아래 표), 지급일이 대개 월말·월초라 1호
   (`turn_of_month`) 창과 겹친다.

■ 관찰 — **표본 부족은 이 저장소의 구조적 장벽이고, 패널이 그것을 못 푼다**

   최근 반려 열한 건의 사유를 묶으면: 표본 부족 3 · 중복 4 · 재료 없음 2 ·
   가설이 데이터에서 안 보임 3(겹침 포함). 이 중 **표본 부족**은 후보의
   품질 문제가 아니라 재는 자의 길이 문제다. 결승 홀드아웃 120봉(≈6개월)에
   들어가는 사건 수(실측):

       연 1회(절세 매도·연말)   기대 **0.5회**
       분기 1회(배당·리펀딩)    기대 **1.9회**
       월 1회(롤·월말)         기대 5.7회

   ⚠️ **패널 관문이 이 문제를 풀 것처럼 보이지만 안 푼다.** 패널의 관측
      단위는 **날짜**이지 종목이 아니다(`panel_gate.daily_terms`). 달력 기반
      사건은 전 종목이 **같은 날** 일어나므로, 종목을 22개로 늘려도 날짜 수는
      그대로다 — 횡단으로 넓히면 분산은 줄지만 **사건 수는 안 늘어난다.**

   그래서 연 단위 사건을 링에 세우려면 필요한 것은 종목이 아니라 **더 긴
   홀드아웃**이다. 그건 판정 시계·재현 규약과 얽힌 큰 변경이라 오늘 하지
   않는다. 다만 **"패널이 생겼으니 이제 연 1회 후보도 된다"고 착각하지
   않도록** 여기 적어 둔다 — 그 착각은 숫자가 멀쩡해 보여서 잡히지 않는다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2026-09-05 수집 라운드 — **등록 0건.** 오늘의 소득은 새 후보가 아니라
**이 기록 자체가 작동한 것**과, 재는 김에 나온 등록 후보에 대한 사실이다.

⑫ **월중 급여일(401k 적립) 재조사 — ⑦과 같은 후보라 반려.**
   검색으로 나온 것은 "401k 적립 자금이 월중(거래일 10~11번째) 수익률을
   올린다"는 문헌이었다. 조사해 보니 **2026-09-02에 ⑦로 이미 반려한 바로
   그 가설**이다. ⑦의 표(42종목·위약 시장·위약 날짜)가 지금도 답이다.

   이 파일의 존재 이유가 여기서 증명됐다 — 반려 사유를 이유까지 적어 둔
   덕분에 같은 후보를 두 번 구현하지 않았다. 다만 **나는 검색 결과를 보고
   조사에 들어갔다가 기록을 보고 멈췄다**. 기록이 없었으면 하루를 썼다.

   ⚠️ 다시 재 본 것이 헛일은 아니었다. 이번에는 **거래일 순번**으로 잘라
   봤고(⑦은 달력일 16일로 봤다), 결론이 같은 방향이다 — 미국 19종목 ·
   15,173관측 · 2023-06~2026-09 스냅샷에서 **가설이 지목한 10~11번째
   거래일**의 일평균은 +0.069% · +0.061%로 **비(非)월말 평균 +0.088%보다
   낮다.** 표본을 반으로 갈라도 10일차 +0.103 → +0.035, 11일차 +0.034 →
   +0.087로 일관된 초과가 없다.

   ⚠️ 그리고 문헌 자체가 이 방침의 첫 관문을 못 넘는다. 월중 효과를
      설명하는 기제가 "운용사가 **가격 충격을 줄이려고 월중을 골라**
      매매한다"인데, 그건 정의상 **가격에 민감한** 매매다. 이 방침이 찾는
      것은 가격을 안 보고 살 수밖에 없는 주체다.

■ 관찰 — **등록된 1호(월말 수급)의 창이 이 표본에서 계속 열세다**

   같은 표를 등록 후보로 돌려 보면(미국 19종목):

       구간                    월말 창(25일~3일)   나머지 날
       2023-06 ~ 2025-01        +0.0313%          +0.0722%
       2025-01 ~ 2026-09        +0.0299%          +0.1027%

   두 반기 모두 창 안이 창 밖보다 낮다. 이것은 1호 가설(월말에 가격 둔감한
   매수 수요가 몰린다)이 **이 표본·이 유니버스에서는 안 보인다**는 뜻이다.

   ⚠️ **그렇다고 1호를 링에서 빼지 않는다.** 성적이 나빠 보인다고 후보를
      치우는 것이 바로 이 저장소가 막으려는 선택 편향이고, 채택 여부는
      오디션이 정한다(도전자는 돈을 받지 않는다). 여기 적는 이유는 반대다 —
      **나중에 1호가 오디션에서 지면 "역시 그랬다"가 아니라 "미리 적어
      뒀다"가 되도록** 남긴다. 좋은 숫자만 남기는 편집이 이 제품의 정체성을
      무너뜨리는 것과 같은 이유로, 나쁜 숫자도 미리 적는다.

   ⚠️ 인샘플 훑기이고 위험 조정·다중검정 보정이 없다. 판정이 아니라 관찰이다.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2026-09-06 수집 라운드 — **등록 0건.** 하나를 반려했고, 재는 김에 **등록된
6호**에 대한 사실이 하나 더 나왔다.

⑬ **주식-채권 목표비중 캘린더 신호 — 거울 다리가 없어 반려.**
   문헌은 Harvey·Mazzoleni·Melone(2025, NBER w33554) "The Unintended
   Consequences of Rebalancing"이고, 가설의 모양은 이 방침이 찾는 그것이다:
   60/40 자금은 달 안에 주식이 채권을 이기면 주식 비중이 목표를 넘으므로
   월말에 **규정상** 주식을 팔고 채권을 산다. 파는 이유가 판단이 아니라
   계약이다.

   등록된 6호(`rebalance_flow`)와 **무엇이 다른가**가 이 후보의 존재 이유다.
   6호의 부호는 **그 종목 자신의** 월중 수익률이 정한다(종목마다 다르다).
   ⑬의 부호는 **주식-채권 스프레드** 하나가 정한다 — 그날 모든 주식이
   같은 부호이고 **채권은 반대 부호**여야 한다. 그 거울이 이 가설의 서명이다.

   ■ 거울 다리를 정면으로 쟀다 (SPY 스냅샷 800봉 · 2023-06~2026-08 · 월 37개).
     "주식이 이긴 달일수록 월말 **채권** 수익률이 높아야 한다"는 예측이다:

         압력창      IEF      TLT      LQD      TIP
         1일       -0.003   -0.068   -0.036   +0.105
         2일       -0.063   -0.040   -0.043   -0.016
         3일       -0.148   -0.064   -0.019   -0.133
         5일       -0.129   -0.036   -0.053   -0.080
         7일       -0.170   -0.136   -0.186   -0.127

     **20칸 중 19칸이 반대 부호**이고, 창을 넓힐수록 더 나빠진다. 채권은
     사지는 것이 아니라 **같이 팔린다**. ⑥(자사주 블랙아웃)에서 배운 대로
     창 하나로 반려하지 않으려고 다섯 창을 다 봤는데, 다섯 창 모두 같다.

   ■ 그러면 스프레드가 주식 쪽에서 만든 음수(-0.27~-0.38)는 무엇인가.
     두 다리로 갈라 보면 그 절반은 **주식 다리**(SPY 자신의 월중 수익률,
     -0.16~-0.35)다 — 그건 **6호가 이미 하는 일**이다. 나머지 절반은
     채권 다리(IEF 월중 수익률, +0.17~+0.33)인데, 그 방향은 "금리가 내리면
     주식이 좋다"는 이미 알려진 거시 경로이고 **그 재료는 이미 링에 있다**
     (`x_tnx_chg5` = 미 10년물 5일 변화, 미국주식 ML 챔피언의 기본 피처).

     즉 ⑬을 세우면 새로 사는 것은 **가설의 서명(거울)이 빠진 6호+금리**다.
     그건 다중검정 문턱만 올린다.

   ⚠️ 정직하게 — **표본이 이것을 판정하지 못한다.** 월 1회 사건이라 관측이
      37개뿐이고 위 상관 중 유의한 것은 없다. 반려의 근거는 유의성이 아니라
      **부호가 다섯 창 모두에서 가설과 반대**라는 것과 **살아남은 절반이
      중복**이라는 것이다. 표본이 길어져 거울이 나타나면 그때 다시 본다.

■ 관찰 — **등록된 6호의 되돌림 다리가 사실상 0이다**

   6호의 문서는 스스로 기각 조건을 적어 두었다: *"①만 있고 ②가 없으면
   그건 '압력'이 아니라 그냥 추세 반전이고, 이 가설은 기각된 것이다."*
   (① 월말에 그달 오른 자산이 눌린다 ② 그 눌림이 다음 달 초에 되돌아온다.)

   같은 표로 재면(24종목 · 월 37개 · 압력창 25일~월말):

       ① 압력 다리 상관 중앙값   **-0.122**  (가설: 음수 — 맞다)
       ② 되돌림 다리 상관 중앙값 **+0.012**  (가설: 양수 — 사실상 0)
       둘 다 가설 부호인 종목     11/24      (동전던지기 기대 6)

   즉 눌리기는 하는데 **되돌아오지 않는다.** 6호 자신의 문서가 정한 기각
   조건에 가깝다.

   ⚠️ **그래도 6호를 링에서 빼지 않는다** — 1호와 같은 이유다(선택 편향,
      채택은 오디션이 정한다). 여기 적는 이유도 같다: 나중에 6호가 지면
      **"미리 적어 뒀다"**가 되도록.

   ⚠️ 인샘플 훑기이고 위험 조정·다중검정 보정이 없다. 판정이 아니라 관찰이다.
"""
from __future__ import annotations

import datetime as _dt
import re
import sys
from pathlib import Path

import numpy as np
import pandas as pd
import pytest

ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(ROOT))

from quant.data.fomc import FOMC_DECISION_DAYS, FOMC_LAST_YEAR  # noqa: E402
from quant.strategies.expiry_week import ExpiryWeek, third_friday  # noqa: E402
from quant.strategies.fomc_drift import FOMCDrift               # noqa: E402


def _df(dates, closes=None):
    idx = pd.DatetimeIndex(dates)
    c = np.asarray(closes if closes is not None
                   else [100.0] * len(idx), dtype=float)
    return pd.DataFrame({"open": c, "high": c + 1, "low": c - 1, "close": c,
                         "volume": np.full(len(idx), 1e6)}, index=idx)


def _bdays(start, end):
    return pd.bdate_range(start, end)


# ── 만기 주간 ───────────────────────────────────────────────────

def test_third_friday_is_computed_correctly():
    assert third_friday(2026, 8) == 21     # 2026-08: 금 7·14·21
    assert third_friday(2024, 3) == 15     # 2024-03-01이 금요일
    assert third_friday(2020, 3) == 20     # 2020-03: 금 6·13·20


def test_expiry_week_holds_exactly_the_expiry_week():
    df = _df(_bdays("2026-08-03", "2026-08-31"))
    s = ExpiryWeek().generate_signals(df)
    held = {str(d.date()) for d in s.index[s > 0]}
    # 2026-08 만기 주 = 8/17(월)~8/21(금)
    assert held == {"2026-08-17", "2026-08-18", "2026-08-19",
                    "2026-08-20", "2026-08-21"}, held


def test_expiry_week_price_cannot_change_the_answer():
    dates = _bdays("2026-08-03", "2026-08-31")
    up = ExpiryWeek().generate_signals(
        _df(dates, closes=np.linspace(100, 200, len(dates))))
    dn = ExpiryWeek().generate_signals(
        _df(dates, closes=np.linspace(200, 100, len(dates))))
    assert list(up) == list(dn), "가격이 달력 판단을 바꿨다"


# ── FOMC 사전 표류 ──────────────────────────────────────────────

def test_fomc_position_stands_on_the_two_bars_before_the_decision():
    """2024-06-12(수) 발표 — 포지션은 6/10(월)·6/11(화), 발표일엔 없다."""
    df = _df(_bdays("2024-06-03", "2024-06-21"))
    s = FOMCDrift().generate_signals(df)
    held = {str(d.date()) for d in s.index[s > 0]}
    assert held == {"2024-06-10", "2024-06-11"}, held


def test_fomc_handles_a_thursday_decision():
    """2024-11-07(목, 대선 주간) — 포지션은 11/5(화)·11/6(수)."""
    df = _df(_bdays("2024-10-28", "2024-11-15"))
    s = FOMCDrift().generate_signals(df)
    held = {str(d.date()) for d in s.index[s > 0]}
    assert held == {"2024-11-05", "2024-11-06"}, held


def test_fomc_beyond_the_calendar_is_flat_and_loud(caplog):
    """달력 수명 밖의 해 — 관망은 하되 조용히 하지 않는다."""
    df = _df(_bdays("2027-06-01", "2027-06-30"))
    import logging
    with caplog.at_level(logging.WARNING):
        s = FOMCDrift().generate_signals(df)
    assert float(s.abs().max()) == 0.0
    assert any("달력 수명" in r.message for r in caplog.records), (
        "달력이 끝났는데 경고가 없다 — 조용한 관망은 고장이다")


def test_fomc_price_cannot_change_the_answer_and_no_future_leak():
    dates = _bdays("2024-06-03", "2024-06-21")
    up = FOMCDrift().generate_signals(
        _df(dates, closes=np.linspace(100, 200, len(dates))))
    dn = FOMCDrift().generate_signals(
        _df(dates, closes=np.linspace(200, 100, len(dates))))
    assert list(up) == list(dn), "가격이 달력 판단을 바꿨다"
    short = FOMCDrift().generate_signals(_df(dates[:8]))
    longer = FOMCDrift().generate_signals(_df(dates))
    assert list(short) == list(longer)[:8], "미래 봉이 과거 판단을 바꿨다"


def test_the_fomc_calendar_itself_is_sane():
    """오타가 곧 선견/왜곡이 되는 상수 — 형식·개수·요일을 못 박는다."""
    per_year: dict[int, int] = {}
    for d in FOMC_DECISION_DAYS:
        dt = _dt.date.fromisoformat(d)          # 형식이 틀리면 여기서 죽는다
        per_year[dt.year] = per_year.get(dt.year, 0) + 1
        assert dt.weekday() in (1, 2, 3), (
            f"{d}: FOMC 결정일이 화·수·목이 아니다 — 오타를 의심할 것")
    assert set(per_year) == set(range(2020, FOMC_LAST_YEAR + 1))
    assert all(v == 8 for v in per_year.values()), (
        f"연 8회 정례가 아니다: {per_year}")


# ── 공통 계약 ───────────────────────────────────────────────────

@pytest.mark.parametrize("cls", [ExpiryWeek, FOMCDrift])
def test_never_short_and_hypothesis_written_where_the_rule_lives(cls):
    assert cls(allow_short=True).allow_short is False
    src = (ROOT / "quant" / "strategies"
           / f"{cls.name}.py").read_text("utf-8")
    for word in ("가설", "둔감"):
        assert word in src, f"{cls.name}: 소스에 '{word}'가 없다"
    claims = re.findall(r"(CAGR|승률|연평균|\d+\s*%\s*(수익|상승))", src)
    assert not claims, f"{cls.name}: 바깥 성적을 본문에 적었다: {claims}"


def test_both_are_wired_into_the_ring():
    from quant.live.retrain import build_challengers, champion_spec
    ring = build_challengers(champion_spec("crypto", "BTC/USDT"),
                             seed="x", evolve=True)
    names = {c.get("strategy") for c in ring}
    assert {"expiry_week", "fomc_drift"} <= names, names


def test_the_index_rebalance_rejection_is_recorded():
    """반려에도 기록이 남는다 — 이 파일 머리에 사유가 있다."""
    src = Path(__file__).read_text("utf-8")
    assert "지수 편입·편출" in src and "반려" in src and "유료" in src


def test_a_rejected_candidate_is_not_quietly_re_registered():
    """이미 반려한 후보를 다시 링에 세우지 않는다.

    2026-09-05에 실제로 그럴 뻔했다 — 검색이 "401k 적립이 월중 수익률을
    올린다"는 문헌을 물어 왔고, 조사에 들어갔다가 이 파일의 ⑦ 기록을 보고
    멈췄다. 기록이 없었으면 같은 후보를 두 번 구현했을 것이다.

    그래서 못 박는다: 월중 급여일 계열은 **반려된 채로 남아 있어야** 한다.
    """
    from quant.live.retrain import FIXED_CHALLENGERS
    names = {str(c.get("strategy") or "") for c in FIXED_CHALLENGERS}
    for banned in ("mid_month", "payday", "midmonth_payroll"):
        assert banned not in names, (
            f"반려한 월중 급여일 후보({banned})가 링에 서 있다 — "
            "반려를 뒤집으려면 이 파일의 ⑦·⑫ 기록부터 고쳐야 한다")
    src = Path(__file__).read_text("utf-8")
    assert "⑫" in src and "⑦과 같은 후보라 반려" in src


def test_the_registered_turn_of_month_carries_its_bad_number_too():
    """등록된 후보의 **나쁜 숫자도** 미리 적어 둔다.

    2026-09-05 실측에서 1호(월말 수급)의 창이 두 반기 모두 나머지 날보다
    낮았다. 그걸 안 적으면 나중에 오디션에서 졌을 때 "역시 그랬다"가 되고,
    그 사후 서술은 아무것도 증명하지 못한다.

    ⚠️ 이 검사는 성적을 요구하지 않는다 — **기록이 남아 있는지**만 본다.
       성적이 나빠 보인다고 후보를 치우는 것이 바로 이 저장소가 막으려는
       선택 편향이고, 채택은 오디션이 정한다.
    """
    src = Path(__file__).read_text("utf-8")
    assert "등록된 1호(월말 수급)의 창이 이 표본에서 계속 열세다" in src
    assert "링에서 빼지 않는다" in src, (
        "나쁜 숫자를 적으면서 '그래서 뺀다'로 이어지면 선택 편향이다")
    # 그리고 실제로 링에 남아 있어야 한다(적어만 두고 조용히 뺐으면 거짓말).
    from quant.live.retrain import FIXED_CHALLENGERS
    assert any(c.get("strategy") == "turn_of_month" for c in FIXED_CHALLENGERS), (
        "열세를 기록하면서 후보를 링에서 뺐다 — 기록과 행동이 갈렸다")


def test_every_rejected_candidate_says_why_it_was_rejected():
    """반려 사유는 **이유까지** 적는다 — "안 한다"만 남으면 다음 사람이
    같은 후보를 다시 조사한다(그 시간이 곧 비용이다).

    수집 라운드는 등록 0건인 날이 정상이다. 그런 날 남길 것이 반려 기록뿐이라,
    그 기록이 부실하면 그날 한 일이 통째로 사라진다.
    """
    src = Path(__file__).read_text("utf-8")
    for who, why in (
            ("지수 편입·편출", "유료"),          # 재료 없음
            ("월말 채권 인덱스", "부분집합"),     # 기존 후보와 중복
            ("국민연금", "결정성"),              # 실시간 관측 불가
            ("절세 매도", "표본"),               # 연 1회 — 판정 자체가 안 난다
            ("국채 입찰", "92%"),                # 기존 월말 후보와 거의 겹친다
            ("레버리지 ETF", "종가 경매"),        # 가설 고유 예측을 일봉으로 못 본다
            ("자사주 매입 블랙아웃", "부호가 반대"),  # 창을 옮기면 뒤집힌다
            ("월중 급여일", "위약"),              # 예측 부호 반대 + 위약이 더 낫다
            ("국채 결제일", "간접"),              # 파는 주체를 못 말한다
            ("최대고통", "미결제약정"),          # 달력만으로 규칙이 안 선다
            ("원자재 선물 롤", "1종목"),          # 패널에 설 수 없다
            ("배당 재투자", "분기 1회"),          # 표본 + 월말 창과 중복
            ("월중 급여일(401k 적립) 재조사", "같은 후보"),  # ⑦의 재발견
            ("주식-채권 목표비중 캘린더 신호", "거울")):  # 가설의 서명이 없다
        assert who in src, f"반려 기록이 없다: {who}"
        assert why in src, f"{who} 반려에 사유가 없다({why})"


def test_the_cross_asset_calendar_signal_stays_rejected():
    """⑬은 반려된 채로 남아 있어야 한다 — 되살리려면 기록부터 고친다.

    ⑬은 6호와 **겉이 아주 비슷해서** 조용히 들어오기 쉬운 후보다(같은 달력,
    같은 월말 창). 다른 것은 부호를 정하는 변수 하나뿐인데, 바로 그 하나가
    가설의 서명이고 데이터에서 다섯 창 모두 반대로 나왔다.
    """
    from quant.live.retrain import FIXED_CHALLENGERS
    names = {str(c.get("strategy") or "") for c in FIXED_CHALLENGERS}
    for banned in ("calendar_signal", "sixty_forty", "asset_mix_flow"):
        assert banned not in names, (
            f"반려한 주식-채권 캘린더 신호({banned})가 링에 서 있다 — "
            "반려를 뒤집으려면 이 파일의 ⑬ 기록부터 고쳐야 한다")
    src = Path(__file__).read_text("utf-8")
    assert "⑬" in src and "거울 다리가 없어 반려" in src
    # 거울이 이 후보의 존재 이유였다는 것 자체가 기록에 남아야 한다 —
    # 그게 없으면 다음 사람이 "6호랑 비슷한데 왜 안 되지"에서 멈춘다.
    assert "20칸 중 19칸이 반대 부호" in src


def test_the_registered_rebalance_flow_carries_its_bad_number_too():
    """등록된 6호의 **나쁜 숫자도** 미리 적어 둔다(1호와 같은 규약).

    6호는 자기 문서에 기각 조건을 스스로 적어 둔 유일한 후보다 — 되돌림이
    없으면 기각이라고. 그 되돌림이 실측에서 사실상 0이었다.

    ⚠️ 이 검사는 성적을 요구하지 않는다 — **기록이 남아 있고, 그 기록이
       조용한 제거로 이어지지 않았는지**만 본다.
    """
    src = Path(__file__).read_text("utf-8")
    assert "등록된 6호의 되돌림 다리가 사실상 0이다" in src
    assert "그래도 6호를 링에서 빼지 않는다" in src, (
        "나쁜 숫자를 적으면서 '그래서 뺀다'로 이어지면 선택 편향이다")
    from quant.live.retrain import FIXED_CHALLENGERS
    assert any(c.get("strategy") == "rebalance_flow"
               for c in FIXED_CHALLENGERS), (
        "되돌림이 없다고 적으면서 후보를 링에서 뺐다 — 기록과 행동이 갈렸다")
