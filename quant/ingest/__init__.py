"""사용자가 가져온 자료(PDF·유튜브·트레이딩뷰)를 **검증 가능한 전략**으로.

⚠️ 이 모듈이 무엇이 아닌지부터 (2026-08-14, 사장님 요청으로 만듦).

    "각 유저마다 투자 관련한 자료를 PDF든 유튜브 링크든 트레이딩뷰든 넣으면
     그 전략이 적용되게끔도 할 수 있어?"

**"적용"이 아니라 "심사"다.** 이 시스템의 병목은 아이디어가 아니다 — 돌연변이가
매일 밤 무한정 만들어낸다. 병목은 **진짜와 착각을 가리는 것**이고, 후보를 늘리면
그 문제는 **더 나빠진다**: 1,000번 시도해서 1등을 고르면 그 1등은 원래 좋아
보이는 게 당연하다(DSR이 존재하는 이유).

그래서 여기 들어온 것은 전부 **도전자로만** 등록된다. 매일 밤 선발전·결승전을
거치고 검증 게이트를 통과해야 돈이 간다. 그리고 시도 횟수가 늘어난 만큼
다중검정 보정에 그 사실을 넘긴다(`trials_of`) — 안 넘기면 검증이 통째로 틀린다.

설계 원칙 넷
  ① **자료 → 코드가 아니라 자료 → 명세.** AI가 코드를 쓰게 하면 검토가 불가능하고
     실행 가능한 코드를 남이 주입하는 통로가 된다. 명세는 사람이 읽고 고칠 수
     있고, git에 남고, 두 사람이 같은 명세로 같은 결과를 얻는다.
  ② **못 뽑으면 못 뽑았다고 한다.** 유튜브 투자 영상 대부분에는 검증 가능한
     규칙이 없다. 억지로 짜내면 그건 자료의 전략이 아니라 **우리가 지어낸 것**이고,
     사용자는 자기 아이디어가 검증됐다고 오해한다. 이 모듈에서 가장 중요한 기능은
     "이 자료에는 규칙이 없습니다"라고 말할 수 있는 것이다.
  ③ **지어내지 않는다.** 뽑은 규칙에는 근거가 된 **원문 문장**이 항상 붙는다.
     근거를 못 대는 규칙은 만들지 않는다.
  ④ **자동 승격 없음.** 명세는 챔피언이 될 수 없다. 도전자로 서서 이겨야 한다.
"""
from quant.ingest.spec import (
    SPEC_VERSION,
    Condition,
    SpecError,
    SpecStrategy,
    StrategySpec,
    spec_from_dict,
)

__all__ = [
    "SPEC_VERSION",
    "Condition",
    "SpecError",
    "SpecStrategy",
    "StrategySpec",
    "spec_from_dict",
]
