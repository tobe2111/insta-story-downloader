# 개선 백로그 — 발견 즉시 기록, 순차 구현

> 방침(2026-08-08 사장님 지시): 작업 중 발견한 개선점은 동결하지 않고
> **계속 구현**한다. 이 파일은 발견 → 구현의 추적 장부다.
> 큰 작업은 별도 라운드로 예약하고, 작은 것은 당일 처리한다.

## 2026-08-11 전수 감사 — 배운 것

하루 동안 결함 72건을 고쳤다. 개별 결함보다 **패턴**이 중요하므로 남긴다.
새 기능을 만들 때 이 세 가지를 먼저 확인할 것.

### ① 같은 규칙을 두 곳에 적으면 반드시 어긋난다

세 번 당했다. /monitor의 손익·낙폭(서버 vs JS), 장부 총노출(`_applied` vs
실행 루프), 레짐·이벤트 게이트(필터 vs 설명문). 마지막 것은 **설명이 실제와
정반대**를 말했다 — 사이트가 "200일선 위(매매 허용)"이라 적는 동안 필터는
데이터 부족으로 막고 있었다.

→ **판단한 쪽이 결과를 남기고, 보여주는 쪽은 읽기만 한다.**

### ② 검사가 초록인 것과 장치가 동작하는 것은 다르다

안전장치 26개를 일부러 망가뜨려 보니 **13개(50%)** 가 아무 검사도 실패하지
않았다. 킬스위치·켈리 상한·CSRF·배포판 실거래 잠금·2단계 오디션 분리…
전부 "소스에 문자열이 있는가"만 보고 있었다. 문자열 검사는 배선이 사라지는
것은 잡지만 **배선이 무력화되는 것은 못 잡는다.**

→ 새 안전장치를 만들면 `scripts/mutation_check.py`에 변이 항목을 함께
  추가할 것. 추가하지 않은 장치는 다음 리팩터링에서 조용히 사라진다.

**그리고 나는 이 글을 쓴 당일에 같은 함정에 빠졌다(결함 73).** 위 ②를
막으려고 만든 '릴리스 공개 게이트'가, 세 OS 빌드가 **전부 성공한** v0.17.1을
"자산 누락"으로 보류하고 경보를 쐈다. 원인은 draft 릴리스를
`/releases/tags/<tag>`로 조회한 것 — draft에는 아직 git 태그가 없어 그 조회는
항상 404다. 즉 **한 번도 통과할 수 없는 게이트**였는데, 검사는 초록이었다:
워크플로 YAML 안에 `REQUIRED=` 와 `빠진 자산` 문자열이 있는지만 봤기 때문이다.

핵심은 "검사를 더 썼어야 했다"가 아니다. **로직을 워크플로 YAML 안에
써 놓으면 검사가 문자열밖에 볼 수 없다.** 그래서 `scripts/publish_release.py`로
꺼내고, 가짜 GitHub API(draft를 태그로 못 찾게 진짜처럼 동작)를 물려 돌린다.

→ **워크플로 안의 로직은 검사할 수 없다고 간주할 것.** 판단이 들어가면
  파일로 꺼내고, 외부 API를 흉내 낸 가짜에 물려 실제로 돌려 볼 것.
→ 안전장치는 **막는 것**만이 아니라 **통과시켜야 할 때 통과하는가**도
  검사할 것. 이 게이트는 '막는' 검사만 있었고 그쪽은 잘 돌았다.

### ③ 코드를 읽어서는 안 보이는 것이 있다

`quant journal`이 개발용 파일을 기본으로 삼아 매일 매매가 도는데도 "거래
없음"만 말했다. 경보 문구는 "30건 이상 시 검토"인데 코드는 10건에서 이미
갈아탔다. 8만원 계좌의 주식 체결 경로는 **한 번도 실행된 적이 없었다.**
전부 함수 단위 테스트는 초록이었다 — 함수는 옳았고 연결된 전체가 엉뚱한
곳을 가리켰다.

→ 명령을 진짜 장부에 대고 돌려 볼 것(`tests/test_cli_smoke.py`).
  커버리지로 '한 번도 안 도는 코드'를 찾을 것.

**같은 날 오후, 커버리지를 다시 떴더니 네 건이 더 나왔다(73~76).**
넷 다 **한 번도 실행된 적 없는 코드**에서 나왔고, 공통점이 하나 있다 —
전부 **개발자가 지나갈 일이 없는 길**이었다.

| 어디 | 왜 아무도 안 갔나 | 무엇이 있었나 |
|---|---|---|
| 릴리스 공개 게이트 | 릴리스는 어쩌다 한 번 | draft를 태그로 조회 → **영원히 통과 불가** |
| `quant setup` | 백테스트·페이퍼는 키가 없어도 돈다 | 구매자가 **처음 치는 명령** |
| `prompt_license` | 개발 중엔 자물쇠를 안 굽는다 | **판매용 키를 0o644로 저장** |
| 웹 `do_GET` 라우트 | 가드 검사가 403에서 멈춘다 | 라우트가 엉뚱한 핸들러를 불러도 초록 |

→ **"내가 개발하면서 매일 지나가는 길"과 "사용자가 지나가는 길"은 다르다.**
  커버리지 0인 함수를 볼 때 "테스트가 없네"가 아니라 **"이 길은 누가
  지나가나"** 를 먼저 물을 것. 답이 '사용자'면 거기가 제일 위험하다.

### ④ 안전장치는 '막는가'뿐 아니라 '통과시키는가'도 검사할 것

결함 73이 정확히 그랬다. 반쪽 릴리스를 **막는** 검사는 여럿이었고 전부
잘 돌았다. 그런데 **정상 릴리스를 통과시키는가**는 아무도 안 봤고, 실제로
한 번도 통과할 수 없는 게이트였다.

같은 함정이 `quant setup`에도 있었다. "권한 약속과 현실이 일치하는가"는
정상 경로만 지나가므로, 배선을 끊어 **무조건 약속하게** 만들어도 초록이다.
약속을 **지킬 수 없는 상황을 일부러 만들어야** 검사가 성립한다.

→ 게이트를 만들면 **양쪽 방향을 다 검사할 것.** 거부 사례만 있는 검사는
  절반이다.

### ⑤ 워크플로 YAML 안의 로직은 검사할 수 없다고 간주할 것

결함 73의 진짜 원인은 "검사를 덜 썼다"가 아니다. 로직이 YAML 안에 있어서
검사가 **문자열밖에 볼 수 없었다.** `REQUIRED=` 가 있는지, `빠진 자산`
문구가 있는지 — 전부 초록이었고 게이트는 죽어 있었다.

→ 판단이 들어가면 파일로 꺼내고(`scripts/publish_release.py`), 외부 API를
  흉내 낸 가짜에 물려 실제로 돌려 볼 것.

### ⑥ 비밀을 파일로 쓰는 곳은 한 군데를 거치게 할 것

`.env`의 API 키는 ㊾에서 0o600으로 조였는데, **정작 팔아서 돈을 받은
`license.key`는 평범한 `write_text`로 0o644**였다(76). 규칙을 한 곳에서만
고치고 다른 곳을 잊은 것 — ①의 변형이다.

→ `quant.utils.envfile.write_private()` 하나만 쓸 것. 반환값이 '실제로
  본인만 읽기인가'이므로, **확인한 사실만** 사용자에게 말할 것.

### ⑦ 실패보다 **멈춤**이 위험하다 — 멈춤은 경보를 울리지 않는다

`ea3695b5`의 CI가 45분을 매달린 뒤 죽었다. 원인은 끝내 못 봤다 — **로그가
남지 않았기 때문이다.** 그걸 쫓다가 더 큰 것을 봤다: **13개 잡 중 11개에
시간 제한이 없었고**, 거기에 8만원을 굴리는 `daily-paper`와 실거래
`kr-live`가 들어 있었다.

배치가 실패하면 경보가 뜬다. **매달리면 아무 일도 안 일어난다.** 그날 매매가
조용히 사라지고, 사장님은 아무 문제 없다고 믿는다. deadman이 결국 잡겠지만
그건 몇 시간 뒤다.

→ **오래 도는 것에는 전부 상한을 걸 것**(`timeout-minutes`). 값은 추측하지
  말고 **최근 실행 시간을 재서** 최대의 3배 이상으로 잡을 것.
→ 상한의 진짜 값어치는 '빨리 죽이는 것'이 아니라 **로그를 남기는 것**이다.
  진단할 수 없는 실패는 다음에 또 온다.
→ 계약 검사로 못 박을 것(`tests/test_workflow_timeouts.py`). 규칙이 사람
  기억에만 있으면 다음 잡에서 또 빠진다 — UTF-8 규칙이 그렇게 빠졌고,
  **그 교훈을 적은 당일에 내가 새 publish 잡에서 또 빠뜨렸다.**

### ⑧ 안전장치는 **경로가 아니라 문**이어야 한다

오후에 나온 결함 82·83이 같은 뿌리였다. `trading_paused`(사장님이 대시보드
에서 누르는 '일시정지')를 읽는 곳이 실행 경로마다 따로 적혀 있었다:

    daily.py ✅ · daily_live.py ✅ · webhook.py ❌ · engine.py ❌
    multi.py ❌ · autolearn.py ❌

**`quant live --real`이 일시정지를 무시하고 계속 주문했다.** 스위치를 누른
사람은 멈췄다고 믿는다.

같은 계열이 하루에 네 번 나왔다 — 킬스위치가 시세 없는 종목을 안 비우고(80),
서킷브레이커가 킬스위치와 다른 코드를 쓰고(80), 일시정지가 웹훅을 안 덮고(82),
연속 루프를 안 덮고(83).

→ **안전장치를 만들 때 "무엇을 막는가"가 아니라 "무엇이 이 문을 지나는가"를
  먼저 정할 것.** 판정은 함수 하나에만 두고(`owner_gate()`, `_flatten_all()`),
  그 문을 지나야 하는 경로를 **소스에서 열거해 검사**할 것
  (`tests/test_owner_gate_covers_all_paths.py`). 면제는 **이유와 함께만**.
→ 문의 **순서**도 검사할 것. 일시정지는 "신규 매매 중단, 보유 유지"이지
  "위험 통제 해제"가 아니다 — 킬스위치 청산보다 앞에 두면 멈춰 둔 사이
  손실이 커져도 못 비운다.

### ⑨ 결함을 고쳤으면 **그 결함의 형제**를 기계적으로 찾을 것

82를 고친 뒤 `broker.target_weight` 호출부와 설정을 읽는 곳을 grep으로
교차한 것이 곧 83이 됐다. 두 줄이면 끝났다. 80에서도 킬스위치만 고치고
서킷브레이커를 놓칠 뻔했다.

→ 결함을 고친 **직후**, "이 실수가 가능한 다른 자리"를 목록으로 만들 것.
  코드를 읽어 판단하지 말고 grep으로 뽑을 것 — 기억은 반드시 빠뜨린다.

### ⑩ 생성물이 곧 기록인 곳은 **다시 생성하면 안 된다**

`quant social-content`를 진짜로 돌려 캡션을 장부와 대조했다. 숫자는 전부
맞았다 — 그런데 그 명령이 **08-10 폴더를 조용히 다른 글로 바꿔 놨다.**

    그날 실제로 나간 글  "오늘 -0.06%"   ← 누적을 '오늘'이라 부르던 결함
    재생성한 글          "오늘 -0.05%"   ← 그 결함을 고친 뒤의 올바른 값

캡션 코드를 고친 날 명령을 한 번 돌린 것만으로 **과거 게시물이 하지 않은
말을 했다고** 기록됐다. 정직성이 유일한 자산인 채널에서 그건 숫자 하나가
틀린 것보다 나쁘다.

핵심은 이 폴더가 **보고서가 아니라 기록**이라는 점이다. 보고서는 다시 만들면
되지만, 기록은 '무엇을 내보냈는가'이지 '무엇을 내보냈어야 했는가'가 아니다.

→ 파일을 쓰는 명령을 만들 때 **"이건 다시 만들어도 되는 산출물인가,
  한 번뿐인 기록인가"** 를 먼저 정할 것. 기록이면 덮어쓰기를 거부하고,
  바꾸려면 의도적인 플래그를 요구할 것.
→ 자동 배치가 그 플래그를 달고 다니면 방어가 무의미해진다 — 그것도 검사할 것.
→ 그리고 **고칠 것이 생기면 과거가 아니라 `docs/trust.html`을 고칠 것.**

### ⑪ 자동화의 **입구와 출구**를 같이 볼 것

`kr-live.yml`은 이렇게 생겼다 — 배치만 보면 흠잡을 데가 없다.

    - name: 준비 진단 (키·토큰·잔고 — 주문 없음)     ← 관문
    - name: 실거래 집행 (기본 모의투자)               ← 실제 주문

그런데 둘 다 뚫려 있었다.

  · **입구(87)** — 진단이 "❌ 미비 항목이 있습니다"를 찍고 **종료코드 0**을
    돌려줬다. GitHub Actions는 단계가 실패해야 잡을 멈추므로, 관문 자리에
    놓였을 뿐 아무도 막지 않았다.
  · **출구(88)** — 집행이 `python ... | tee live.txt` 인데 `shell:` 선언이
    없어 기본 `bash -e`(pipefail 없음)로 돌았다. 파이프라인 종료코드는
    `tee` 것이라 **집행이 실패해도 단계가 초록**이었다.

즉 **들어갈 때 안 막고, 나올 때 안 알렸다.** 둘 중 하나만 있었어도
나머지 하나가 잡았을 텐데 나란히 비어 있었다.

→ 워크플로를 볼 때 단계 **이름과 순서**만 보지 말 것. 각 단계에 대해
  **"이게 실패하면 무엇이 멈추는가"** 를 물을 것. 답이 '아무것도'면
  그 단계는 로그일 뿐 관문이 아니다.
→ 셸 파이프(`| tee`)는 기본적으로 실패를 삼킨다. `shell: bash` 를 명시할 것
  (그러면 GitHub이 `-e -o pipefail` 로 돌린다).
→ 진단 명령은 **종료코드로도 판정을 말할 것.** 사람이 읽는 화면과 기계가
  읽는 종료코드가 다른 말을 하면, 자동화는 늘 기계 쪽을 믿는다.

### ⑫ **닮은 숫자 두 개**가 나란히 있으면 반드시 하나는 잘못 쓰인다

포트폴리오 장부에 이런 두 값이 있다.

    "alloc":  {"us_stock:QQQ": 0.0802, ...}   ← 배분 **예산**(슬라이스)
    "weight": 0.0684                          ← 실제 적용 **총노출**

둘 다 0~1의 소수고, 둘 다 '비중'처럼 읽힌다. 그래서 세 곳이 틀렸다.

  · **91** — SNS 카드가 `alloc`을 `매수 8%`로 찍었다. `alloc`은 예산이라
    **모델이 관망한 종목에도 붙는다.** 8월 10일 QQQ 장부는 `비중 0.0 ·
    "관망 (현금)"`인데 공개 카드는 "나스닥100 ETF 매수 8.0%"였다 —
    한 주도 안 산 종목을 샀다고 방송했다.
  · **91-b** — 같은 카드의 캡션은 `총노출 7%`라 말한다. 한 종목(15%)이
    전체(7%)보다 크다는, **그 자체로 성립하지 않는 조합**이 한 게시물
    안에 같이 나갔다. 두 숫자를 나란히 놓기만 했어도 보였다.
  · **92** — 체결 기록이 슬라이스를 빼먹었다. **한 줄 위**에서 주문은
    `pend["weight"] * sl`로 나가는데 기록은 `pend["weight"]`였다.
    결과: 체결 하나가 그날 총노출의 2.4배로 장부에 남았다.

  · **93** — 다종목 트레이더가 **목표**의 합을 노출로 적었다. 시세를 못 받아
    건너뛴 종목, 브로커가 예외로 거부한 종목까지 들고 있다고 기록했다.
    하필 데이터·브로커가 흔들리는 날에 과대 보고한다.

네 번 다 원인이 같다 — **이름이 비슷해서, 또는 '하려던 것'과 '한 것'을
구별하지 않아서 정의를 확인하지 않았다.**

→ 장부에 비슷한 이름의 숫자를 둘 이상 남길 때는, 필드 옆에 **정의와
  "합이 무엇과 같은가"** 를 주석으로 박을 것. `applied`의 합은 `weight`다.
  이 한 줄이 있으면 91도 92도 안 생긴다.
→ 공개 화면에서 **다른 정의의 숫자를 나란히 놓고 산술이 성립하는지**
  볼 것. "한 종목이 전체보다 크다"는 계산기 없이도 보인다.
→ 값을 쓰는 쪽이 아니라 **만드는 쪽에서 한 번만 계산할 것.** 총노출과
  종목별 노출이 같은 함수(`_applied`)에서 나오면 어긋날 수가 없다.
→ 스케일러(슬라이스·킬스위치·변동성 타깃)를 곱하는 코드는 **곱한 뒤의
  값을 변수로 잡아** 주문과 기록이 같은 변수를 쓰게 할 것.
→ 루프 안에 `continue`가 있으면 **루프 밖에서 원본 컬렉션을 집계하지 말 것.**
  건너뛴 것이 조용히 합계에 들어간다. 통과한 것만 담는 변수를 따로 둘 것.

### ⑬ **가장 멀리 가는 화면이 가장 엄격해야 한다**

같은 데이터, 같은 집계인데 두 화면이 다르게 말하고 있었다.

    paper.html : 55~60% 구간 · 8일 · 실제 12% · **95% 구간 2~47%**
                 "구간이 좁아질 때까지는 비율보다 구간을 믿으세요"
    SNS 카드    : "55~60%라 말한 날 8일 · **실제 상승 12%**"   ← 구간 없음

사이트는 신중했고 카드는 아니었다. **순서가 거꾸로다.** 사이트는 찾아온
사람이 표와 주석을 같이 읽지만, 카드는 스쳐 지나가는 사람이 숫자 하나만
가져간다. 방어 장치는 맥락이 적은 쪽에 더 두껍게 있어야 한다.

나빠 보이는 숫자만 문제가 아니다. "70% 이상 → 실제 64%"(구간 39~84%)는
증거처럼 읽히고, 그게 이 채널에서 더 위험하다.

→ 같은 사실을 두 화면에서 말한다면, **경고·단서·불확실성이 어느 쪽에
  붙어 있는지** 확인할 것. 짧은 쪽에 없으면 그쪽이 결함이다.
→ 비율을 보이는 곳에는 **표본 수와 구간을 붙일 것.** 이 저장소는 엣지
  판정에서 이미 윌슨 하한을 쓴다 — 같은 잣대를 방송에도 쓸 것.
→ 계산해 놓고 **그리지 않는 것**도 같은 결함이다. 변이 시험이 정확히
  그 구멍(구간을 계산만 하고 렌더링에서 빼면 검사가 통과)을 잡아냈다.

### ⑭ 고친 결함은 **형제를 찾기 전까지 고친 게 아니다**

8월 10일에 "캡션이 누적을 '오늘'이라 불렀다"를 고치고 trust.html에
공개까지 했다. 그런데 **같은 데이터를 쓰는 SNS 카드는 그대로였다.**
다음 날 감사에서야 발견했다 — 그사이 카드는 계속 누적을 "오늘"이라
큰 글씨로 찍고, 첫 장 문구까지 그 값으로 골랐다.

정정문을 쓰는 것이 오히려 위험을 만든다. 읽는 사람은 "고쳤구나"로 읽고,
쓴 사람도 그렇게 믿는다. **고쳤다고 적는 순간 그 계열은 수색이 끝난
것으로 취급된다.**

→ 결함을 고칠 때는 **고치기 전에** 같은 값을 읽는 곳을 전부 grep할 것.
  (`return_pct`를 읽는 곳: 캡션·카드·index·paper·today·weekly)
→ 정정문에는 **어디까지 고쳤는지**를 적을 것. "캡션을 고쳤다"와
  "이 값을 쓰는 모든 화면을 고쳤다"는 다른 문장이다.
→ 오늘 잡은 결함의 절반 이상이 형제였다(91→92→93, 95). 이 방법이
  새 결함을 찾는 것보다 싸고 확실하다.

### ⑮ **사람이 손댄 흔적**은 결과와 같은 화면에 있어야 한다

장부에는 이렇게 적혀 있었다.

    # 어드민 개입의 흔적 — 일시정지·노출 배수는 숨기지 않고 기록한다
    "paused": paused, "exposure_scale": ...

기록은 정직했다. 그런데 **사이트·카드·캡션 어디에도 표시되지 않았다.**
이 시스템에서 사람이 결과를 바꿀 수 있는 통로가 정확히 그 둘인데,
읽는 사람은 알 방법이 없었다. '전부 자동'이라는 주장이 조용히 거짓이 된다.

같이 나온 것: 스레드 캡션이 500자를 넘으면 짧은 판으로 바뀌는데,
킬스위치 고지가 '배분 상위' 줄에 붙어 있어 **하이라이트와 함께 잘렸다.**
주석은 "링크·고지는 지키고 하이라이트를 줄인다"였는데 코드는 반대였다.

→ "기록했다"와 "보여줬다"는 다른 일이다. 감사할 때 **장부에만 있고
  화면에 없는 필드**를 목록으로 뽑아 볼 것.
→ 길이·용량 제한이 있는 출력은 **무엇부터 버리는지**를 명시적으로
  정할 것. 기본값은 늘 '뒤에 있는 것부터'이고, 고지는 대개 뒤에 있다.

### ⑯ **남의 문턱을 그대로 쓰지 말 것** — 표본이 다르면 문턱도 다르다

드리프트 감시는 이렇게 적혀 있었다(신용평가 업계 관행).

    PSI < 0.1 : 안정   /   0.1~0.25 : 주의   /   ≥0.25 : 드리프트

문제는 그 관행이 **표본 수만~수십만 건**을 전제한다는 것이다. 이 저장소는
`n_new=60`(최근 60거래일)으로 잰다. 같은 분포에서 뽑은 두 표본만
시뮬레이션해도:

    중앙값 0.19 · 95% 0.44 · 99% 0.94
    → 아무 일이 없어도 절반이 "주의", 29%가 "드리프트"

실제로 그날 장부에서 20종목 중 **17종목**이 0.25를 넘었다. 그대로 두면
"17종목 드리프트"라는 경보가 매일 나가고, 진짜 신호(0.97)와 잡음(0.31)이
같은 등급에 묶인다. **울리지 않는 경보보다 항상 울리는 경보가 나쁘다** —
전자는 없는 걸 알지만 후자는 있다고 믿게 만든다.

고친 방식은 새 통계를 발명한 게 아니라 **잣대를 표본에 맞춘 것**이다:
그 표본 크기의 귀무분포를 고정 시드로 뽑아 거기 견준다.

→ 논문·업계 관행에서 가져온 상수(문턱·기간·계수)는 **그 전제**를 함께
  적을 것. 전제가 우리와 다르면 그 상수는 우리 것이 아니다.
→ 문턱을 정할 땐 **"아무 일도 없을 때 이 값이 얼마나 나오나"** 를 먼저
  시뮬레이션할 것. 코드 열 줄이면 되고, 답이 바로 나온다.
→ 잣대는 **측정값과 같이 장부에 남길 것.** 숫자만 남기면 나중에 읽는
  사람이 문턱을 추측하고, 대개 관행값으로 추측한다.

### ⑰ 검사가 **닿지 않는 언어**가 있는지 볼 것

공개 사이트는 전부 `docs/*.html` 안의 인라인 자바스크립트로 돈다.
`fetch("status.json").then(...)` 하나가 화면 전체를 그린다. 그런데 그날까지
**그 코드의 문법을 확인해 주는 검사가 하나도 없었다.**

오타 하나면 이렇게 된다:

  · 파이썬 검사 전부 통과 (HTML을 텍스트로만 읽으니까)
  · CI 초록
  · 사이트는 "불러오는 중…"에서 영원히 멈춤

하루에만 감사 89·91·94·95·96·98·99·100이 이 파일들을 고쳤다. 그동안
안전망이 없었다. 파이썬 커버리지는 100%에 가까웠는데, **파이썬이 아닌
코드가 검사 밖에 통째로 있었다.**

→ 저장소에 어떤 언어가 있는지 세어 볼 것. 각 언어마다 "이게 깨지면
  무엇이 잡아주나"를 물을 것. 답이 없으면 그 언어는 검사 밖이다.
→ 실행이 어려우면 **파싱만이라도** 할 것(`new Function(src)` 한 줄이면
  된다). 문법 오류만 걸러도 '사이트가 통째로 죽는' 사고는 막는다.
→ 외부 도구가 필요한 검사는 **건너뛰지 말고 실패시킬 것.** 조용한
  skip은 없는 검사와 같다(오늘 "건너뜀은 통과가 아니다"를 다시 배웠다).

### ⑱ **감시의 창**은 감시 대상의 시각과 함께 계산할 것

dead man's switch는 "지난 26시간 안에 배치 커밋이 있는가"로 장부 연속성을
봤다. 값 자체는 그럴듯했지만 시각이 어긋나 있었다.

    SNS 커밋   22:45 UTC 실행 → 약 23:19 UTC 커밋
    감시 실행  23:30 UTC 예약 → 실측 23:56~00:01 UTC (크론 지연)
    26시간 창 시작 ≈ 21:56 UTC(전날) → **어제 커밋이 여유 1.3시간으로 창 안**

그래서 SNS 항목은 **하루짜리 누락을 구조적으로 못 잡았다.** 실제로 게시가
죽은 날 감시는 초록이 될 상황이었다. 검사도 공범이었다 — 크론 문자열과
`"26 hours ago"`를 **그대로 못 박기만** 하고, 그 값이 통하는지는 아무도
안 봤다.

→ 시간 창을 쓰는 감시는 **①오늘 것은 창 안 ②어제 것은 창 밖**을 동시에
  검사할 것. 하나만 보면 헛울리거나(①만) 못 잡는다(②만).
→ 지연을 넣고도 판정이 뒤집히지 않는지 볼 것(여기선 크론 +60분).
→ 값을 고정하는 검사는 **값이 옳은지는 확인하지 않는다.** 산술로 볼 것.

### ⑲ 고장난 계측기와 **보이지 않는 계측기**는 서로를 가려 준다

`feature_health`는 "외부 소스가 죽어 피처가 조용히 사라지는 것"을 잡으려고
만든 장치다. 그런데 목록의 11개 중 8개가 **존재하지 않는 이름**이었다
(`x_btc` ← 실제 `x_btc_ret5`, `x_fred_dgs10` ← 아예 없음). 태어날 때부터
매일 "0/11 전부 누락"을 기록했다.

왜 아무도 몰랐나 — **그 값을 어느 화면에도 보여주지 않았기 때문이다.**
장부에만 있고 화면에 없는 값은 틀려도 티가 나지 않는다. 그리고 픽스처가
같은 유령 이름을 적어 두어 **검사까지 같은 거짓말**을 하고 있었다.

→ 계측기를 만들면 **반드시 어딘가에 표시할 것.** 보이지 않는 계측기는
  고장 여부를 확인할 방법이 없다(감사 105를 고치자마자 106이 드러났다).
→ "이름 목록"을 손으로 적는 검사는 **목록이 실재하는지**부터 볼 것.
  픽스처는 목록에서 파생시킬 것 — 손으로 적으면 같이 썩는다.

### ⑳ **만든 사람이 헷갈리면** 읽는 사람은 반드시 헷갈린다

이 실험에는 계좌가 둘이다 — 통합 8만원 하나, 종목 1만원 20개(합 20만원).
사이트는 종목 계좌의 자산·비중을 아무 설명 없이 '종목별 현황'으로 보여줬고,
**이 프로젝트를 만든 사람이 직접 물었다**: "8만원인데 왜 20만원이지?"

숫자는 전부 맞았다. 그런데 **무엇의 숫자인지 모르면 거짓말과 같은 효과**를
낸다. 정직성이 유일한 자산인 채널에서는 '틀린 숫자'와 '설명 없는 숫자'의
피해가 같다.

→ 화면에 숫자를 놓을 때 "이건 무엇의 숫자인가"를 **열 이름 안에** 넣을 것.
→ 독자가 스스로 더해 보고 놀라기 전에 **합계를 먼저 밝힐 것.**
→ 합계는 장부에서 계산할 것. 산문에 박으면 종목이 늘 때 조용히 거짓이 된다.

### ㉑ **부품 검사와 배선 검사는 다른 것이다**

낙폭 자동 브레이크(킬스위치)에는 검사가 셋 있었다. 순수 함수
`_kill_switch_scale(prev, dd)`의 단계·히스테리시스, 변동성 스케일러와의
적용 **순서**, 그리고 소스 문자열. 셋 다 초록이었다.

그런데 배치에서 그 함수를 부르는 한 줄을 `risk_scale = 1.0`으로 바꾸자 —
즉 **브레이크를 통째로 뽑아 버리자** — 1,580개 검사가 전부 통과했다.
아무도 "낙폭이 커지면 노출이 준다"는 문장 자체를 확인하지 않았다.

부품이 완벽한 것과 부품이 연결된 것은 다르다. 그리고 검사가 많을수록
이 착각은 커진다 — 킬스위치는 이 저장소에서 **검사가 가장 많은 장치**였다.

같은 병이 곧바로 **다섯 개** 더 나왔다. 전부 "규칙은 옳게 적혀 있고,
그 규칙이 지켜지는지 확인하는 사람이 없었다"였다.

| 장치 | 무엇을 지우면 통과했나 |
|---|---|
| 미검증 엣지 변동성 게이트 | 배치가 게이트를 안 묻고 목표 20%를 박아 넣어도 |
| 한 종목 과집중 상한(3/n) | 상한을 `1e9`로 풀어도 |
| 의회 비중 EMA 감쇠 | 하룻밤에 전액을 옮겨도 |
| 의회 상관 실패 = 중복 | 실패를 '무상관'으로 되돌려도 |
| 낙폭 킬스위치 | 위 본문 |

공통점: **순수 함수로 부르는 검사만 있었다.** 함수 검사는 쓰기 쉽고
빠르고 결정적이라 계속 늘어나는데, 늘어날수록 "이 장치는 검사가 많다"는
안심만 커진다.

→ 안전장치는 **함수가 아니라 결과**로 검사할 것. 진짜 배치를 돌려,
  위험한 상태를 넣고 **행동이 달라지는지** 볼 것.
→ 검사는 '줄어든다'가 아니라 **정확한 값**을 요구할 것. 부등호는 감쇠를
  되돌리는 결함(예전에 실제로 있었다)을 통과시킨다.
→ 그리고 늘 **대조군**을 같이 둘 것 — 평시에 노출이 0이면 "줄었다"는
  검사는 `0 == 0`을 확인하는 헛것이 된다.

### 변이 항목이 가리키는 곳이 틀릴 수 있다

같은 회차에서 '배포판 실거래 잠금'을 무력화했더니 지정한 테스트가
통과했다. 결함처럼 보였지만 아니었다 — 전체 검사로 돌리니 **다른
파일**이 잡았다. 가리킨 곳이 틀렸을 뿐 장치는 지켜지고 있었다.

→ 변이를 못 잡으면 **전체 검사로 한 번 더** 돌릴 것. 안 그러면 없는
  결함을 만들어 내고, 진짜 구멍(위 킬스위치)과 구별할 수 없다.
→ 그리고 **같은 장치를 두 번 찌르지 말 것.** 항목 수를 부풀리면 '몇 개를
  지키고 있나'라는 이 도구의 유일한 숫자가 거짓이 된다(실제로 코인
  미완성 봉 항목이 중복으로 들어갔다).

### ㉒ **대조군 없는 '이상 없음'은 아무 말도 아니다**

풀링(패널) 학습의 룩어헤드 차단 검사가 매일 초록이었다. 미래 종목 데이터를
오염시켜도 신호가 안 바뀌니 "미래를 안 본다"로 읽혔다.

진짜 이유는 **풀링이 통째로 죽어 있었기 때문**이다. pandas 3.0의
`to_numpy()`가 읽기 전용 배열을 돌려주면서 `ok &= …`가 예외를 냈고,
`except Exception: return None`이 그것을 삼켰다. 풀을 넣든 안 넣든 결과가
같았으니, 오염시켜도 당연히 같았다.

그리고 살려 놓고 보니 **미래 행이 전부 들어오고 있었다.** `.asi8`는
마이크로초, 비교 상대는 나노초 — 1000배 차이라 상한 필터가 언제나 전부
통과였다. 그것을 고치자 **세 번째**가 나왔다: 스냅샷 폴더를 프레임 **끝**
날짜로 한 번만 골라서, 같은 과거 봉이라도 뒤에 미래가 얼마나 붙어 있느냐에
따라 풀이 달라졌다(링 인과성 검사 54봉).

    127 풀링이 죽어 있었다      → 128·129가 안 보였다
    128 미래 행이 전부 들어왔다 → 127을 고치자 드러남
    129 폴더 선택이 프레임 끝   → 128을 고치자 드러남(CI가 잡음)

죽은 장치는 그 뒤의 결함을 **전부** 가려 준다. 그리고 그 상태가 초록으로
보인다 — "이상 없음"과 "장치가 죽어 있음"이 구별되지 않는 것이 이 계열의
본질이다.

→ "나쁜 일이 안 일어난다"를 확인하는 검사 옆에는 **"좋은 일은 일어난다"를
  확인하는 대조군**을 반드시 둘 것. 오염이 모델을 흔드는가, 노출이 0이
  아닌가, 신호가 상수가 아닌가.
→ **정수로 바꾼 시각을 비교하지 말 것.** 단위가 어긋나도 조용히 통과한다.
  datetime64·timedelta64로 두면 라이브러리가 맞춰 준다.
→ **보너스 기능의 조용한 실패**를 허용하지 말 것. 실패해도 계속 가는 것은
  옳지만, 실패했다는 사실은 남겨야 한다(`pool_error_`).

### ㉓ **돌지 않는 검증은 장식이다 — 두 번째**

`verify` 명령이 있었지만 어떤 워크플로도 실행하지 않았던 것을 고쳤는데,
같은 일이 변이 시험 도구에서 되풀이됐다. `scripts/mutation_check.py`는
121개 항목을 쌓아 두고 **CI에서 한 번도 돌지 않았다.** 다른 모든
안전장치를 지키는 도구가 정작 아무도 안 지키는 상태였다.

게다가 그 도구의 머리말은 "건너뜀은 통과가 아니다"라고 적어 놓고,
종료코드는 `1 if (missed or broken)` — **건너뜀을 통과로 취급**했다.
121개 항목이 전부 코드와 어긋나도 0을 준다.

→ 도구를 만들면 **그날 안에 워크플로에 건다.** 안 걸면 다음 리팩터링에서
  조용히 죽는다.
→ 무거우면 **둘로 쪼갠다** — PR마다 도는 몇 초짜리 구조 검사, 야간에 도는
  전수. 무겁다는 이유로 안 거는 것이 가장 나쁘다.
→ 문서가 약속한 실패 조건을 **종료코드가 그대로** 지키는지 볼 것.

### ㉔ **검사와 실전이 같은 세계에 있는지 확인할 것**

오늘 가장 비쌌던 두 결함(풀링 사망 · 학습 상한 무력화)은 pandas 3.0의
동작 변화가 원인이었다. 그런데 실전 배치는 pandas **2.3.3**으로 돌고
있었다 — 장부 환경 지문이 매일 그렇게 적고 있었는데도 아무도 안 봤다.

    검사(CI·개발)  pandas 3.0.5
    실전(매일 배치) pandas 2.3.3

`requirements.txt`는 "어느 날 갑자기 메이저가 설치돼 동작이 바뀌는 사고를
막는다"고 적어 놓고 `pandas>=2.0,<3`을 선언했지만, **그 파일로 설치하는
워크플로는 감사 잡 하나뿐**이었다. 돈을 굴리는 배치들은 전부 버전 없이
`pip install pandas …` — 그날 PyPI 최신이었다.

한쪽 세계에만 있는 결함은 다른 쪽에서 **영원히 안 보인다.** 실전 장부만
보면 그 결함은 존재하지 않았고, CI만 보면 정상 동작을 한 번도 못 봤다.

그리고 장부에는 매일 `lock:3e29b4dc…`가 찍혔다. 읽는 사람은 고정돼
있다고 믿는다 — 그 해시는 **파일 내용의 해시일 뿐**이었다.

→ 검사와 실전은 **같은 파일 하나**로 설치할 것.
→ '고정했다'는 주장은 **돌고 있는 환경이 그 범위 안인지** 검사로 못 박을
  것. 파일에 적는 것만으로는 아무것도 고정되지 않는다.
→ 해시·지문이 **실제로 보장하지 않는 것**을 보장하는 것처럼 보이지 않게
  할 것(어긋나면 `deps!N`으로 말하게 했다).
→ 환경 차이를 기록해 두었다면 **그 기록을 실제로 읽을 것.** 매일 찍히던
  `pd2.3.3`이 답을 이미 갖고 있었다.

### ㉕ **검사가 순서에 의존하면, 초록도 빨강도 우연이다**

`test_markets_module_is_dependency_free`는 "quant.markets는 pandas 없이
임포트돼야 한다"고 적어 놓고 `importlib.reload(m)` 한 줄이 전부였다.
전체 검사에서는 앞선 테스트가 이미 pandas를 임포트해 둔 뒤라 reload는
언제나 성공한다 — **그 주장을 한 번도 확인한 적이 없었다.**

그리고 그 reload가 `quant.markets`의 상수를 새 객체로 다시 만들어, 바로
아래 '같은 객체인가' 검사를 오염시켰다.

    그 파일만 단독 실행 → 실패
    전체 검사          → 통과 (둘 다 reload 이전의 낡은 객체를 가리켜서)

전체가 초록인 이유가 '정상'이 아니라 '우연'이었다. 그리고 아무도 그 파일을
단독으로 돌리지 않으니 영원히 안 보인다 — 변이 시험이 기준선을 잡느라
단독 실행한 덕에 드러났다.

→ 검사 안에서 **모듈을 reload하지 말 것.** '무거운 의존 없이 임포트되는가'
  같은 주장은 **별도 프로세스**에서 그 의존을 실제로 막고 확인할 것.
→ 최상단에서 잡아 둔 별칭과 동일성을 비교하지 말 것 — 살아 있는 모듈에서
  그때그때 읽을 것.
→ '있는가'가 아니라 **'쓸 수 있는가'**를 물을 것. `key in mapping`은 값이
  None이든 오타든 통과한다.

### ㉖ **이름을 바꿨으면 읽는 쪽을 전부 찾을 것 — 조용히 0을 읽는다**

감사 119에서 장부의 `turnover.ratio`를 `symbols_ratio`로 바꾸고 진짜
회전율 `traded`를 추가했다. 그런데 **소비처를 찾지 않았다.** 경보 코드는
그대로 `.get("ratio") or 0.0`이었고, 없는 키를 0으로 읽어

    비용 = 0 × 252 × 0.0043 = 0  →  경보가 한 번도 울릴 수 없다

같은 날 "고친 결함은 형제를 찾기 전까지 고친 게 아니다"(⑭)를 적어 놓고
그 회차에 내가 어겼다. 게다가 검사는 **낡은 픽스처**(`{"ratio": …}`)를
써서 그 죽음을 매일 초록으로 봤다.

`or 0.0`·`or ""`·`get(k, 0)`은 편의처럼 보이지만, **고장을 '이상 없음'으로
바꾸는 변환기**다. 없는 값과 0인 값은 다른 뜻이다.

→ 장부 키 이름을 바꾸면 **그 문자열을 저장소 전체에서 grep**할 것.
→ 없는 값에 기본값을 씌우기 전에 물을 것: 그 기본값이 **안전한 쪽**인가?
  경보라면 '못 잰 값'은 0(조용함)이 아니라 **판정 보류**여야 한다.
→ 검사 픽스처를 손으로 적지 말 것. **장부가 실제로 쓰는 키**에서 파생시킬
  것(그래서 이름이 갈리면 검사가 먼저 빨개진다).

### ㉗ **주입 없는 검사는 네트워크가 없는 날 조용히 헛돈다**

감사 138의 검사를 처음 이렇게 썼다.

    out = _run(...)
    if not out.get("orders"):
        return                    # ← 데이터 수신 실패면 그냥 통과

이 환경에는 네트워크가 없다. 그래서 시세 수신이 실패 → 종목이 통째로
스킵 → 주문이 하나도 안 생김 → **네 검사가 전부 아무것도 확인하지 않고
초록**이었다. 하루 종일 그 병을 잡아 놓고 내가 만들었다.

게다가 대조군을 고치다 두 번째 함정도 나왔다: 챔피언이 관망(0)을 내면
예산이 0이라 **어느 가격이든 0주**가 된다. 그러면 '비싸서 못 샀다'와
'신호가 없어서 안 샀다'가 구별되지 않아 대조군이 성립하지 않는다.

→ 외부에 의존하는 경로는 **데이터·부착기·사이징까지 주입**할 것.
→ `if not X: return`으로 빠져나가지 말 것. 전제가 안 서면 **실패**시켜야
  한다 — 조용히 통과하면 그 검사는 영원히 아무것도 안 본다.
→ 대조군은 **한 변수만** 다르게 할 것. 다른 이유로도 같은 결과가 나오면
  그건 대조군이 아니다.

### ㉘ **파이프 뒤의 종료코드는 앞 명령의 것이 아니다**

전체 검사를 이렇게 돌렸다.

    pytest tests/ -q | tail -8      →  "exit 0"

파이프의 종료코드는 **`tail`의 것**이다. pytest가 실패해도 0이 나온다.
그 결과 하루 동안 두 번, 실패한 검사를 '통과'로 보고했고 **CI가 대신
잡아 줬다**(한 번은 진짜 룩어헤드였다 — 감사 129).

이 저장소는 워크플로에서 같은 결함을 이미 고쳤다(감사 116, `shell: bash`로
`pipefail` 활성화). 규칙은 코드에만 적용한 것이 아니었는데, 정작 **감사하는
사람의 손버릇**에는 적용하지 않았다.

→ 검사 결과는 **종료코드를 직접** 읽을 것. 파이프를 쓸 거면 `PIPESTATUS[0]`
  이나 파일로 받고 나서 볼 것.
→ 그리고 종료코드만 믿지 말 것 — 출력에서 `FAILED` 줄도 함께 셀 것.
  두 신호가 어긋나면 그 자체가 도구 고장의 신호다.

### 도구 자신도 세 번 틀렸다

변이 시험 도구가 (a) 없는 테스트 파일을 '잡았다'로 세고, (b) 아무것도
바꾸지 않는 변이를 '못 잡음'으로 세고, (c) 복원 후에도 변조된 .pyc가
재사용돼 거짓 결과를 냈다. (c)가 가장 위험했다 — 반대 방향이면 결함 있는
코드를 '안전하다'고 보고한다.

→ **검증 도구도 검증할 것.** 통과를 안심으로 읽지 말고, 실패를 결함으로
  읽지도 말 것(그 변이가 진짜 동작을 바꾸는지부터 확인).

## 구현 완료

- ✅ (2026-08-09, 알파 14차) **배포판 실거래 잠금 E2E 스모크** — 빌드
  워크플로가 실거래 CLI 3경로를 진짜 실행해 차단을 확인하고, 빌드된
  실행파일도 QUANT_SMOKE 모드로 표식·잠금을 기계 검증한다(로그 echo만
  믿는 검증은 회귀를 못 잡는다).
- ✅ (2026-08-09, 알파 14차) **세대별 기록 아카이브** — 판정 시계에
  '이전 구조 누적 / 현재 구조' 구간별 일수·TWR 분리 표시 + 차트에 구조
  교체 마커. 이전 세대 기록이 현 구조 실적처럼 읽히는 착시를 마저 제거.
- ✅ (2026-08-09, 알파 14차) **체결 가정 검증(fill_check)** — 페이퍼
  실측 개장 갭 vs 백테스트 슬리피지 가정을 시장별 집계해 사이트 공개.
  실측 불리 평균이 가정을 넘으면 '백테스트 낙관 의심'을 스스로 단다.
  첫 실측: 한국주식 불리 갭 평균 ~79bp vs 가정 14bp → 낙관 플래그 점등
  (표본 3건 — 표본이 쌓이면 비용 프리셋 상향 검토).

- ✅ (2026-08-08) **사이트 신뢰도 곡선에 윌슨 95% 신뢰구간 병기** — 소표본
  비율이 확신처럼 읽히지 않게. 표본 30일 이상인데 신뢰구간이 예측 구간을
  완전히 벗어나면 "⚠️ 보정 어긋남" 플래그 표시(과신 모니터링).
- ✅ (2026-08-08) **확률대 적중률 전 종목 합산 폴백** — 종목 표본 25건
  미달 시 전 종목 합산(이질성은 문구에 명시)으로 표본 조기 확보, 둘 다
  미달이면 "표본 축적 중 (n=X)".
- ✅ (2026-08-08) **deadman 감시 범위 확장** — 재학습·페이퍼에 더해
  SNS 콘텐츠 커밋, 배포된 사이트(status.json)의 신선도까지 확인.
- ✅ (2026-08-08) **외부 하트비트 2중화** — daily-paper 성공 시
  HEARTBEAT_URL(healthchecks.io류)로 핑. GitHub 전체 장애도 외부에서 경보.
  ⚠️ 활성화하려면 사장님이 healthchecks.io 체크 생성 후 GitHub Secret
  `HEARTBEAT_URL` 등록 필요.

## 대기 중

- ⏳ **60~70% 확률대 과신 대응** — 현재 신뢰도 곡선에서 60~70% 구간 실제
  상승 38%(8일 표본). 사이트의 '보정 어긋남' 플래그가 표본 30일부터 자동
  감시한다. 플래그가 켜지면: 보정(calibrate) 챌린저 승격 여부 확인 →
  그래도 지속 시 확률 상한 클립 등 보수적 개입 검토. (통계적 확증 전
  개입은 과적합 — 관찰 단계)
- ⏳ **한국주식 체결 비용 프리셋 재검토** — fill_check 첫 실측에서 불리
  갭 평균 ~79bp vs 가정 14bp(표본 3건). 표본 30건 이상 쌓여도 가정 초과가
  유지되면 MARKET_COST_PRESETS.kr_stock 슬리피지 상향(백테스트 보수화).
  소표본 과잉반응은 금물 — 관찰 단계.

## 종결 (역사 기록)

- ✔ (기록) "알파 5차 예약(2026-08-10)" 항목은 알파 5차~13차가 이미 순차
  구현되며 소화됐다 — 크로스에셋(5차)·수급(10차) 포함. 낡은 예약만 정리.

### ㉙ **0과의 비교는 부동소수에서 거의 언제나 틀린다**

샤프를 계산하는 자리 다섯 곳이 전부 이렇게 막고 있었다.

    sd = r.std(ddof=1)
    if sd <= 0:  ...        # 또는  if sd > 0:  /  .replace(0.0, np.nan)

그런데 **값이 전부 같아도 표준편차는 정확히 0이 아니다.**

    np.full(100, 0.001).std(ddof=1)  ==  2.18e-19
    샤프 = 0.001 / 2.18e-19 = 4.59e15
    DSR  = 1.0                          ← "확실한 실력"

관망 중 고정 비용, 거래정지된 시세, 짧은 검증 구간에서 우연히 같은 값 —
이런 계열이 검증 3종을 만점으로 통과하고, 그 판정이 엣지 게이트를 풀어
목표 변동성을 12%→20%로 올린다.

**크기에 견주어** 본다: `sd <= 1e-12 × |평균|`. 절대 문턱으로 막으면 고빈도·
소수점 계열까지 퇴화로 몰아 가드가 덫이 된다(④).

판정은 `quant/utils/numerics.py` 한 곳에 둔다 — 다섯 곳에 흩어 놓으면
다음에 또 네 곳만 고친다(⑭).

### ㉚ **변이를 안 잡으면 코드가 아니라 내 시나리오가 틀렸을 수도 있다**

PBO의 퇴화 가드에 변이를 걸었는데 검사가 못 잡았다. 검사를 강화하기 전에
왜 못 잡는지부터 봤더니 — **내가 만든 피해 시나리오가 피해를 주지 않았다.**

전 구간이 상수인 설정은 샤프가 1e15로 튀어도 IS에서도 OOS에서도 1등이라
λ가 양수로 남는다. PBO는 그대로다. 진짜로 해로운 건 **일부 구간만 평평한**
설정이다 — 오래 관망하다 뒤늦게 무너진 전략. 그걸로 다시 만드니

    가드 없음  PBO 0.056 → 0.560
    가드 있음  PBO 0.056 → 0.056

검사가 침묵할 때 기본 반응이 '검사를 세게 만든다'면, 존재하지도 않는
피해를 못 박는 검사를 쓰게 된다. 먼저 **피해를 실제로 재현**할 것.

### ㉛ **입력을 나열한 순서가 돈의 배분을 바꾸면 안 된다**

HRP는 상관 구조를 나무로 읽어 예산을 나눈다. 그런데 통계적으로 동일한
자산들 사이에서 단일 연결 군집의 '잎 순서'는 **입력 열 순서**로 갈리고,
재귀 이분은 그 순서를 그대로 비중으로 옮긴다.

    A,E,B,F,C,D →  A 0.127 · B 0.125 · C 0.487 · D 0.259
    F,C,A,D,E,B →  A 0.128 · B 0.483 · C 0.127 · D 0.260

같은 데이터인데 목록 순서만 바꿔서 한 종목 비중이 4배 뛴다. 호출 쪽 열
순서는 `rets_map`(=AUTO_TARGETS) 삽입 순서라, 종목을 한 줄 옮겨 넣는
것만으로 어제와 다른 포트폴리오가 나간다.

이 저장소는 재현성을 전제로 돌아간다 — 시드 고정, `env_fingerprint`,
`snapshot_pool_day`. 그런데 **배분에는 그 규칙이 걸려 있지 않았다.**
계산 전에 이름순으로 못 박으면 끝이다.

형제 확인 결과 ERC는 원래 순서 무관이었고(반복법이 대칭, 실측 차이 8e-17),
`_xsec_tilt`에는 "유니버스 나열 순서가 배분을 좌우하면 안 된다"고 이미
적혀 있었다. **한 곳만 빠져 있었다**(⑭).

남은 성질 하나는 고치지 않고 재 두었다: 유니버스가 바뀌면(종목 하나가
데이터 실패로 빠지면) 군집 나무가 바뀌어 남은 종목 사이 배분이 재배치된다.
조용한 군집의 **총 예산은 그대로**(0.9978 → 0.9979)이므로 위험 판단은
흔들리지 않고, 비용은 순수하게 회전(36.6%)이다. 재귀 이분의 성질이라
고칠 수 없다 — 대신 수치로 못 박아 두었다.

### ㉜ **아무도 안 보는 경로에 안전장치가 더 필요하다**

주문 견고화 래퍼(`RobustBroker` — 재시도·체결확인·규격검사)가 어디에
걸려 있었는지 세어 보니 이랬다.

    cli `trade`   (사람이 '실전' 두 글자를 입력)      → 걸려 있음
    cli `webhook` (사람이 'yes'를 입력)               → 걸려 있음
    `live-daily`  (kr-live.yml이 매일 무인 실행)      → **없음**

사람이 지켜보고, 두 글자를 직접 입력하고, 결과를 눈으로 보는 경로에는
전부 걸려 있었다. **매일 새벽 아무도 안 볼 때 도는 경로에만 없었다.**
순서가 정확히 반대다 — 사람이 보는 자리는 사람이 이상을 눈치채지만,
무인 배치는 그렇지 않다.

이건 우연이 아니라 만들어지는 방식의 문제다. 대화형 명령은 만들 때 손으로
돌려 보니 문제가 바로 보이고, 배치는 "페이퍼에서 되니까"로 넘어간다.

없으면 무슨 일이 생기는가.

    재시도 없음      HTTP 오류 한 번에 그 종목이 그날 통째로 빠진다.
    체결확인 없음    KIS 접수 응답은 status='accepted', filled=0이다.
                     `kr_live.py`는 "실제 체결은 RobustBroker의
                     confirm_fills가 측정한다"고 **적어 두었는데**
                     그 래퍼가 없어서 아무도 측정하지 않았다.
                     → 한 주도 안 체결된 날에도 장부는 "주문 N건".
    규격 없음        min_qty·qty_step이 0이라 1주 미만도 내려간다.

마지막 것은 ⑭의 교과서적 사례다. 감사 139는 "RobustBroker가 브로커에게
규격을 묻도록" 고쳤는데, **답할 줄 아는 브로커가 코인 하나뿐이었다.**
주식 둘은 메서드 자체가 없어 기본 규격(전부 0)으로 떨어졌다. 물음을
고치고 답을 안 고치면 고친 게 아니다.

규칙: **집행 경로를 새로 만들면, 기존 경로가 무엇을 거쳐 가는지부터
세어 볼 것.** 새 경로가 그중 하나라도 안 거치면 그 이유를 적을 것.

### ㉝ **분모가 작을수록 많이 사는 규칙에는 반드시 상한이 있어야 한다**

역분산·ERC·HRP는 전부 "변동성이 작을수록 많이"다. 그러면 분모가 사실상
0인 종목이 나타났을 때 그 종목이 **포트폴리오 전부**를 가져간다.

    s0~s3  분산 1.0e-04  → 역분산 1.2e+04
    MMF    분산 4.6e-41  → 역분산 2.2e+40
    ─────────────────────────────────────
    MMF 비중 1.000

`np.isfinite`로 막고 있었는데 2.2e40은 유한하다(㉙과 같은 병, 이번엔
**배분의 분모**). 그리고 이건 가공의 입력이 아니다 — 매일 같은 비율로 오르는
상품(예금형 ETF·MMF·고정 배당)의 pct_change가 정확히 이 모양이다.
고유값 3개, 표준편차 9.2e-17.

두 겹으로 막는다.

    ① 퇴화 판정   패널 최대 분산에 견주어 무시할 만큼 작은 열은 뺀다.
    ② 과집중 상한 균등가중의 3배.

②가 따로 필요한 이유: 30일에 한 번 1틱 움직이는 저유동 종목은 부동소수
문제가 아니라 **진짜로 분산이 작다.** 퇴화 판정으로는 안 걸리고 걸려서도
안 된다 — 그건 상한이 막을 일이다(0.997 → 0.600).

세 가지를 더 배웠다.

**빼는 것이 주는 것이 될 수 있다.** 호출 쪽이 `slices.get(key, 1.0/n)`이라
키를 지우면 그 종목이 **기본 슬라이스**를 받는다. 반드시 0으로 남겨야 한다.

**합이 0인 배분도 참(truthy)이다.** 호출자는 `if not w:`로만 검사하는데
값이 전부 0인 dict는 통과한다 — 폴백이 안 걸리고 하루 통째로 관망이 된다.
배분을 돌려주기 전에 **합이 1인지** 확인한다.

**같은 알고리즘이 두 파일에 따로 구현돼 있었다.** `live/hrp.py`(실거래)와
`portfolio/allocation.py`(오디션). 감사 147에서 앞의 것만 고치고 뒤를 못
찾았다 — 파일 이름으로 형제를 찾았기 때문이다. **알고리즘 이름으로 찾을 것**(⑭).

### ㉞ **같은 판정을 두 곳에서 쓰면 언젠가 갈라진다 — 극단값에서 먼저**

확률 보정은 구간을 0.10 폭으로 나눠 '어긋남이 확정된' 구간에만 개입한다.
그런데 구간 판정이 두 곳에서 달랐다.

    표를 만들 때  k = min(int(p * bins), bins - 1)   ← p=1.0을 마지막 칸에
    적용할 때     row["lo"] <= p < row["hi"]         ← p=1.0은 어디에도 안 걸림

결과:

    예측 0.97 · 실제 적중률 0.50 · 표본 80 → 어긋남 확정
        prob 0.970 → 보정 0.50 (개입)
        prob 0.999 → 보정 0.50 (개입)
        prob 1.000 → 보정 1.00 (개입 없음)

p=1.0은 **증거로는 세어지는데 보정은 안 받는다.** 하필 "100% 오른다"는
가장 과신한 말이 통째로 검문을 피해 간다.

두 표현이 대부분의 입력에서 같은 답을 내니까 오래 안 보인다. 갈라지는 건
**경계값 하나**뿐이고, 경계값은 보통 가장 극단이라 가장 위험하다.

규칙: 판정은 함수 하나로 만들고 **양쪽이 그걸 부른다.** 같은 규칙을 두 번
쓰지 않는다(㉙에서 `numerics.py`로 모은 것과 같은 이유).

### ㉟ **읽기와 쓰기는 다른 코드다 — 검사가 둘 다 지나가야 한다**

'낡은 시세로 평가하기'를 고치고 검사를 넷 썼는데, 변이 시험이 하나를
잡지 못했다. **쓰기를 통째로 지워도 검사가 통과했다.**

이유는 단순했다. 검사가 `last_price`를 상태 파일에 **미리 심어 두고**
시작했다. 그래서 '심어 둔 값을 읽는가'만 확인했을 뿐, 그 값을 **남기는**
코드는 한 번도 지나가지 않았다. 하루만 돌리는 검사로는 구조적으로 못 잡는다.

이틀을 돌리는 검사로 바꾸니 잡혔다 — 첫날 정상, 둘째 날 장애.

상태를 거쳐 가는 기능(어제 남긴 것을 오늘 읽는 것)은 **한 번의 실행으로
검사할 수 없다.** 저장·복원·이월·쿨다운·마지막 체결일 — 전부 같다.

### ㊱ **없는 값의 기본값이 '손실 0'이면 안 된다**

`PaperBroker.equity(marks)`는 marks에 없는 종목을 **매입가**로 평가한다.
편해 보이지만 "산 뒤로 한 푼도 안 움직였다"고 치는 것이다.

포트폴리오 배치는 데이터를 못 받은 종목을 `prices`에서 빼는데 포지션은
그대로 복원했다. 그래서 그 종목의 손익이 통째로 0이 됐고, 그 자산이

    · 장부의 자산·수익률
    · 킬스위치가 읽는 낙폭(twr_index)
    · 사이트에 나가는 TWR

로 그대로 흘러갔다. **폭락한 종목이 하필 그날 데이터 장애를 만나면 손실이
사라지고 브레이크도 안 걸린다.** 두 사건은 같이 일어날 이유가 충분하다 —
거래정지·상장폐지·거래소 장애는 급락과 함께 온다.

기본값을 고를 때는 **어느 쪽으로 틀리는지**를 본다. '손실 0'은 언제나
낙관 쪽으로 틀리고, 낙관 쪽 오류는 안전장치를 푸는 방향이다.
마지막으로 알던 시장가도 틀린 값이지만 덜 틀리고, 무엇보다 그 사실을
장부에 남기면 **틀렸다는 것이 보인다.**

### ㊲ **손으로 적은 데이터에는 만료일이 있고, 만료는 조용히 온다**

FOMC 가드는 손으로 적어 둔 날짜 목록으로 돈다. 목록은 2027년까지다.
그다음 날부터:

    event_dates()에 그날이 없다 → is_event_day() = False
      → 가드가 아무것도 막지 않는다
        → "오늘은 주요 이벤트 없음(매매 허용)"을 **매일** 말한다

에러도 경고도 장부 흔적도 없다. **꺼진 안전장치보다 나쁜 것은 꺼진 줄
모르는 안전장치다.**

`CALENDAR_END` 상수는 있었다. 그런데 **읽는 곳이 한 곳도 없었고**(⑭ 계열),
손으로 적은 값이라 목록과도 어긋나 있었다(12/31 vs 실제 12/08).

세 가지를 한다.

1. **끝을 데이터에서 계산한다** — `max(FOMC_DATES)`. 손으로 적으면 목록을
   늘렸을 때 상수만 뒤처진다.
2. **판단문이 '없음'과 '모름'을 구분한다.** 달력이 끝난 뒤의 "이벤트 없음"은
   사실이 아니라 무지다.
3. **만료 자체를 없앤다.** 사장님 지시로 한 걸음 더 갔다(감사 155) —
   공표 목록이 끝난 뒤는 **규칙으로 추정해 채운다.** 규칙은 마지막 공표
   연도에서 자동으로 뽑는다("그 달의 마지막 수요일이면 마지막, 아니면
   앞에서 N번째"). 실측 정확도는 2026년 8/8, 2025년 7/8이고, 틀릴 수 있는
   만큼 추정은 ±3일로 더 넓게 가린다 — **모르면 더 조심하는 쪽으로 기운다.**

   지평선은 `오늘 + 30년`이라 매 실행마다 다시 계산된다. 즉 몇 년이 지나도
   앞으로 30년은 언제나 채워져 있다. 이게 '영구'의 실제 구현이다.

   그래도 **추정은 추정이라고 말한다.** 판단문에 `projected: True`와
   "공표 일정이 …까지뿐이라 추정 달력으로 판단했다"를 남긴다. 가드가 도는
   것과 근거가 확정인 것은 다른 문제다.

같은 모양의 데이터가 또 있는지 볼 것: 하드코딩된 휴장일, 종목 코드, 만기
스케줄, 세율·수수료율, API 버전. **끝이 있는 목록에는 그 뒤를 메울 규칙이
있어야 하고, 규칙으로 메운 자리는 그렇다고 밝혀야 한다.**

### ㊳ **성적이 운인가와 파라미터가 운인가는 다른 질문이다**

검증 관문이 셋 있었다.

    워크포워드+DSR   이 성적이 운인가(다중검정 보정)
    PBO              IS 1등이 OOS에서 동전던지기인가
    CPCV             여러 경로에서 일관된가

셋 다 **성적**을 묻는다. 넷째 질문이 빠져 있었다 — **이 파라미터가 운인가.**
옆칸으로 한 스텝만 옮겨도 무너지는 설정은, 데이터가 조금만 달라져도 무너진다.
성적 셋이 전부 통과해도 이건 따로 걸릴 수 있다.

    fast\slow   20     40     60
       5       0.10   0.11   0.12
      10       0.11   9.90   0.13     ← 혼자 9.90 = 외딴 봉우리
      20       0.12   0.13   0.14

`stability.py`가 그걸 재고 있었다. 그런데 **부르는 곳이 한 곳도 없었다** —
`__init__`에서 export만 되고 운영 코드에도 CLI에도 없었다(⑭ 계열, 135·139·150과
같은 병). 도구가 있는데 쓸 방법이 없으면 없는 것과 같다.

배선은 **끝까지** 해야 한다. 계산 → 콘솔 출력 → 장부 기록 → 매일 도는 경보.
셋만 하고 멈추면 그 값은 하루 뒤에 사라진다(㉜).

### ㊴ **한 곳에 모으는 것과 한 값으로 재는 것은 다르다**

감사 146에서 '표준편차 0 판정'을 `numerics.py` 한 곳으로 모았다. 옳은
결정이었다(⑭). 그런데 149에서 배분기까지 같은 상수를 쓰게 되면서 **서로
다른 두 질문이 한 문턱을 공유**하게 됐다.

    ① 이 분산은 부동소수 잡음인가      패널 안 최대 분산 대비(분산 대 분산)
    ② 이 계열에 쓸 만한 분산이 있는가  그 계열의 크기 대비(표준편차 대 크기)

①은 아주 엄격해야 한다 — 진짜로 조용한 자산(저유동 ETF)을 잡음으로 몰면
배분에서 통째로 지워지고 그건 가드가 덫이 되는 것이다(④).
②는 훨씬 느슨해야 한다 — 표준편차가 평균의 100만분의 1인 계열은 잡음이든
아니든 샤프를 말할 수 없다.

한 문턱(1e-12)으로 둘을 재니 ②가 새어 나갔다. 실측:

    매일 정확히 +0.005%씩 오르는 계열(예금형 ETF·MMF)
        std/평균 = 2.3e-12          ← 문턱을 **간발의 차로** 통과
        몬테카를로 샤프 중앙값 = 8,130,000,000,000

**중복을 없애는 것과 구분을 없애는 것은 다르다.** 같은 코드를 쓰더라도
질문이 다르면 파라미터는 나뉘어야 하고, 각 상수 옆에 '이 문턱이 답하는
질문'이 적혀 있어야 한다.

그리고 이런 경계 결함은 **간발의 차**로 숨는다. 2.3e-12 대 1e-12 —
자릿수가 같으면 눈으로는 안 보인다. 문턱을 정할 때는 실제 계열의 값이
문턱에서 **몇 자릿수 떨어져 있는지**를 적어 둘 것.

### ㊵ **경보 검사는 문구를 부정하지 말고 표식을 볼 것**

오늘 같은 실수를 **세 번** 했다(감사 136·154·160).

    assert "이벤트 없음" not in reason     ← 경고문이 "'이벤트 없음'이 아니라…"
    assert "과대확신" not in text          ← 정상 문구가 "뚜렷한 과대확신 구간은 없습니다"
    assert '"ratio"' not in src            ← 내가 쓴 설명 주석에 걸림

패턴은 늘 같다. **경보가 없다는 것을 확인하려고 경보의 단어를 부정하는데,
그 단어가 정상 문구나 설명문 안에도 들어 있다.** 사람이 읽는 글을 검사
대상으로 삼으면 언제나 이렇게 된다.

부정 단언은 **경보에만 있는 표식**으로 한다.

    assert "←" not in text and "⚠️" not in text      # 경보 마커
    assert not gate.get("projected")                 # 구조화된 플래그
    assert "매매 허용" not in reason                  # 평시 문장에만 있는 말

가장 좋은 것은 애초에 **문자열이 아니라 구조**를 보는 것이다 — 판단문에
`{"projected": True}` 같은 칸을 두고 그걸 검사한다. 문구는 언제든 다듬어지고,
다듬을 때마다 검사가 조용히 무의미해진다.

### ㊶ **관문은 '중요한 값'이 아니라 '실제로 쓰는 값'을 검사해야 한다**

데이터 무결성 관문(`scan_ohlcv` + `is_severe`)은 페이퍼·포트폴리오 두 배치의
입구에 서 있다. 데이터가 깨졌으면 그날 매매를 멈추라고 세운 문이다.

그런데 OHLC 밴드 검사가 **close만** 보고 있었다.

    bad = (high < low) | (close > high) | (close < low)      ← open이 없다

이 시스템은 주식 체결을 **다음 봉 시가**로 한다. 즉 관문이 **체결에 쓰는
바로 그 값**을 안 보고 있었다. 실측으로 밴드 99~101인 봉의 시가를 5000으로
바꿔도 위반 0건·통과였고, 그 5000이 그대로 체결가가 된다.

close를 검사한 것이 이상한 게 아니다 — close가 '대표 가격'처럼 느껴지니까
그것부터 검사한 것이다. 하지만 관문의 기준은 **직관적 중요도가 아니라
아래 코드가 실제로 읽는 값**이다.

규칙: 검증기를 쓸 때는 그 데이터를 **소비하는 쪽을 먼저 읽는다.**
어느 컬럼이 돈으로 바뀌는지 확인하고, 그 컬럼부터 검사한다.

### ㊷ **파일에 조금씩 흘려 쓰면, 죽었을 때 반쪽이 남는다**

`df.to_csv(path)`는 파일을 여는 즉시 **비우고** 한 줄씩 흘려 쓴다. 중간에
죽으면(Ctrl-C·디스크 참·OOM·컨테이너 종료) 반쪽짜리 파일이 남는다.

반쪽이라도 **문법은 멀쩡하다.** 그래서 다음 실행의 `read_csv`가 조용히
성공하고, 검증기(`_validate`)도 통과한다. 형태가 맞으니까.

실측(200봉 캐시를 절반으로 자름):

    돌려준 봉 99개 · 제공자 재호출 없음
    마지막 봉 2025-04-09   (진짜는 2025-07-19)

TTL 한 시간 동안 계속 **3개월 묵은 봉**이 '오늘'로 쓰인다. 조용히 틀린다.

규칙 두 가지.

1. **원자적으로 쓴다.** 임시 파일에 다 쓴 뒤 `os.replace`로 갈아끼운다.
   같은 파일시스템 안의 replace는 원자적이라 '반쪽 상태'가 존재할 수 없다.
   (이 저장소는 상태 JSON에 이미 `atomic_write_json`을 쓰고 있었다 —
   알고 있었는데 캐시에만 안 썼다.)
2. **길이를 함께 적는다.** 저장할 때 봉 수를 옆에 남기고 읽을 때 대조한다.
   원자적 쓰기가 막지 못하는 훼손(파일시스템 손상·사람이 건드림)까지 잡는다.

일반화: **"읽었더니 형식이 맞다"는 "온전하다"가 아니다.** 잘림·절단은
형식을 안 깨뜨린다. 온전함은 형식이 아니라 **약속된 크기**로 확인한다.

### ㊸ **캐시는 투명해야 한다 — 켜고 끄면 답이 달라지는 스위치는 덫이다**

`CachedDataProvider`는 "켜면 빨라진다"는 얼굴을 하고 있다. 그런데 켜는 순간
세 가지가 달라지고 있었다(감사 162).

    ① `df.attrs`가 사라진다 — CSV는 값만 저장한다.
       감사 135가 넣은 '어느 소스가 줬는가'(무조정가인 보조 소스인가)가
       캐시를 지나면 없어진다.
    ② 값이 비트 단위로 달라진다 — pandas의 빠른 CSV 파서는 왕복 정확도를
       보장하지 않는다(300칸 중 52칸이 최대 1.4e-14).
    ③ 잘린 캐시를 온전한 것처럼 내준다(㊷).

셋 다 **성능 최적화가 조용히 데이터를 바꾼** 경우다. 최적화의 계약은
"같은 답을 더 빨리"인데, 저장·직렬화가 끼면 그 계약이 새기 쉽다 —
직렬화 형식(CSV)이 원본(DataFrame)보다 표현력이 낮기 때문이다.

규칙: 캐시·메모이즈·스냅샷을 넣을 때 검사는 결과가 아니라 **동치**로 쓴다.

    assert cached.equals(fresh)                    # 값 (비트 단위)
    assert dict(cached.attrs) == dict(fresh.attrs) # 부가정보
    assert len(cached) == len(fresh)               # 길이

그리고 **부가정보를 잃는 쪽으로 조용히 실패하지 말 것.** 출처를 모르면
비워 두고(`{}`), 절대 그럴듯한 기본값을 지어내지 않는다.

곁들여: 이번 것은 **오늘 터지는 결함이 아니다.** 캐시를 켜는 곳은 학습
루프뿐이고 새벽 배치는 캐시를 안 쓴다. 그래도 고쳤다 — 캐시는 다음 사람이
배치에 켜 볼 스위치이고, 그때는 아무도 이 세 가지를 의심하지 않을 것이다.
그 사실을 검사로 못 박아 뒀다(`test_who_actually_turns_the_cache_on`):
배치가 `cached=True`를 켜면 그 검사가 깨지면서 이 파일을 읽게 된다.

### ㊹ **"비었는가"는 프레임을 비우는 코드 **뒤에서** 물어야 한다**

주식 제공자는 소스 3개를, 코인 제공자는 거래소 5개를 순서대로 시도한다.
소스 하나가 흔들려도 그날 기록이 안 비게 하려고 만든 구조다.

그 구조가 통째로 무력화되는 구멍이 있었다(감사 163).

    df = fetch(...)
    if df is None or df.empty:                       # ← 소스가 준 원본만 본다
        raise ValueError("빈 결과")
    out = self._drop_unclosed(self._validate(df))    # ← 여기서 비워질 수 있다
    out.attrs["source"] = name
    return out                                       # 0봉인데 '성공'

`_validate`의 dropna와 `_drop_unclosed`의 미완결 봉 제거가 둘 다 프레임을
비울 수 있는데, 검사는 그 **앞에** 있었다. 코인 쪽엔 아예 없었다.

0봉이 '성공'으로 반환되면 세 가지가 동시에 일어난다.

    · 보조 소스·보조 거래소를 건너뛴다 (3중 체인의 존재 이유가 사라진다)
    · `synthetic_fallback` 표식이 안 붙는다 → 가짜 데이터 경보도 안 울린다
    · `attrs["source"]`엔 "yfinance"라고 적힌다 → 장부에 그렇게 남는다

규칙: **폴백 체인에서 '실패'의 정의는 반환 직전에 판정한다.** 중간
변환(정규화·필터·검증)이 결과를 무의미하게 만들 수 있으면, 그 변환들이
다 끝난 자리에서 한 번 더 묻는다. 앞에서 한 번 물었다고 답이 그대로일
거라 믿으면 안 된다 — 사이에 있는 코드가 답을 바꾼다.

### ㊺ **"없는 값"과 "못 쓰는 봉"은 다르다 — dropna는 그 둘을 구별 못 한다**

`_validate`가 `df.dropna()`로 끝나고 있었다. 다섯 컬럼(OHLCV) 중 하나라도
비면 그 봉을 통째로 지운다는 뜻이다.

그런데 **거래량이 원래 없는 계열이 있다.** 지수(^VIX·^VIX3M·^TNX)와
환율(KRW=X)이 그렇고, 이 시스템은 그 넷을 크로스에셋 피처로 전부 받는다.

실측(거래량만 없는 120봉 지수 계열):

    _validate 통과 후 0봉 · source="yfinance" · 합성 폴백 아님

가격은 120봉 전부 멀쩡했다. 한 봉만 결측이어도 같은 병이다 — 시계열에
구멍이 나고, 그 구멍을 건너뛴 이틀치 수익률이 **하루치인 척** 라벨과
사이징에 들어간다.

규칙: `dropna()`를 통째로 쓰지 말고 `subset=`으로 **없으면 못 쓰는 컬럼**만
지정한다. 여기서는 가격 넷이다. 거래량은 없어도 봉은 유효하다.

없는 값을 0으로 채우는 것도 답이 아니다 — '거래량 0(거래정지)'과
'거래량 미상(지수)'이 구별되지 않게 된다. NaN으로 남기고, 그 값을 쓰는
쪽이 각자 견디게 한다. 이 저장소는 이미 다 견디고 있었다:

    유동성 필터   : dollar_vol NaN → 진입 보류(보수적)
    시장충격 비용 : isfinite 검사에 걸려 0 (컬럼 없을 때와 동일)
    ML 피처       : vol_z가 fillna(0.0)로 흡수

대신 **보고서에는 보이게** 했다. 안 보이면 '결측이 없다'와 구별이 안 된다.

### ㊻ **부동소수에서 `floor(a / b)`는 "정확히 나누어떨어질 때" 틀린다**

주문 수량을 거래소 단위(lot)에 맞추는 코드였다.

    qty = math.floor(qty / self.qty_step) * self.qty_step
    qty = round(qty, 12)          # ← 이미 깎인 뒤라 아무 소용이 없다

    0.3 / 0.1 == 2.9999999999999996   →  floor 2  →  0.2

**정확히 배수인 수량이 한 칸 깎인다.** 실측(감사 164):

    스텝 0.1     0.3 → 0.2 (33% 덜)   0.7 → 0.6 (14%)   8.2 → 8.1 (1.2%)
    스텝 0.0001  0.0003 → 0.0002 (33%)

제일 아픈 곳은 **청산**이다. 0.3개를 전부 팔라고 해도 0.2개만 나가고 0.1개가
남는다. 손절·킬스위치가 "정리했다"고 말한 뒤에도 포지션이 살아 있다.

이 병의 특징: **작은 수량일수록 비율 손실이 크다.** 8.2는 1.2%지만 0.3은
33%다. 8만원으로 시작하는 계좌는 늘 작은 수량 쪽에 있다.

규칙: 나눈 몫을 내림하기 전에 **눈금에 붙었는지 먼저 본다.**

    n = qty / step
    nearest = round(n)
    if math.isclose(n, nearest, rel_tol=1e-9, abs_tol=1e-9):
        n = nearest
    qty = math.floor(n) * step

내림 자체는 유지한다 — 있는 돈보다 많이 주문하면 안 되기 때문이다. 고치는
것은 "내림"이 아니라 "나눗셈이 만든 가짜 미달"이다.

같은 병이 부등호에도 있다. `qty * price < min_notional`은 딱 최소금액인
주문을 걸러낸다(0.29 * 100.0 == 28.999999999999996 < 29.0). 경계 비교에는
아주 작은 여유를 준다.

일반화: **부동소수로 "딱 맞는가"를 물으면 안 된다.** `==`뿐 아니라
`floor`·`<`·`%` 전부 같은 함정이다. 눈금·경계·나머지를 다루는 코드는
전부 허용오차를 끼고 써야 한다.

### ㊼ **같은 칸이 상대에 따라 다른 뜻이면, 값만 보고 알아맞히지 말 것**

ccxt의 `precision.amount`는 거래소 모드에 따라 뜻이 다르다.

    TICK_SIZE      : 값이 곧 단위   0.001 · 1.0 · 10.0
    DECIMAL_PLACES : 값이 소수 자릿수  3 → 0.001

우리 코드는 모드를 안 받고 값만 보고 추측했다.

    if isinstance(prec, float) and 0 < prec < 1:   ← 1.0 이상이면
        return prec
    return 0.0                                     ← '제약 없음'

그래서 **단위가 1 이상인 시장이 조용히 무제약이 됐다.** 하필 그쪽이
라운딩이 제일 중요한 시장이다 — 정수 단위로만 살 수 있는 종목에 3.7개를
주문하면 거절당한다. 그리고 주요 거래소는 **전부** TICK_SIZE였다
(binance·okx·bybit·upbit·coinbase, ccxt 4.5.70에서 확인).

추측이 위험한 이유는 틀려서가 아니라 **틀린 방향이 '제약 없음'이어서**다.
모르면 막는 게 아니라 푸는 쪽으로 넘어졌다.

규칙 둘.
1. 뜻이 상대에 달린 값은 **뜻을 함께 받아 온다.** 여기서는 거래소의
   `precisionMode`를 같이 넘기게 했다. 추측 경로는 남기되 최후수단으로만.
2. 그래도 모를 때의 기본값은 **안전한 쪽**이어야 한다. '제약 없음'이
   기본값이면, 모르는 순간 보호장치가 통째로 꺼진다(㉞와 같은 계열).

### ㊽ **z-점수는 규모를 지운다 — 그래서 잡음 열이 진짜 신호와 동급이 된다**

종목 선별기가 팩터(PER·PBR·ROE)를 z-점수로 표준화해 합산한다. 표준화는
단위가 다른 팩터를 섞기 위한 것인데, **부작용으로 "얼마나 다른가"까지
지워 버린다.**

실측(감사 165): 종목 간 PER 차이가 2e-7인 열 — 사실상 같은 값 — 인데
z는 ±1.22로 나온다. ROE가 0.05→0.35로 뚜렷하게 벌어진 열과 **정확히 같은
세기**다. 그 의미 없는 열 하나를 더하면 상위 2종목이 [C, B]에서 [C, A]로
바뀐다. B가 빠지고 A가 들어온 이유가 2e-7짜리 PER 차이뿐이다.

`std != 0`만으로는 못 막는다 — 0이 아니라고 '차이가 있다'는 뜻이 아니다.
감사 159가 만든 판정(계열의 크기 대비 표준편차)을 그대로 쓴다.

    if not np.isfinite(std) or degenerate_spread(std, float(col.abs().mean())):
        continue        # 이 팩터는 아무것도 못 말한다

일반화: **표준화(z·랭크·백분위)는 신호와 잡음을 같은 크기로 만든다.**
표준화 전에 "이 열이 말할 게 있는가"를 먼저 물어야 한다.

### ㊾ **결측을 0으로 채우면 '자료 없음'이 '딱 평균'이 된다**

같은 선별기에서:

    score = score.add((z * direction).fillna(0.0), fill_value=0.0)
                                     ^^^^^^^^^^^ 결측 → 0 = z점수의 한가운데

z-점수에서 0은 "없음"이 아니라 **"정확히 평균"**이다. 그래서 자료가 없는
칸이 조용히 '평범한 성적'으로 채점됐다.

실측: PER·PBR·ROE 세 팩터 중 **ROE 하나만 있는 종목**이, 셋 다 좋은 종목과
나란히 상위 2종목에 뽑혔다. 나머지 둘을 평균으로 받았기 때문이다.

방향이 나쁘다. **자료가 부실할수록 극단값 하나로 올라오기 쉬워진다** —
정보가 적은 종목이 상을 받는다. 팩터를 셋 보자고 정해 놓고 실제로는
하나만 보고 산 셈이다.

고침 둘.
1. 합이 아니라 **있는 팩터의 평균**으로 점수를 낸다(팩터 수가 다른
   종목끼리 비교 가능해진다).
2. **최소 보유 개수**를 둔다 — 요청한 팩터의 절반은 있어야 후보로 본다.

일반화: 결측의 기본값은 그 척도에서 **'중립'이 아니라 '불이익'**이거나,
아예 **후보에서 빼는 것**이어야 한다. 중립으로 채우면 모르는 것이 공짜로
평균 점수를 받는다(㉞·㊼와 같은 계열 — 모르는 쪽이 유리해지는 기본값).

곁들여 같은 파일에서 고친 것: 동점을 입력 순서로 갈랐다. 후보 목록에 적은
순서만 바꿔도 다른 종목을 샀다. 점수가 같으면 **종목명**으로 가른다 —
재현할 수 없는 선택은 검증할 수도 없다(감사 147과 같은 원리).

### ㊿+① **리샘플의 '라벨 위치'는 규칙마다 다르다 — 맡기면 반대로 틀린다**

상위 시간봉 게이트가 이렇게 돼 있었다.

    htf = close.resample(self.htf).last()
    up  = (htf > htf.rolling(w).mean()).shift(1)      # 완성된 봉만 쓰자
    return up.reindex(close.index, method="ffill")

의도는 옳았다. 그런데 pandas의 **기본 라벨 위치가 규칙마다 다르다.**

    'W' · 'ME'    →  label='right'   봉이 **끝나는** 시각에 라벨
    '4h' 등 일중  →  label='left'    봉이 **시작하는** 시각에 라벨

그래서 같은 코드가 규칙에 따라 **반대 방향으로** 틀렸다(감사 166).

    주봉  : 오른쪽 라벨 = 이미 완성 시각인데 shift를 더함
            → 상위 봉을 하나 더 기다린다 (폭등 7일 뒤가 아니라 13일 뒤)
    일중  : 00:00~03:00 봉을 '00:00'이라 부름. 그 종가는 03:00에야 정해진다
            → **미래를 본다** (02:00 폭등인데 00:00에 게이트가 열린다)

늦는 쪽은 돈을 놓치고, 이른 쪽은 백테스트 성적을 공짜로 올려 준다.

고침은 shift를 더 얹는 게 아니라 **라벨 위치를 명시**하는 것이다.

    close.resample(rule, label="right", closed="right").last()
    # 라벨 시각 = 완성 시각 → ffill이 저절로 '완성된 마지막 봉'을 집는다
    # 진행 중인 봉은 라벨이 미래라 ffill이 닿지 못한다 → 룩어헤드 구조적 불가

일반화: **시간을 다루는 API의 기본값을 믿지 말 것.** 특히 "구간에 붙는
이름이 시작인가 끝인가"는 라이브러리마다·규칙마다 다르고, 틀려도 조용하다.
그리고 룩어헤드는 `shift`를 더해서 막는 게 아니라 **"이 값이 알려지는 시각"을
인덱스로 삼아** 구조적으로 막는 것이 안전하다.

곁들여 — 검사를 고르다 한 번 헛돌았다. 폭등을 4h 봉 **경계**에 맞춰 놨더니
두 라벨 방식의 답이 우연히 같아서 아무것도 안 잡혔다. 경계에서 갈리는
버그는 **경계가 아닌 곳**에 자극을 줘야 드러난다(㉚과 같은 계열).

### ㊿+② **NaN은 부등호를 전부 빠져나간다 — 그래서 '조용히 매도'가 된다**

주문을 만드는 마지막 함수에 이런 두 줄이 있었다(감사 167).

    if price <= 0:
        return None                              # NaN <= 0  → False
    ...
    side = "buy" if delta > 0 else "sell"        # NaN > 0    → False

NaN은 **모든** 비교에서 False다. 그래서 첫 줄을 그냥 통과하고, 마지막 줄에서
`else` 가지로 떨어진다. 결과:

    price  = NaN  →  ('sell', nan, nan)
    equity = inf  →  ('buy',  inf)

방향이 최악이다. 값이 망가지면 시스템이 **청산 쪽으로 기운다.** "이상하면
아무것도 안 한다"가 아니라 "이상하면 전부 판다"가 기본 동작이었다.

규칙 셋.

1. `x <= 0`으로 "나쁜 값"을 거르지 말 것. **`math.isfinite(x)`를 먼저** 묻는다.
   범위 검사는 유한성 검사를 대신하지 못한다.
2. `if A else B` 형태로 방향을 정할 때, **NaN이 어느 가지로 가는지** 확인한다.
   대개 `else` — 즉 작성자가 "기본"이라 여긴 쪽이 아니라 **안 쓴 쪽**이다.
3. 관문은 **입력**과 **결과** 둘 다 본다. 입력이 전부 유한해도 나눗셈이
   넘칠 수 있다(`1e308 / 1e-300`).

그리고 이 저장소가 실제로 겪은 교훈: `safe_amount`가 바로 그 파일 맨 위에
있었다. 독스트링에 "'inf 수량 주문'을 막는다"라고 적혀 있었다. 어댑터들은
잔고·체결을 읽을 때 성실히 통과시켰는데, **그 값들이 모여 주문이 되는
자리에서는 아무도 안 물었다.** 부품을 만들었으면 **가장 중요한 한 곳**에
배선됐는지부터 확인할 것(㉑·감사 135와 같은 계열).

곁들여 — 검사를 짜다 또 헛돌았다. 보유 0으로 검사했더니 `price=inf`가
'목표수량 0 → delta 0'이 되어 **다른 가드가 대신** 잡았고, 정작 입력 가드를
지워도 통과했다. 보유가 있으면 `price=inf`는 **전량 매도(수량 50, 유한!)**가
되어 수량 가드로는 절대 못 잡는다. 가드가 둘이면 **서로를 가리지 않는
사례**로 각각 검사해야 한다.

### ㊿+③ **채점할 수 없는 표본을 오답으로 세지 말 것**

적중률 계산이 이랬다(감사 168).

    active  = (sig.abs() > 1e-9) & ret_next.notna()
    correct = active & (np.sign(sig) == np.sign(ret_next))

`np.sign(0.0)`은 0이다. 그래서 다음 봉이 **정확히 보합**이면 롱이든 숏이든
`sign(sig) != 0` — 무조건 '틀림'이 된다. 그 봉은 애초에 **맞출 방향이 없던**
봉인데 오답으로 분모·분자에 들어갔다.

실측(롱 고정, 상승 6 · 하락 2 · 보합 2): **60.0%** vs 방향이 있던 봉만 **75.0%**.

두 가지를 배웠다.

1. **"우리에게 불리한 오류"도 오류다.** 성적을 부풀리지 않았으니 괜찮다고
   넘기기 쉽다. 하지만 틀린 숫자는 판단을 흐리고, 언젠가 반대 방향으로
   똑같이 틀린다.
2. **더 나쁜 것은 편향이 표본마다 다르다는 점이다.** 보합 빈도는 종목마다
   다르다(거래정지·상하한가·저유동 국내주식 ≫ 코인). 그래서 같은 실력이어도
   종목에 따라 점수가 달라지고, **종목 간 비교가 조용히 무너진다.** 절대값이
   조금 낮은 것보다 이쪽이 훨씬 위험하다.

규칙: 비율 지표를 만들 때 분모에 **"판정 불가"를 섞지 않는다.** 판정 불가는
성공도 실패도 아니다. 빼되 **몇 개를 뺐는지 함께 돌려준다** — 안 보이면
"판정 불가가 없었다"와 구별되지 않는다(㉜와 같은 계열).

그리고 지표의 **정의**를 바꾸면 그 전후 숫자는 비교할 수 없다. 과거 기록은
고치지 않고, 기준이 바뀐 사실을 공개 페이지에 적었다.

곁들여: 이동 창(rolling) 검사를 짜다 창 경계에 걸렸다. 창이 전 구간보다
짧으면 봉 하나가 빠져 값이 달라진다(5/7 = 71.4%). **"두 계산의 규칙이
같은가"를 볼 때는 창이 전 구간을 덮게 해야** 규칙 차이와 경계 차이가
섞이지 않는다.

### ㊿+④ **"자극을 주면 반응한다"만 검사하면 규칙을 뒤집어도 통과한다**

전략 8종의 계약을 고정하다 변이를 **세 번** 놓쳤다(감사 169). 패턴이 전부
같았다.

    평균회귀: "하단 밴드에서 산다"만 검사   → 조건을 `<` → `>` 로 뒤집어도 통과
    켈트너  : "상단 돌파하면 산다"만 검사   → 뒤집어도 그 봉에선 어차피 1
    MACD    : "내리면 숏"이라 가정          → 애초에 규칙을 잘못 알고 있었다

이유는 하나다. **자극이 있는 자료에서는 맞는 코드와 틀린 코드가 같은 답을
낸다.** 뒤집힌 평균회귀는 "밴드 안이면 계속 롱"이 되는데, 급락 봉에서는
정상 코드도 롱이라 구별이 안 된다.

규칙 셋.

1. **자극이 없는 자료에서 '아무것도 안 하는가'를 함께 본다.** 방향을
   고정하는 것은 이쪽이다. 평균회귀는 "2σ를 한 번도 안 벗어난 완만한
   드리프트에서 전 구간 무포지션", 켈트너는 "밴드를 안 뚫은 조용한 구간에서
   전 구간 무포지션".
2. **마지막 봉만 보지 말 것.** 뒤집힌 코드도 진입·청산이 번갈아 일어나
   마지막 봉이 우연히 0일 수 있다. 구간 전체(`abs().max()`)를 본다.
3. **규칙을 '가격의 방향'으로 번역하지 말 것.** MACD의 규칙은 "내리면 숏"이
   아니라 "macd선 < 신호선"이다. 지수적 하락에서는 두 EMA의 차이 자체가
   줄어들어 macd선이 신호선 **위**로 올라간다(실측 -1.9165 vs -1.9972) —
   규칙상 롱이 맞다. 검사는 **정의 그대로** 쓴다. 내 직관을 정답으로 삼으면
   코드가 아니라 검사가 틀린다.

### ㊿+⑤ **변조와 복원이 같은 초에 일어나고 글자 수가 같으면, 파이썬은 변조된 .pyc를 계속 쓴다**

이 저장소가 감사 72에서 이미 겪고 `_purge_bytecode`로 막아 둔 함정인데,
내가 손으로 재현 스크립트를 돌리다 **똑같이 걸렸다**(감사 169).

`if c[i] < lo[i]` → `if c[i] > lo[i]` 는 **글자 수가 같다.** 복원을 `cp`로
같은 초 안에 하면 (mtime, 크기)가 .pyc 기록과 일치해 파이썬이 캐시를
그대로 재사용한다. 디스크의 소스는 멀쩡한데 **실행되는 것은 변이본**이다.

증상이 지독하다 — 소스를 몇 번을 읽어도 맞는데 결과가 틀리다. 나는
`inspect.getsource`까지 찍어 보고도 30분을 헤맸다. 결정적 단서는
**같은 코드를 파일이 아니라 메모리에서 exec하면 답이 달라진다**는 것이었다.

규칙: 소스를 프로그램으로 바꿔 치는 도구(변이 시험·핫픽스 실험)는
**바꿀 때와 되돌릴 때 모두 `.pyc`를 지운다.** 손으로 실험할 때도 마찬가지다.
"소스는 맞는데 동작이 틀리다"면 가장 먼저 `__pycache__`를 의심할 것.

### ㊿+⑥ **"같은 곳인가"를 호스트로만 판단하면 스킴 강등을 놓친다**

HTTP 리다이렉트에서 인증 헤더를 뗄지 판단하는 코드였다(감사 170).

    same_host = (urlsplit(req.full_url).netloc == urlsplit(newurl).netloc)
    if not same_host:
        ...민감 헤더 제거...

`netloc`은 **호스트:포트**다. 스킴이 안 들어간다. 그래서 같은 호스트로의
**https → http** 리다이렉트가 "같은 곳이니 괜찮다"로 통과했다.

    실측  https://api.example.com/v1 → http://api.example.com/v2
          Authorization·X-Api-Key 유지 → **평문 전송**

여기 실려 나가는 것은 실거래 자격증명이다(알파카 APCA 키, KIS
appkey/appsecret, 웹훅 토큰). 상대 서버의 302 한 번이면 도청 가능한 채널로
키가 나간다.

규칙: **"목적지가 같은가"는 (스킴, 호스트, 포트)로 판단한다.** 셋 중 하나라도
다르면 자격증명을 떼는 쪽이 기본이다. 예외는 명백히 안전해지는 방향
(http → https 승격)뿐이다.

일반화: 보안 판단을 **URL의 일부**로 하면 빠뜨린 부분이 곧 구멍이 된다.
`netloc`, `hostname`, `path` 같은 부분 속성은 각자 무엇을 **안 담는지**를
먼저 확인하고 쓸 것.

### ㊿+⑦ **가릴 통로가 둘이면 둘 다 가려야 한다 — 하나만 가리면 '가렸다는 착각'이 남는다**

같은 파일에서(감사 170):

    detail = exc.read().decode(...)[:2000]                  # 서버가 준 본문
    raise HttpError(f"HTTP {code} {_redact_url(url)}: {detail}")
                                    ^^^^^^^^^^^^ URL만 가렸다

API 오류 응답은 요청을 되울리는 일이 흔하다 —
`{"error":"Invalid API key","request":"...?apikey=SUPERSECRET123"}`.
왼쪽 칸에서 애써 가린 키가 **바로 오른쪽 칸으로** 새어 로그에 남는다.

이런 반쪽 방어가 특히 나쁜 이유는, 모듈 머리말에 "예외 메시지가 로그로
새어도 키가 노출되지 않도록 방어한다"라고 **적혀 있었다**는 점이다. 읽는
사람은 방어된 줄 안다. 지켜지지 않은 약속은 없는 약속보다 위험하다(㊾와
같은 계열).

규칙: 비밀이 흘러나갈 수 있는 통로를 **세어 보고** 전부에 같은 필터를 건다.
로그 한 줄에 들어가는 조각이 둘이면 둘 다, 셋이면 셋 다.

### ㊿+⑧ **코드를 망가뜨렸다 되돌리는 도구는, 죽어도 되돌려야 한다**

변이 시험 도구는 운영 코드를 실제로 망가뜨렸다가 `finally`로 되돌린다.
그런데 `finally`는 **파이썬이 예외를 처리할 때만** 돈다. 바깥에서 SIGTERM으로
죽이면(타임아웃·CI 취소) 되돌리지 못하고 **변조된 채로 디스크에 남는다.**

2026-08-12 감사 171에서 실제로 그랬다. 내가 `timeout 110`으로 감싸 돌렸고,
프로세스가 잡히면서 `quant/portfolio/screener.py`가 `if r:` 가드가 빠진
상태로 작업 트리에 남았다. 그대로 커밋됐으면 **재무를 못 받은 종목이
후보에 섞이는 코드**가 배포된다.

무서운 점은 증상이 없다는 것이다. 테스트는 초록이고(그 변이는 잡히지
않는 축이었다), git status를 안 보면 모른다. 나는 "왜 원본 문자열이 0회지?"
라는 엉뚱한 신호로 알아챘다.

규칙 셋.

1. **정리(cleanup)를 `finally`에만 맡기지 않는다.** 시그널 핸들러(SIGTERM·
   SIGINT·SIGHUP)와 `atexit`을 함께 건다. 세 경로 중 하나는 거의 항상 돈다.
2. **무엇을 망가뜨렸는지 전역에 적어 둔다.** 핸들러는 지역 변수를 못 본다.
3. **되돌린 뒤 그 사실을 말한다.** 조용히 되돌리면 다음 사람이 같은 함정에
   또 걸린다.

일반화: "잠깐 바꿨다가 되돌린다"는 패턴(변이 시험, 설정 임시 변경, 파일
잠금)은 전부 이 위험을 안고 있다. **되돌리기가 실패할 수 있는 경로를
먼저 세어 볼 것.**

### ㊿+⑨ **"살아 있나" 확인에 본문을 해석하지 말 것 — 성공 경로가 실패 경로가 된다**

SNS 게시 직전, 카드 이미지가 공개됐는지 확인하는 코드였다(감사 172).

    fetch = http_get or (lambda u: get_text(u, timeout=20))
    try:
        fetch(u); ok = True
    except RuntimeError:            # ← 여기로 안 온다
        sleep(delay)

여기 오는 URL은 **PNG**다. `get_text`는 본문을 UTF-8로 디코드하므로 PNG의
첫 바이트 `0x89`에서 `UnicodeDecodeError`가 난다. 그건 `ValueError`지
`RuntimeError`가 아니라, 재시도 그물을 그대로 빠져나간다.

방향이 완전히 거꾸로였다.

    이미지가 아직 없다(404) → HttpError(RuntimeError) → 잡힘 → 재시도  ✅
    이미지가 정상 공개됨(200) → UnicodeDecodeError → **게시 전체 실패**  ❌

**성공하면 죽는다.** 이런 버그는 "가끔 실패한다"가 아니라 "제대로 되면
실패한다"라서, 로그만 보면 원인이 정반대로 읽힌다.

규칙 둘.
1. **존재 확인은 상태코드로 한다.** 본문을 파싱·디코드하는 함수를 헬스체크에
   재사용하지 말 것. 확인용 함수는 예외 대신 True/False를 돌려주게 만든다.
2. **재시도 `except`는 실제로 날아오는 예외 종류를 세어 보고 정한다.**
   `except RuntimeError`처럼 좁게 잡으면, 예상 못 한 한 종류가 그물을
   빠져나가 재시도 로직 전체를 무의미하게 만든다.

### ㊿+⑩ **검사에서 늘 꺼 두는 단계가 있으면, 거기가 사각지대다**

위 결함이 오래 살아남은 이유는 단순하다. **그 단계를 켜고 도는 검사가 하나도
없었다.** SNS 게시 검사 8건이 전부 이렇게 부른다.

    run(..., wait_public=False)

파이프라인 자체는 촘촘히 검사돼 있었다 — 컨테이너 생성, 폴링, 멱등 마커,
플랫폼별 실패 격리까지. 하필 **실전에서만 켜지는 한 단계**가 통째로 그물
밖이었다.

`wait_public=False`는 검사에서 합리적인 선택이다(네트워크를 안 타려고). 문제는
그렇게 꺼 둔 뒤 **켠 상태를 보는 검사를 따로 안 만든 것**이다.

규칙: 검사에서 어떤 스위치를 반복적으로 끄고 있다면, 그 스위치를 **켠
경로만 보는 검사**를 하나 따로 만든다. 끄는 이유(느리다·네트워크)는
그 경로를 가짜로 대체할 이유이지, 안 봐도 될 이유가 아니다.

같은 눈으로 볼 것: `if os.getenv(...)`로 감싼 코드, `dry_run=True` 기본값,
`if not TESTING:` 분기 — 전부 "검사는 절대 안 지나가는 길"이다(㉜와 같은 계열).

### ㊿+⑪ **`pd.NA`는 float 파이프라인을 object로 바꿔 놓고 조용히 터진다**

KRX 수급 z점수 계산이었다(감사 173).

    sd = s5.rolling(60).std().replace(0.0, pd.NA)     # ← 여기
    z  = ((s5 - mu) / sd).astype(float).clip(-4.0, 4.0)

`replace(0.0, pd.NA)`는 계열을 **object dtype**으로 바꾼다. 그러면 다음 줄의
`.astype(float)`가 터진다.

    TypeError: float() argument must be a string or a real number, not 'NAType'

그리고 그 예외를 바깥의 `except Exception`이 삼킨다. 결과는 **수급 피처 두 개가
통째로 사라지는 것** — 표준편차가 0이던 그 구간만이 아니라, **그 종목의 그날
전체**가 없어진다. 로그 한 줄 말고는 흔적이 없다.

언제 표준편차가 정확히 0이 되나: 5일 합이 60일 내내 같을 때. 수급이 0으로만
기록되는 저유동 종목, 장기 거래정지, 결측을 0으로 채우는 API. 흔치 않지만
**하필 그런 종목에서 맥락 피처가 조용히 사라지는 것**이 제일 나쁘다.

고침은 한 글자다 — `np.nan`. 이건 float라 dtype이 안 바뀐다.

형제 찾기 결과가 이 결함의 성격을 말해 준다: 저장소 전체에서 `pd.NA`를 쓰는
곳은 **여기 하나뿐**이었다. 나머지 10곳(`stochastic`·`ensemble`·`adx`·`rsi`·
`accuracy`·`allocation` …)은 전부 `np.nan`이다. **혼자만 다른 코드**가 오래
살아남는 이유는, 다른 곳과 달라서 눈에 띄지 않기 때문이 아니라 **터지는
조건이 드물어서**다.

규칙: 숫자 계열에 결측을 넣을 때는 **그 계열의 dtype을 유지하는 값**을 쓴다.
`pd.NA`는 nullable 확장 dtype(Int64·boolean)용이고, float64 계열에는
`np.nan`이다. 그리고 `except Exception`으로 감싼 블록 안에서 dtype이 바뀌면
**실패가 조용해진다** — 감싸기 전에 안에서 무엇이 터질 수 있는지 세어 볼 것.

### ㊿+⑫ **곁가지 기록이 본류를 죽이면 안 된다 — 특히 '돈이 움직인 뒤'라면**

`record_order`는 주문을 **보낸 뒤에** 불리는 감사 로그다. 독스트링에는
"실패해도 예외를 삼킨다"라고 적혀 있었는데, 실제로는 한 종류만 삼켰다.

    except OSError:
        pass

직렬화 실패 두 가지가 그대로 새어 나갔다(감사 174).

    datetime 키 등 직렬화 불가 키 → TypeError
    순환 참조                     → ValueError

여기서 죽으면 **돈은 이미 움직였는데 배치가 그 자리에서 멈춘다.** 남은
종목은 처리되지 않고, 방금 낸 주문은 장부에도 안 남는다. 실패 타이밍이
최악이다 — 되돌릴 수도, 기록할 수도 없는 지점.

규칙 셋.

1. **부수효과(로그·알림·지표 전송)를 감쌀 때는 `except Exception`이 맞다.**
   좁게 잡는 것이 미덕인 자리와 아닌 자리가 있다. 판단 기준은 "여기서 죽으면
   본류가 어떻게 되는가"다.
2. **삼키되 숨기지 않는다.** `pass` 대신 경고를 남긴다. 안 그러면 기록이
   비어 있는 이유를 나중에 알 수 없다.
3. **약속(독스트링)과 코드가 어긋나면 그 자체가 결함이다.** 읽는 사람은
   "삼킨다"를 보고 안전하다고 믿는다(㊾와 같은 계열).

곁들여 같은 파일에서 **죽은 손잡이**를 하나 지웠다. `build_review`가
`periods_per_year`를 받아 놓고 본문에서 한 번도 안 썼는데, CLI는 시장에 맞춰
(코인 365 / 주식 252) 그 값을 성실히 계산해 넘기고 있었다. 고르면 뭔가
달라질 것처럼 보이지만 아무 일도 안 난다 — 있는 척하는 손잡이는 없는 것보다
나쁘다(감사 135와 같은 계열).

### ㊿+⑬ **"보냈다"와 "닿았다"는 다르다 — 보낼 곳이 없으면 성공이 아니다**

`flag_watch`는 새 경보(킬스위치 발동·과최적화 의심)를 알리고, **성공한 것만**
장부에 '보냈다'로 적는다. 적힌 플래그는 다음 실행부터 '이미 켜져 있던 것'으로
분류돼 다시 오지 않는다. 그래서 반환값 하나가 "이 경보가 사장님께 닿았는가"를
결정한다.

`notifications.py` 머리말은 그 위험을 이미 알고 있었다 — "실패를 성공으로
적으면 그 경보는 영원히 다시 오지 않는다". 그런데 **문이 하나 더 있었다.**

    get_notifier()  →  MultiNotifier([ConsoleNotifier()])   # 시크릿 미설정 시
    ConsoleNotifier.send()  →  항상 True

텔레그램·슬랙·디스코드를 하나도 설정하지 않으면, 콘솔만 남고 콘솔은 언제나
성공한다. 킬스위치가 발동한 날 그 사실이 **깃허브 액션 로그 안에 한 줄 남고
장부에는 '전달됨'으로 적혀 영구히 사라진다.**

핵심은 이것이다. **"전송 실패"는 막았는데 "전송 대상 없음"은 안 막았다.**
같은 결과(아무도 못 받음)로 가는 두 번째 길을 안 본 것이다.

규칙 셋.
1. 전달 성공을 기록하는 코드는 **"누가 받았는가"**를 물어야 한다. 로그·콘솔·
   파일 기록은 '보관'이지 '전달'이 아니다. 채널마다 그 구분을 표시해 둔다.
2. **설정이 비어 있는 상태를 성공으로 반올림하지 말 것.** 0개 채널에 보내면
   0명이 받는다. `all([])`이 True인 것과 같은 함정이다.
3. 그리고 **조용히 실패하지 말 것** — 왜 경보가 안 오는지 알려면 "보낼 곳이
   없다"가 로그에 있어야 한다.

이 결함의 좋은 점은 고치면 저절로 복구된다는 것이다. 전달로 안 치므로 장부에
안 적히고, 나중에 채널을 설정하는 순간 **밀린 경보가 다시 뜬다.**

### ㊿+⑭ **아직 안 판 포지션은 성과가 아니다 — 평가손익이 실현통계에 섞이면 지표가 자기 자신을 칭찬한다**

`extract_trades`가 자료 끝까지 들고 있던 포지션도 한 건의 '완결 거래'로 세고,
그 **평가손익**을 승률·기대손익·이익팩터에 넣었다(감사 176).

코드는 알고 있었다 — `exit_eq = ... else eq[end]  # 데이터 끝이면 미실현`.
**아는데 결과에 안 적었다.** 쓰는 쪽은 그냥 거래 한 건으로 읽는다.

방향이 나쁘다. 상승장에서는 들고 있는 포지션이 대개 이익 중이라 승률·기대손익이
**위로** 부풀고, 하락장에서는 눌린다. 즉 **"지금 잘 되고 있으면 과거 성적도 좋아
보인다".** 성과 지표가 현재 포지션의 평가손익에 따라 흔들리면 그건 성과 지표가
아니라 기분이다.

실측(손실로 청산 1건 + 이익 중 미청산 1건): 승률 50% → **0%**.

거래 수가 적을 때 특히 아프다. 이 저장소는 "30건 미만은 잡음"이라고 스스로
경고하는데, 그 적은 표본에 미실현 한 건이 섞이면 비중이 크다.

규칙: **"끝난 것"과 "진행 중인 것"을 같은 통에 넣지 않는다.** 실현통계는
청산된 것만으로 낸다. 진행 중인 것은 **개수만 따로** 알린다(감사 168의
`n_flat`, 감사 173의 소스 결측과 같은 원리 — 빼되 숨기지 않는다).

일반화: 시계열의 **마지막 구간**은 거의 항상 '진행 중'이다. 롤링 통계·구간
집계·세션 분석 — 마지막 조각이 완결된 것처럼 섞이지 않는지 늘 확인할 것.
경계는 끝에서 틀린다.

### ㊿+⑮ **검사에서 로직을 '옮겨 적으면' 그 코드는 검사되지 않는다**

감사 177에서 변이를 두 건 놓쳤고, 둘 다 검사를 잘못 쓴 탓이었다.

**① 로직 재현.** `grid_search`의 방향 보정(`cmp = -score if lower_better`)을
검사에 손으로 옮겨 적고 그걸 확인했다. 그 줄을 지우는 변이가 그대로 통과했다 —
나는 **내 사본**을 검사했지 코드를 검사한 게 아니었다. 진짜 함수를 부르니
바로 잡혔다.

    ❌ def _pick(obj, scores): cmp = -s if obj in LOWER_IS_BETTER else s ...
    ✅ grid_search(df, Strategy, grid, objective="volatility")["best_score"]

느리다는 이유로 재현하고 싶어지는데, 그러면 **검사가 코드와 함께 틀린다.**
느린 게 문제면 자료를 줄이지 로직을 옮기지 않는다.

**② 한 칸만 보기.** 히트맵은 ★(최적 한 칸)와 색(모든 칸) 두 가지로 말한다.
★만 검사했더니 **색 방향을 뒤집는 변이**가 통과했다. ★는 제자리인데 주변이
전부 반대로 칠해져, 눈으로는 정반대로 읽히는 그림이 된다.

규칙: **산출물이 여러 통로로 말하면 통로마다 검사한다.** 표식·색·순서·문구는
각각 따로 틀릴 수 있다.

곁들여 이 감사에서 확인만 하고 끝난 것(결함 없음): `LOWER_IS_BETTER`를
grid와 heatmap이 **같은 출처**에서 가져온다. `max_drawdown`이 집합에 없는
근거(음수로 저장돼 "클수록 좋다")도 실측 -0.3333로 확인했다 — 양수 크기로
저장되기 시작하면 탐색이 **가장 아팠던 조합**을 최적으로 고르므로, 그 사실
자체를 검사로 못 박아 뒀다.

### ㊿+⑯ **방어를 만들었으면 '가장 많이 쓰는 길'이 그 앞을 지나는지 확인할 것**

`quant/utils/http.py`는 바깥에서 오는 것을 받는 공용 문이고, 방어가 세 겹
걸려 있다 — 응답 크기 상한 32MB, 리다이렉트 인증헤더 제거, 오류 메시지의
비밀 가리기.

그런데 **바깥 데이터를 가장 많이 받는 세 경로**가 그 문을 안 지났다(감사 178).

    sentiment.py  공포탐욕지수   urlopen + resp.read()
    stock.py      야후 chart    urlopen + json.load(resp)
    stock.py      stooq CSV     urlopen + resp.read()

셋 다 본문을 **상한 없이** 통째로 읽는다. 실측(가짜 57MB 응답): 예전에는
그대로 메모리에 올렸고, 지금은 "응답이 너무 큽니다"로 안전하게 폴백한다.

이 결함의 모양이 반복된다 — **부품은 있고 배선이 없다**(감사 135·157·170·
ADXFilter…). 이번에는 방어 부품이 있는데 정작 매일 밤 도는 길이 옆으로
비껴갔다.

규칙: 공용 안전장치를 만들면 **그것을 안 쓰는 곳을 찾는 검사**를 함께 만든다.
"쓰고 있는가"를 사람이 기억하게 두지 말 것.

    assert "urlopen(" not in source     # 각자 문을 열지 않는다

이런 '금지 목록' 검사는 문자열 검사라 약해 보이지만, **새로 들어오는 코드**를
막는 데는 아주 효과적이다. 다음 사람이 무심코 `urlopen`을 쓰면 그 자리에서
걸린다. (주석은 빼고 보게 해야 한다 — 안 그러면 이 결함을 설명하는 주석
자체에 걸린다. FROZEN_IDEAS ㊵의 자기참조 함정과 같은 계열이다.)

### ㊿+⑰ **손으로 만든 입력을 넣으면 계산하는 코드가 한 줄도 안 돈다**

감사 179(사람이 읽는 산출물 넷 — 대시보드·체결격차·기여도·HTML 리포트)에서
**결함은 못 찾았다.** 대신 내 검사가 두 번 헛돌았다.

**첫째, 기여도.** "전부 손실인데 '이 전략이 75% 기여'라고 말하지 않는다"를
검사한다며 이런 dict를 손으로 만들어 `attribution_report`에 넣었다.

    {"a": {"sharpe": -1.0, "total_return": -0.2, "contribution_hint": None}}

힌트가 None인 걸 확인했으니 통과다 — 그런데 **힌트를 만드는 코드는 한 줄도
안 돌았다.** 내가 답을 손으로 적어 넣고 그 답을 다시 읽은 것뿐이다. 그래서
`if total > 0:` → `if True:` 변이가 유유히 빠져나갔다. 진짜 하락장을 만들어
`strategy_attribution`을 **실제로 돌리자** 그 자리에서 잡혔다.

규칙: 검사하려는 것이 **계산의 결과**라면 계산을 돌려야 한다. 자료구조를
손으로 빚어 넣는 건 포맷터 검사이지 로직 검사가 아니다.

**둘째, 정렬.** "장부가 날짜순이 아니어도 '직전'을 제대로 찾는가"를 보려고
행을 그냥 섞었더니 — 우연히 같은 답이 나와 `chrono(hist)` 제거 변이를
놓쳤다. '직전'은 `hist[j-1]`로 찾으므로, **배열에서 앞에 놓인 행의 비중이
진짜 전날과 달라야** 답이 갈린다. 앞자리에 비중 1인 행을 두자 정렬 없이는
`prev_w=1` → 비중 변화 없음 → 표본이 통째로 사라졌다.

규칙: '순서를 지키는가'를 검사할 때 **아무렇게나 섞으면 안 된다.** 틀린
순서가 **틀린 답을 내도록** 섞어야 한다. 무작위 섞기는 우연히 맞는다.

### ㊿+⑱ **두 숫자를 맞댈 땐 '같은 것을 재는 숫자'인지 먼저 볼 것**

감사 180. `break_even_cost`는 수수료를 올릴 때 슬리피지도 0.5배로 **함께**
올린다. 그러니 거기서 나온 숫자는 '수수료'가 아니라 **총비용 fee×1.5의
대리 변수**다. 그런데 리포트는 이렇게 대조했다.

    if preset["fee"] > be:          # 수수료 : 수수료

둘 다 `fee`라는 이름을 달고 있어 맞대는 게 자연스러워 보인다. 실제로는
**한쪽만 슬리피지를 품고 있었다.** 시장마다 그 비율이 제각각이라 격차가
일정하지도 않다.

    시장            수수료    슬리피지   총비용   (fee만 볼 때 놓치는 몫)
    crypto         0.1000%   0.0500%   0.1500%   1/3
    crypto_upbit   0.0500%   0.0500%   0.1000%   1/2
    us_stock       0.0100%   0.0500%   0.0600%   **5/6**

미국주식은 무수수료 브로커가 일반화돼 비용의 거의 전부가 슬리피지다.
실측(손익분기 편도 0.03% = 총비용 0.045%짜리 전략): 예전에는 적자 목록에
`us_stock`이 **아예 안 나왔다.** 총비용 0.06%로 손익분기를 33% 넘기는데도.

미국주식은 이 저장소가 실제로 굴리는 시장이고, 이 도구의 존재 이유가
"비용을 못 이기는 전략"을 폭로하는 것이다. 그 도구가 정작 우리 시장에서
안심시키고 있었다.

규칙: 대조하는 두 값에 **같은 이름이 붙어 있다는 것은 근거가 아니다.**
각각이 무엇을 포함하는지 세어 보고, 다르면 **포함 범위를 맞춘 뒤** 맞댄다.
그리고 그 범위를 반환값에 **명시적으로 실어 보낸다** — 여기서는
`slippage_ratio`와 `break_even_total_cost`를 함께 돌려주게 했다. 호출부가
"아마 총비용이겠지"를 짐작하지 않도록.

곁들여 이 감사에서 확인만 하고 끝난 것(결함 없음): 교차검증은 겹침이
없으면 `ok=False`다("검증 못 함"은 "통과"가 아니다 — 감사 163과 같은 계열),
레짐 분류는 expanding 분위수라 절단 검사를 통과한다(미지 구간은 1.0이
아니라 0.5로 인코딩 — 1.0이면 이력이 짧은 초반이 통째로 강세장이 된다),
HTML 리포트의 표는 metrics의 실제 값을 찍는다.

### ㊿+⑲ **오염을 막는 보정을 '사실'로 착각하지 말 것**

감사 181. `fred_series`는 발표 시차만큼 인덱스를 **앞으로 민다**. 판단(ML
피처)이 "그 시점에 실제로 알 수 있던 값"만 쓰게 하려는 룩어헤드 방지책이고,
거기서는 완전히 옳다. 문제는 그 다음이다 — 표시용 카드가 **그 시프트된
인덱스의 마지막 날짜를 그대로 "이 값의 날짜"로** `status.json`에 실었다.

    PUBLICATION_LAG_DAYS = {"T10Y2Y": 1, "BAMLH0A0HYM2": 1, ...}

넷 다 시차 1일이라 **관측일 + 1일**이 찍힌다. 실측(FRED 최신 관측일
2026-08-12): `date = "2026-08-13"` — **공개 기록에 미래 날짜**.

시프트는 "이 값을 언제부터 써도 되는가"를 뜻하지 "이 값이 언제 것인가"를
뜻하지 않는다. 두 질문의 답이 다른데 한 자료구조가 둘 다인 척했다.

규칙: 오염 방지를 위해 **일부러 왜곡해 둔 값**은 그 왜곡을 아는 곳에서만
써야 한다. 다른 목적으로 재사용할 땐 **원본을 다시 부른다**(여기서는
`lag_days=0`). 파생값이 원본보다 손에 가까이 있다는 이유로 집어 쓰면,
그 왜곡이 엉뚱한 자리에서 사실 행세를 한다.

이 계열의 신호: **한 함수가 두 소비자에게 봉사하는데 요구가 반대인 경우.**
판단은 "늦게 알았다고 쳐 달라", 표시는 "언제 것인지 그대로 말해 달라".
그럴 땐 기본값 하나로 둘 다 만족시킬 수 없다 — 부르는 쪽이 명시해야 한다.

곁들여 확인만 하고 끝난 것(결함 없음): 뉴스 브리핑은 피드 하나가 죽어도
나머지가 살고 망가진 XML은 빈 목록이며 카테고리 간 중복을 제거한다.
`python -m quant` 진입점은 실제로 CLI를 부른다 — 이건 소스를 봐선 알 수
없어 **하위 프로세스로 진짜 돌려서** 못 박았다(4줄짜리 파일도 죽으면
야간 잡 전체가 조용히 무동작이 된다).

### ㊿+⑳ **'모름'은 숫자가 아니다 — 기본값이 파괴적인 쪽이면 안 된다**

감사 182, 전수조사의 마지막 파일(`broker/kiwoom_live.py`)에서 결함 셋이
나왔다. 셋 다 모양이 같다 — **모를 때 조용히 위험한 답을 낸다.**

**① 모르는 주문 방향의 기본값이 '매도'였다.**

    api_id = self.TR_BUY if side == "buy" else self.TR_SELL

정확히 `"buy"`가 아닌 **무엇이든** 매도가 되어 나간다. 사는 줄 알고 팔린다.
지금 상위 로직은 소문자만 만들지만 `market_order`는 Broker의 **공개 API**다.
"현재 호출자가 옳게 부른다"는 방어가 아니다.

**② 보유 목록 키가 없으면 '0주 보유'라 답했다.** 감사 55와 같은 결함이다
— 그때는 401·429·500·타임아웃이 전부 '0주'였다. 실거래에서 보유를 0으로
읽으면 상위 로직이 목표 비중만큼 **다시 사서** 포지션이 두 배가 된다.

특히 고약한 이유: 이 파일은 **첫 줄부터** "응답 필드명은 개정될 수 있다"고
적어 두고 필드명을 오버라이드 가능한 속성으로 뒀다. **어긋나는 일이 실제로
일어난다는 전제**로 만들어 놓고, 어긋났을 때의 답이 가장 위험한 쪽이었다.
위험을 알고 적어 두는 것과 그 위험에 대비하는 것은 다른 일이다.

**③ 예수금 필드가 없으면 '현금 0'이라 답했다.** 0은 보수적으로 보이지만
아니다 — 상위 로직은 현금으로 평가금액을 만들고 거기에 목표 비중을 곱한다.
0으로 읽히면 평가금액이 쪼그라들어 **보유분을 팔라는 지시**가 나온다.
"작은 값 = 안전"은 방향을 안 따져 본 직관이다.

규칙: 바깥 응답을 읽을 때 **키가 없는 것과 값이 0인 것을 구분한다.** 키가
있는데 그 안에 없으면 진짜 '없음', 키가 아예 없으면 '모름'이고 모르면
매매하지 않는다. 그리고 **기본값을 고를 땐 그 값이 상위에서 어떤 행동이
되는지까지 따라가 본다** — 여기서는 셋 다 '팔거나, 두 배로 사거나'였다.

형제도 같이 고쳤다(`broker/kr_live.py`, KIS). 증권사를 갈아탄 날 한쪽만
살아 있으면 그게 더 나쁘다(⑭).

**이로써 전수조사 116/116 = 100%.** 운영 코드 전 파일이 변이 시험에
진입했다(변이 항목 328건). 다만 '진입'은 '완전'이 아니다 — 파일마다 걸어
둔 변이는 그 파일의 **일부 계약**만 겨눈다. 전수조사가 끝났다는 말은
"모든 파일에 최소 하나의 살아 있는 계약이 박혔다"는 뜻이지 "이 저장소에
결함이 없다"는 뜻이 아니다. 그 둘을 섞어 말하지 말 것.

### ㊿+㉑ **변이 시험 자신도 감사 대상이다 — '잡았다'는 표시가 거짓일 수 있다**

전수조사 116/116을 채운 직후, **야간 잡을 처음으로 끝까지 돌려 봤다.**
328건 중 3건이 `❌ 못 잡음`이었다. 셋 다 오래된 항목이고, 셋 다 원인이
달랐다. 이걸 안 돌려 봤으면 "전 파일 진입 완료"라고만 적고 끝났을 것이다.

**① 검사가 주석에 기대고 있었다.**

    assert src.count("_drop_unclosed") >= 2, "정의만 있고 호출되지 않는다"

진짜 호출을 통째로 지워도 통과한다 — 그 호출을 **설명하는 주석**에 같은
이름이 적혀 있기 때문이다. 게다가 그 주석은 내가 감사 163에서 직접 붙였다.
㊵의 자기참조 함정에 그 함정을 아는 사람이 걸린 것이고, 감사 178에서 같은
대비책(주석 걷어내고 세기)을 만들어 놓고 **새 검사에만** 적용했다. 형제를
안 찾은 것이다(⑭).

**② 방어가 절대 안 걸리는 자리에 있었다.**

`hrp_weights` 끝의 "합이 1이 아니면 폴백"은 바로 위 `w = w / w.sum()`이
이미 합을 1로 만든 뒤라 **어떤 입력으로도 안 걸린다.** 방어를 위험한
자리가 아니라 **만든 자리**에 둔 것이다. 진짜 위험한 곳은 호출부였다.

    slices = hrp or erc or {k: 1.0 / n for k in weights}

파이썬에서 **값이 전부 0인 dict는 참(truthy)이다.** 배분기가 전부 0을
돌려주면 `or`가 폴백으로 안 넘어가고 그대로 채택된다 — 전 종목 비중 0,
하루 통째로 관망인데 장부에는 "hrp로 배분함"이라 적힌다.

**③ 변이 자체가 무의미했다.**

    w * eff_scale * vscale * guard_damp  →  w * eff_scale * guard_damp * vscale

**곱셈은 교환법칙이 성립한다.** 값이 같으니 어떤 검사도 못 잡는다. 항목
설명은 "오늘 고친 결함 재현"이라 적혀 있었지만, 재현되는 게 없었다.
진짜 결함은 순서가 아니라 **vscale을 무엇으로 계산하느냐**다.

그 자리를 겨누자 이번엔 **검사가 그 자리에 닿지를 못했다.** 두 겹이었다.

  · 2종목 합성 장부에서는 비중이 0 아닌 종목이 하나뿐이라 `ex_ante_vol`이
    **추정 자체를 안 한다**(배수 1.0 고정). 즉 그 harness로는 변동성
    스케일러가 **아예 안 돌았다** — 스케일러에 관한 어떤 결함도 안 보인다.
  · 목표가 기본값이면 배수가 `MAX_VOL_SCALE` 천장에 붙는다.

규칙: **한도에 눌려 있는 값으로는 순서·비율을 검사할 수 없다.** 무엇을
넣어도 같은 답이 나오기 때문이다. 검사하려는 양이 **천장도 바닥도 아닌
중간 구간**에 오도록 입력을 잡고, 그 전제를 검사 안에 명시적으로 박아 둘 것
(여기서는 `assert 0 < scale < 1`).

그리고 더 큰 규칙: **자동 검증 장치를 만들었으면 끝까지 한 번은 돌려 보고
그 결과를 읽을 것.** 이 저장소는 "돌지 않는 검증은 장식"이라고 이미 적어
뒀는데(감사 125), 이번엔 돌긴 돌았고 **결과를 안 읽었다.** 야간 잡은
어제 한 번 돌아서 이미 실패해 있었다.

### ㊿+㉒ **가장 많이 의지하는 자리가 가장 덜 검사돼 있었다**

전수조사 116/116을 채운 뒤 밀도를 재 봤다.

    운영 코드 116파일 · 주석 뺀 17,662줄
    변이 항목 304건 → 58줄당 1건 · **49개 파일은 변이가 딱 1건**

그 1건짜리 목록의 맨 위가 `web/app.py`(1,813줄)와
`backtest/engine.py`(252줄)였다. **이 저장소의 모든 숫자가 나오는 자리**에
계약이 하나 박혀 있었다. 파 보니 결함 셋이 나왔다(감사 184).

  · 신호 인덱스가 봉과 어긋나도 그냥 계산 — 실측 총수익률 -7.32%,
    예외도 경고도 없음. **그럴듯해서 사람이 못 잡는다.**
  · 파산 바닥 없음 — 음수 자산에 음수를 곱해 **10,000 → -20,000 → +20,000**,
    총수익률 **+100%**. 그 사이 CAGR은 NaN이고 오디션은 안 거른다.
  · `next_open_fill`에서 진입가가 체결가가 아니라 결정가 — 불리 갭 5%면
    손절이 **-20.60%** 에서 걸린다(설정 -15%). 오디션 경로에 켜져 있다.

규칙: **"어디를 감사했나"가 아니라 "무엇에 얼마나 의지하나"로 순서를 정할 것.**
파일 목록을 채우는 순서로 돌면 큰 파일과 작은 파일이 같은 한 칸이 되고,
정작 전부가 매달린 자리가 가장 얇은 채로 끝난다. 밀도를 재 보기 전에는
그게 안 보였다 — **"100%"라는 숫자 자체가 그걸 가렸다.**

### ㊿+㉓ **자동 커밋이 CI를 건너뛰면 그 커밋만 감사를 안 받는다**

감사 185. 스위트를 돌렸더니 내가 안 건드린 검사가 깨져 있었다. `main`이
이미 빨간 상태였다. 범인은 야간 배치의 자동 커밋이었다.

    5658418 매일 자동 페이퍼 기록: 2026-08-12 **[skip actions]**

장부에 네 필드(`stale_marks`·`lot_infeasible`·`data_source`·
`data_source_fallback`)가 새로 생겼는데 화면 어디에도 없었다. 그걸 잡으라고
만든 검사가 바로 그 커밋에서만 안 돌았다.

그중 `lot_infeasible`은 **지금 켜져 있다** — 두 종목이 배정 예산(₩2,142·
₩16,941)으로 1주 값(₩274,000·₩166,600)을 못 사서 담기지 못했다. 사이트는
유니버스 전체로 분산한다고 말하는데 실제 보유는 그보다 적다. 계좌가 작을수록
비싼 주식부터 빠지므로 **조용히 편중이 생긴다.**

규칙: `[skip actions]`를 다는 커밋이 **내용을 바꾼다면** 그 내용에 대한
검사도 함께 건너뛴 것이다. 사람이 안 쓰는 커밋일수록 더 그렇다 — 아무도
안 읽으니까.

곁들여 같은 함정에 또 걸렸다: 이 경고를 설명하는 **주석에 "N종목"이라고
썼더니** 산문 검사가 그걸 사이트의 주장으로 읽고 실패했다(㊵ 자기참조).
설명은 검사 대상이 아니라고 생각하지 말 것 — 파일을 통째로 훑는 검사에는
주석도 본문이다.

### ㊿+㉔ **'위반 없음'만 확인하는 검사는 검사를 꺼도 똑같아 보인다**

감사 187에서 내가 또 밟았다. 의존성 잠금을 이렇게 검사했다.

    assert lock_violations("requirements.txt") == []

지금 환경이 범위 안이니 통과한다 — 그런데 **판정 자체를 `if False:`로 꺼도
똑같이 통과한다.** 변이가 유유히 빠져나갔다. 없는 것을 확인하는 검사는
'없다'와 '안 본다'를 구별하지 못한다.

고친 방법: **실제로 어기는 입력을 넣어 걸리는지 본다.**

    req.write_text("pandas>=99,<100")
    assert lock_violations(req)          # 걸려야 한다
    req.write_text("pandas>=0,<999")
    assert lock_violations(req) == []    # 안 걸려야 한다

이건 ⑨ '대조군'의 다른 얼굴이다. 대조군을 "정상일 때도 값이 나오는가"로만
생각했는데, **부정형 단언(`== []`·`not in`·`is None`)에는 반대 방향의
대조군이 특히 필요하다.**

### ㊿+㉕ **같은 통계를 두 화면이 다르게 세고 있었다**

감사 168에서 적중률이 **보합인 날을 오답으로** 세는 것을 찾아 고쳤다.
감사 187에서 그 **형제**를 찾았다 — 방송·사이트의 "모델이 70%라 말한 날의
실제 상승 비율"도 보합을 '오르지 않음'으로 센다.

여기서는 셈 자체가 틀린 게 아니다. 보정 곡선이라 "올랐나?"에 보합은 No가
맞다. 틀린 건 **말하지 않은 것**이다. 실측(같은 성질의 두 종목):

    보합 4일 섞인 종목 → 상승 비율  56%
    보합 없는 종목     → 상승 비율 100%

거래정지·상하한가·저유동 국내주식은 보합이 잦고 코인은 거의 없다. 그래서
**같은 실력이 종목에 따라 다른 숫자로 보인다.** 감사 168에서 정한 규칙을
그대로 적용했다 — 빼거나 숨기지 않고 **몇 날이었는지 함께 말한다.**

규칙: 결함을 하나 고쳤으면 **그 결함의 정의를 문장으로 적고, 그 문장이
참인 자리를 전부 찾을 것.** "적중률이 보합을 오답으로 센다"가 아니라
"보합 빈도가 종목마다 다른데 그것을 반영·고지하지 않는 자리"로 적었다면
그때 둘 다 나왔다. ⑭를 지켰다고 생각했는데 **검색어가 좁았다.**

### ㊿+㉖ **재추출은 '무엇을 뽑나'뿐 아니라 '누가 덜 뽑히나'를 봐야 한다**

감사 188. 블록 부트스트랩을 두 곳에서 이렇게 잘랐다.

    idx.extend(range(start, min(start + block, n)))     # compare.py
    out.extend(r[start : start + block])                # monte_carlo.py

블록은 `start ≤ j`인 자리에서만 j를 담는다. 그래서 **앞쪽 원소는 애초에
들어갈 블록이 적다.** 실측(n=100 · block=10 · 2만 회):

    index 0    0.108×      (균등이면 1.00×)
    앞 10개    0.584×
    중간·뒤    1.05×

코드만 읽어서는 안 보인다 — 어디에도 "앞부분을 빼라"는 줄이 없다. **경계
처리(끝에서 자르기)의 부작용**으로 생긴 편향이라, 잡으려면 **뽑힌 빈도를
세어 봐야** 한다. 그래서 이번 검사는 "결과가 그럴듯한가"가 아니라
**"각 인덱스가 몇 번 뽑혔나"** 를 직접 센다.

규칙: 표집·가중·집계 코드를 감사할 때는 **출력이 아니라 '입력 각각이 몇 번
쓰였나'를 히스토그램으로 볼 것.** 균등해야 할 것이 균등한지는 눈으로 못
읽는다. (같은 계열: ㊾ '0과의 비교', ㊿+⑱ '같은 이름이라고 같은 것 아니다'.)

이게 어디에 쓰이는지가 중요하다 — **챔피언을 바꿀지 정하는 A/B 유의성
검정**과 **사이트의 '무작위 대비 백분위'**다. 둘 다 "우리가 잘하고 있나"를
판정하는 자리다. 판정 도구의 표본이 기울어 있으면 판정 전체가 기운다.
docs/trust.html에 공시했다.

### ㊿+㉗ **'실패했을 때'는 보안과 별개의 질문이다**

감사 190. `quant/utils/envfile.py`는 실거래 키(`.env`·`license.key`)를 쓰는
자리이고, **보안은 꼼꼼히 감사돼 있었다** — 0o600으로 먼저 만들어 노출되는
순간을 없애고(㊾), chmod가 실패하면 "600 확인됨"이라 말하지 않는다.

그런데 **쓰기가 실패했을 때**는 아무도 안 봤다.

    fd = os.open(fp, O_WRONLY | O_CREAT | O_TRUNC, 0o600)   # 제자리에서 자른다

대상 파일을 먼저 자르고 그 위에 쓴다. 중간에 실패하면(디스크 가득·중단)
빈 파일만 남는다. 실측: `BINANCE_KEY=... · KIS_APP_SECRET=...` → `''`.

감사 162에서 **캐시**에는 원자적 쓰기를 넣었다. 캐시는 다시 받으면 되지만
API 키는 거래소에서 재발급받아야 한다. **덜 중요한 파일이 더 잘 지켜지고
있었다.**

규칙: 파일을 쓰는 코드를 감사할 때 **두 질문을 따로 던질 것.**
① 이 파일을 남이 읽을 수 있나(보안) ② **쓰다 죽으면 원래 있던 게 남나**(내구성).
한쪽이 잘 돼 있다고 다른 쪽이 됐다고 착각하기 쉽다 — 여기서는 보안 주석이
빽빽해서 오히려 다 본 것처럼 보였다.

곁들여 배운 것: 고치고 나니 **기존 검사 두 개의 전제가 사라졌다.** 둘 다
"chmod를 실패시켜" 약속을 못 지키는 상황을 만들었는데, 이제 chmod를 아예
안 쓴다. 검사가 지키던 **계약**(권한을 확인 못 했으면 "확인됨"이라 말하지
않는다)은 그대로이므로, 지우지 않고 **상황을 만드는 방법만** 바꿨다.
구현을 고치면 그 구현에 기대던 검사도 함께 봐야 한다 — 초록이라고 넘어가면
계약이 조용히 사라진다.

### ㊿+㉘ **실패를 '오늘 확인한 사실'로 도장 찍으면, 그 거짓말이 캐시 수명만큼 산다**

감사 191. 실적 가드는 발표일 ±1일에 비중을 절반으로 줄인다 — 갭이 가장 큰
날을 겨눈 보호다. 발표일 목록은 야후에서 받아 7일 캐시한다.

    except Exception:
        dates = (entry or {}).get("dates", [])           # 옛 캐시라도 쓴다
    entry = {"dates": dates, "fetched": today.isoformat()}   # ← 실패인데 '오늘'

**조회가 실패해도 `fetched`에 오늘을 적었다.** 다음 실행에서 '신선하다'가
되어 7일 동안 재시도를 안 한다. 첫 조회가 실패했다면 `dates`는 빈 목록이라,
그 일주일은 **"이 종목엔 실적 발표가 없다"로 굳는다.** 실측:

    08-12  조회 실패        → 캐시 {dates: [], fetched: "08-12"}
    08-13  **실제 발표일**   → 조회가 정상인데 부르지도 않음 → 가드 1.0

가드가 지키려는 바로 그 날에 꺼져 있다. 코드 주석은 이 경로를 "조회 실패 =
가드 미발동(무해)"이라 적어 뒀는데, **하루 미발동이 아니라 일주일이고 그
안에 발표일이 들어 있다.**

규칙: 캐시에 '언제 받았나'를 적을 때, **그 도장은 성공에만 찍는다.** 실패
시에는 데이터는 살려 쓰되 신선도는 갱신하지 않는다 — 그래야 내일 다시
시도한다. `'못 받았다'와 '없다'는 다르다`(감사 163·182)의 **시간축 판**이다:
공간적으로 구분해 놓고도 시간적으로 뭉개면 같은 결함이 된다.

곁들여 이 감사에서 확인만 하고 끝난 것: 같은 모양의 갱신 캐시는 `web/app.py`의
`_PRICE_CACHE` 하나뿐인데, 거기는 `if prices:`로 **성공했을 때만** 도장을
찍는다 — 이미 옳았다. 두 곳이 같은 규칙을 다르게 쓰고 있었던 셈이다(㉞).

### ㊿+㉙ **형제를 고칠 때는 '한 곳으로 모으기'까지 가야 끝난다**

감사 182에서 "모르는 주문 방향의 기본값이 매도"를 찾아 **두 브로커**에
검사를 넣었다. ⑭를 지켰다고 생각했다. 감사 192에서 **같은 모양을 두 곳 더**
찾았다.

    paper.py   signed = quantity if side == "buy" else -quantity
    retry.py   signed = delta    if side == "buy" else -delta

하필 `paper.py`는 **8마일 챌린지의 모든 기록이 나오는 브로커**다. 거기서
방향이 뒤집히면 장부·사이트·방송이 전부 그 위에 쌓인다. `retry.py`는 체결
확인이 그 분기로 증감 방향을 정하므로 확인 로직까지 반대로 돈다.

무엇이 달랐나 — 감사 182에서는 **검사를 복사해 넣었다.** 복사는 그 순간의
목록에만 적용된다. 이번에는 판정을 `base.normalize_side()` 하나로 모으고
여섯 브로커가 전부 그걸 부르게 한 뒤, **"방향으로 갈라지면서 공용 판정을
안 지나는 브로커가 있는가"** 를 기계적으로 세는 검사를 붙였다. 새 브로커가
자기 방식으로 방향을 읽으면 그 자리에서 걸린다.

규칙: 형제를 찾았으면 **같은 코드를 두 번 쓰지 말고 한 곳으로 모을 것.**
복사한 검사는 다음 형제를 못 막는다 — 막는 것은 *"모두가 여기를 지나는가"*
를 세는 검사뿐이다(㉞ + 감사 178의 '금지 목록' 검사).

곁들여 이번에도 대조군에서 미끄러졌다. 지정가 방향 변이를 처음엔 놓쳤는데,
검사가 **소문자로만** 미체결 경우를 확인했기 때문이다. 갈라지는 곳은
"`BUY`인데 매수로는 안 닿고 매도로는 닿는 봉" **하나뿐**이었다 — 회계는
`market_order`가 다시 정규화해 맞춰 주므로 겉으로는 멀쩡하다.
**대조군은 '정상값'이 아니라 '갈라지는 값'으로 잡아야 한다.**

### ㊿+㉚ **'언제 보내나'와 '무엇을 요약하나'는 다른 날짜다**

감사 193. 일일 요약은 UTC 날짜가 바뀐 **직후** 보낸다. 그런데 요약 대상으로
그 시점의 `today`, 즉 **막 시작한 날**을 넘겼다.

    return t, build_daily_summary(state, today=t)      # t = 방금 시작한 날

새 날에는 기록이 0~1건뿐이다. 실측(08-12에 3사이클 돌고 롤오버):

    예전:  "일일 요약(2026-08-13): 오늘 사이클 1회 · 자산 111.00"
    실제:  08-12은 3사이클 · 그 날 마지막 자산 110.00

하루를 마감하는 보고인데 **아직 시작도 안 한 날**을 보고했다. 어제가 아무리
바빴어도 늘 0~1회로 찍히니, 받는 사람은 "봇이 거의 안 돌았다"고 읽는다.
자산·적중률도 `history[-1]`(전체 최신)에서 가져와 **다음 날 값**을 어제
요약에 실었다.

규칙: 주기 보고에는 날짜가 **둘** 있다 — *보내는 시각*과 *대상 기간*. 코드에
변수가 하나뿐이면(`today`) 둘이 같다고 가정한 것이고, 경계에서 반드시
갈라진다. 이름을 나눠 두면(`sent_on` / `covers`) 애초에 안 헷갈린다.

기존 검사가 왜 못 잡았나: 롤오버 검사가 `"일일 요약" in text`만 봤다.
**보냈는지는 봤고 무엇을 보냈는지는 안 봤다** — ⑨ 대조군의 또 다른 얼굴이다.
"동작했다"를 확인하는 검사와 "옳은 것을 동작했다"를 확인하는 검사는 다르다.

### ㊿+㉛ **끄려고 적은 글자가 켜는 결과를 낸다 — 파이썬 truthiness**

감사 195. `config/settings.json`은 사장님이 어드민에서 바꾸고 **깃에 커밋되는**
파일이다. 즉 UI 말고 **사람이 직접 고치는 일이 실제로 있다.** 불린을 이렇게
읽었다.

    out["risk_override_ack"] = bool(raw.get("risk_override_ack", False))

파이썬에서 **비어 있지 않은 문자열은 전부 참**이다. 실측:

    risk_override_ack = "false"  →  True   ← 켜졌다
    risk_override_ack = "no"     →  True
    risk_override_ack = "0"      →  True
    social_post       = "false"  →  True   (계속 게시한다)

`risk_override_ack`는 이 파일이 스스로 **"실수로 켜지지 않게 별도 키"**라고
적어 둔 스위치다 — 엣지 미입증 상태에서 목표 변동성 상한을 푼다. 그런데
**끄려는 시도가 켜는 결과**를 냈다. 안전 원칙과 정확히 반대다.

규칙: 바깥에서 들어온 값을 `bool()`로 감싸지 말 것. `bool("false")`는 참이다.
**글자를 글자대로** 읽고(`"false"·"0"·"no"` → 거짓), 모르는 값은 **추측하지
말고** 기본값 + 경고로 둘 것. 설정·환경변수·쿼리스트링·CSV — 사람이 손으로
쓰는 모든 통로에 해당한다.

곁들여 검사가 헛돌 뻔했다: 이 저장소의 로거는 `propagate = False`라
**pytest의 `caplog`가 아무것도 못 본다.** "경고를 남기는가"를 caplog으로만
확인했으면 경고가 아예 없어도 통과했을 것이다. 로거에 직접 수집 핸들러를
붙여야 한다 — *검사 도구가 대상을 볼 수 있는지부터 확인할 것.*

### ㊿+㉜ **내 실험이 실제 경로를 지나는지부터 확인할 것**

감사 196. FRED 파서가 `"nan"`·`"inf"`·`"1e400"`을 통과시키는 걸 보고,
그게 `status.json`에 실려 브라우저의 `JSON.parse`를 깨뜨린다고 판단했다.
사이트 전체가 안 뜨는 큰 결함으로 보였다.

**틀렸다.** 내 실험이 `json.dumps`를 **직접** 부른 것이었다. 실제 writer는
`jsonio.atomic_write_json`이고, 거기서 `sanitize()`가 비유한값을 `null`로
바꾼다 — 이미 검사(`test_jsonio.py`)와 변이 항목으로 지켜지고 있었다.
실제 경로로 다시 돌려 보니 파일에 `NaN`이 없다.

이건 감사 179에서 배운 것(**손으로 만든 입력을 넣으면 계산 코드가 안 돈다**)의
쌍둥이다. 그때는 검사가 실제 코드를 안 지나 **결함을 놓쳤고**, 이번엔 실험이
실제 코드를 안 지나 **없는 결함을 봤다.** 방향만 반대고 원인은 같다.

규칙: "이 값이 저기까지 간다"를 주장하기 전에 **그 경로를 실제로 태워 볼 것.**
중간에 있는 방어를 모른 채 양쪽 끝만 보면, 있는 결함도 없는 결함도 만든다.

(원천 방어는 그래도 넣었다 — 공개 JSON은 아래층이 막지만 **ML 피처는
아무도 안 막는다.** 같은 값이 두 방향으로 흐를 때, 한쪽이 막힌다고 다른
쪽이 막히는 게 아니다.)

곁들여 확인: `fred_frame`은 어디서도 안 불린다(정의·재export만). 실제 거시
피처는 `data/crossasset.py`가 만든다. 배선 안 된 부품 목록에 하나 더다.

### ㊿+㉝ **검사가 달력에 의존하면 언젠가 반드시 무시된다**

2026-08-13. PR #193의 CI가 빨개졌다 — 내가 그날 건드리지도 않은
`test_the_decision_frame_really_excludes_that_bar`가 실패했다.

    assert 12 < 12

원인은 회귀가 아니라 **날짜가 바뀐 것**이었다. 그 검사는 `"2026-08-01"`부터
12봉을 만들어 마지막 봉이 `2026-08-12`가 되게 했고, "코인의 진행 중인 봉이
빠지는가"를 봤다. 쓴 날에는 08-12가 오늘이라 진행 중이었다. UTC로 하루가
넘어가자 08-12는 **완성 봉**이 되었고, 뺄 것이 없으니 길이가 그대로였다.
코드는 멀쩡한데 검사만 늙은 것이다.

이 실패가 나쁜 이유는 실패 자체가 아니라 **그 다음**이다. 한 번
"아, 달력 때문이네" 하고 넘기고 나면, 같은 자리에서 **진짜 회귀**가 났을
때도 정확히 같은 얼굴로 나타난다. 늑대가 온다고 두 번 외친 검사는 세 번째에
아무도 안 본다. 안전장치가 꺼지는 가장 흔한 방식은 삭제가 아니라 **무시**다.

규칙: '지금'과 비교하는 코드를 검사할 때는 둘 중 하나만 할 것 —
① **지금을 주입한다**(`now=` 인자를 넘긴다. `test_unclosed_bar.py`가 그렇게
한다), 또는 ② **오늘을 기준으로 데이터를 만든다**(`_frame()`이 그렇게 한다).
날짜를 상수로 적어 두는 것은 "이 검사는 오늘 하루만 유효합니다"라고 적는
것과 같다.

형제 확인: `bar_status`·`bar_elapsed_fraction`·`_drop_unclosed`·`_signal_frame`
— 시계를 보는 함수를 부르는 검사 전부를 훑었다. 나머지는 전부 ①이나 ②였고,
상수 날짜를 그냥 흘려 넣은 것은 이 한 자리뿐이었다.

### ㊿+㉞ **NaN은 안전장치를 '끄지' 않는다 — 아무 말 없이 없앤다**

감사 198. 매매를 강제로 멈추는 장치 둘(`DailyLossKillSwitch`·`CircuitBreaker`)이
같은 방식으로 무력화됐다. 실측:

    킬스위치   : 그날 첫 관측이 NaN → day_start=nan → -50%도 **-99%도 발동 안 함**
    서킷브레이커: 첫 관측이 NaN → peak=nan. `max(nan, x)`는 nan을 그대로 둔다
                 (x > nan이 False) → **세션이 끝날 때까지 회복 못 함**
    서킷브레이커: 첫 관측이 inf → 다음 관측이 곧바로 '-100% 낙폭' **오청산**

파이썬에서 NaN과의 모든 비교는 False다. 그래서 결함이 `if`문 하나를 끄는 게
아니라 **그 아래 판단 전체를 조용히 통과**시킨다. 경고 한 줄 없고, 로그만
보면 "오늘은 발동할 일이 없었다"와 구분되지 않는다.

세 번째 얼굴이 있다 — **설정 오기입.** 한도는 분수(0.03 = 3%)인데 `3`이라고
적으면 "-300% 도달 시 중단"이라 영영 발동하지 않는다. 그런데 시작 로그는
`-{limit:.0%}`로 **"-300%"라고 또박또박 찍는다.** 사장님은 브레이크가 걸려
있다고 믿는다. 음수로 적으면 반대로 항상 발동한다.

규칙: 안전장치는 없는 것보다 **'있다고 믿는데 없는 것'**이 나쁘다 — 그 믿음
위에서 더 크게 베팅하게 되기 때문이다. 그래서 ① 숫자가 아닌 값은 기준선으로
삼지도 비교하지도 않고 **기존 기준선을 보존한 채 판정만 보류**하고(오염된
기준선은 다음 사이클이 멀쩡해도 되살아나지 않는다), ② 한도값은 **시작할 때
세운다.** 설정 오타는 실행 전에 드러나야 한다.

판정은 `live/riskguard.py` 한 곳에 모았다 — 감사 182에서 브로커 둘만 고쳤다가
형제 넷을 빠뜨린 적이 있다. 한쪽만 고치면 "킬스위치는 막는데 서킷브레이커는
안 막는" 상태가 되고, 그건 둘 다 안 막는 것보다 알아채기 어렵다.

### ㊿+㉟ **무거워서 못 쓰는 함수는 결국 각자 베껴 쓴다**

감사 197. 조종석(`reporting/dashboard.py`)과 감시 탭(`web/app.py`의 브라우저
JS)이 자산 곡선으로 손익·낙폭을 각자 계산했고, 둘 다 **입금을 수익으로 셌다.**

    8만원 → (92만원 입금) → 95만원
    화면   : 손익 **+1087.50%**    사실 : 100만 → 95만 = **-5.00%**

낙폭도 같이 거짓이 된다 — 입금이 고점을 끌어올려 그 직전의 손실이 지워진다
(감사 ㊿에서 킬스위치가 입금 때문에 풀리던 것과 같은 계열이고, `twr_index`
독스트링이 "낙폭도 이 지수 위에서 재야 한다"고 **이미 적어 둔** 내용이다).

왜 베꼈나: 올바른 계산(`twr_index`)이 `daily.py`에 있었고, 거기서 가져오면
numpy·pandas가 딸려 온다 — 감사 102에서 SNS 게시가 죽은 바로 그 이유다.
**무거워서 안 쓰고 각자 계산한 것이다.** 그래서 함수를 옮겼다: 표준
라이브러리만 쓰는 `ledger_basics.py`로. 가볍게 만들어 놓으면 아무도 베껴 쓸
이유가 없다.

곁들여 배운 것 둘:

**① 형제를 안 찾았으면 고친 것이 되돌아갔을 것이다.** 감시 탭 JS는 5초마다
돌며 서버가 계산한 값을 덮어쓴다. 조종석만 고쳤다면 화면이 잠깐 맞았다가
조용히 틀린 값으로 되돌아갔을 것이다 — **안 고친 것보다 나쁘다.** 고친
사람은 고쳤다고 믿기 때문이다.

**② 판정을 옮기면 그 판정을 지키던 검사도 함께 봐야 한다.** `sm.max_drawdown`
이라는 **글자**가 app.py에 있는지 세던 검사가 빨개졌다. 계약(잘린 구간 KPI로
덮어쓰지 않는다)은 그대로인데 구현이 옮겨 간 것뿐이라, 지우지 않고 **글자
대신 실제로 나가는 값**을 보도록 다시 세웠다(감사 190과 같은 처리).

### ㊿+㉜-b **변이 도구는 임포트하지 말 것 — import가 곧 실행이다**

2026-08-13. 남은 감사량을 세려고 `mutation_check.py`를 `importlib`로 임포트해
항목 목록을 읽으려 했다. 그게 **실제 변이 시험을 시작했다.** 죽였다고 생각한
`pkill`은 자기 셸만 죽였고(exit 144), 러너는 212초 동안 살아서 `ml.py` →
`daily.py` 순으로 운영 코드를 하나씩 망가뜨리고 있었다. 커밋 전 훅이 잡았다.

감사 171과 **같은 사고, 다른 문**이다. 그때는 `timeout` 래핑의 SIGTERM이
`screener.py`를 변이된 채 남겼다. 도구를 중간에 죽이면 운영 코드가 망가진
채 남는다는 사실은 알고 있었는데, **'실행'이 아니라 '임포트'라고 생각해서**
같은 문으로 다시 들어갔다.

규칙: 이 도구의 **내용을 알고 싶으면 실행하지 말고 파싱할 것**(`ast`).
그리고 죽일 때는 `pkill -f` 패턴이 정말로 그 프로세스를 가리키는지
`ps`로 먼저 확인할 것 — 아니면 자기 자신만 죽는다.

### ㊿+㊱ **"형제를 둘 다 고쳤다"고 적은 날, 형제는 넷이었다**

감사 199. 감사 182에서 "잔고 응답에 필드가 없으면 0이 아니라 오류다"를
키움·KIS에 넣고 **"형제를 둘 다 고친다"**고 적어 두었다. 그런데 같은 모양이
`crypto_live.py`와 `us_live.py`에도 있었다. 실측:

    Alpaca  equity 키만 어긋남 → 총자산 0     → **전 종목 청산 지시**
    Alpaca  cash   키만 어긋남 → 현금   0
    ccxt    free   키만 어긋남 → 현금   0
    ccxt    total  키 없음     → 보유   0개   → 목표만큼 다시 사서 **두 배**

특히 고약한 것: 그 사이 **감사 192에서 이 두 파일을 열었다.** `normalize_side`를
넣으려고 `market_order`를 고쳤는데, 바로 몇 줄 위의 `get_cash`는 안 봤다.
**파일을 열었다는 것이 그 파일을 봤다는 뜻은 아니다.**

`us_live.get_equity`가 가장 날카롭다 — `equity()`는 MultiTrader·RobustBroker가
총자산을 물을 때 부르는 권위 있는 값이고, 0이면 모든 종목의 목표가
(비중 × 0) = 0주가 된다. **키 이름 하나가 계좌를 비운다.**

규칙: "형제를 다 고쳤다"는 **셀 수 있어야 한다.** 문장으로 적지 말고
검사로 적을 것 — 이번에는 네 브로커가 전부 `base.require_field`를 지나는지
`ast`로 확인하는 검사를 붙였다. 파일이 늘어날 때 그 검사가 잡는다.

곁들여 그 검사를 처음엔 **글자 찾기**로 썼다가 제 발에 걸렸다. 고친 이유를
설명하려고 독스트링에 적어 둔 옛 코드(`acct.get("equity", 0.0)`)를 위반으로
잡은 것이다(감사 183과 같은 자리). 코드를 봐야 하면 소스를 읽지 말고
**파싱할 것.**

### ㊿+㊲ **예산은 n등분이 아니라 좋은 아이디어부터**

2026-08-13 사장님 결정. *"예산이 있는대로 유연하게. 1주 값이 그 종목 예산을
넘으면 기존 투자한 종목을 매도하고 사도 된다."* 그리고 무엇을 포기할지는 —
*"이건 비율로 따지는 게 아니야. 수익률이 더 높을 것이라 판단되는 최우선
선택을 하는 거지."*

왜 필요했나. 실측(원금 100만원 · 20종목 균등 5% = 50,000원):

    SK하이닉스 1주 1,504,000원 (계좌의 150.4%)   현대차   409,500원 (41.0%)
    LG화학       275,500원 (27.6%)               삼성전자 255,500원 (25.6%)
    NAVER        215,500원 (21.6%)               KB금융   166,000원 (16.6%)
    KODEX 200    103,250원 (10.3%)

**7종목 중 자기 예산으로 1주라도 살 수 있는 것이 0개다.** 예전 규칙(자기
배정금액 안에서만 정수 주 내림)에서는 국내주식이 통째로 미뤄진다.

바꾼 것: 정수 주 시장의 돈을 **한 주머니로 모으고 확신도 순으로 채운다.**
1위가 1주를 사면 하위 종목은 오늘 못 산다 — 이미 들고 있었다면 목표가 0이
되어 팔린다. 그것이 "팔아서라도 산다"의 실체다.

**집중도 상한을 두지 않는 근거**: 주머니 총액이 원래 그 시장에 배정된 돈이라
**총노출이 보존된다.** 한 종목이 커지면 다른 종목이 그만큼 줄 뿐 레버리지가
생기지 않는다. 비율로 막을 필요가 없다 — 막아야 할 것은 '없는 돈을 만드는
것'이지 '있는 돈을 좋은 쪽에 몰아 쓰는 것'이 아니다.

대가는 숨기지 않는다: "오늘 국내주식은 한 종목뿐"인 날이 생기고, 그 사실은
`lot_priority`(무엇을 넘겨 샀는가 · 누가 양보했는가)와 `lot_infeasible`로
장부에 남는다.

그리고 **유연화로 안 풀리는 것도 있다.** SK하이닉스 1주는 계좌 전체보다
비싸다 — 원금의 문제지 규칙의 문제가 아니다. 그대로 미루고 적는다.

곁들여 **매도 선집행**을 넣었다. 순서를 정하지 않으면 dict 순서대로 주문이
나가 판 돈이 같은 사이클의 매수에 안 쓰인다. 한 종목의 1주를 사려고 다른
종목을 파는 이상, "먼저 팔고 그 돈으로 산다"가 지켜져야 그 결정이 그날
안에 완성된다.

곁들여 배운 것: 새 검사가 처음엔 **건너뛰었다**(빈 계좌로 시작해 매수만 남).
그리고 기존 내림 변이가 **빠져나갔다** — 주머니 잔액(정수)이 먼저 걸려
내림을 꺼도 결과가 우연히 정수였다. 둘 다 "확인한 줄 알았는데 아무것도
확인 안 한" 경우다. *검사는 그 규칙이 **답을 정하는** 상황에서 돌아야 한다.*

### ㊿+㊳ **막으려던 것과 같은 문을 다른 이름이 지나고 있었다**

감사 200 · `utils/jsonio.py`. 이 모듈은 첫 줄부터 목적을 적어 두었다 —
NaN이 저장을 멈추는 것을 막는다. 이유까지 남겨 놓았다:

    "allow_nan=False로 raise시키면 cycle의 except가 삼켜 저장이 아예 멈춘다."

즉 **"여기서 예외가 나면 그날 장부가 사라진다"**를 알고 만들어졌다. 그런데
**같은 except가 삼키는 다른 예외**는 막지 않고 있었다. 실측:

    float('nan')      → None       (설계대로)
    np.float64('nan') → None       (float의 하위 클래스라 우연히 막힌다)
    np.float32(1.5)   → TypeError   ← 저장이 통째로 죽는다
    np.int64 · np.bool_ · Timestamp · datetime · Path → 전부 TypeError

`np.float64`가 통과하는 것이 가장 고약하다 — numpy 값이 들어와도 대개
괜찮으니 안심하게 되는데, `float32`·`int64`·`bool_` 하나가 섞이는 날 그날
기록이 사라진다. `np.bool_`은 pandas 비교식(`a > b`)이 그냥 내놓는 값이다.

**지금 새는 곳은 아니다** — 실제 하루치 실행으로 확인했다(감사 196의 교훈:
실험이 실제 경로를 지나는지부터 확인할 것). `daily.py`가 필드마다 `float(...)`
로 감싸 두었다. 하지만 그 방어는 **필드를 추가하는 사람이 매번 기억해야
하는 종류**라, 원천에 최후 수단을 뒀다.

방향은 '거절'이 아니라 '보존'이다. 하루치 장부를 잃는 것보다 값이 남는 편이
낫다 — 숫자·시각은 뜻이 보존되게 바꾸고(numpy는 `.item()`, 시각은 ISO),
정말 모르는 것만 문자열로 하되 **경고를 남긴다.** 살리는 것과 숨기는 것은
다르다.

규칙: 모듈이 "무엇을 막는다"고 적었으면, **그 무엇이 지나는 문을 전부**
세어 볼 것. 한 종류만 막고 안심하면, 같은 문으로 다른 이름이 들어온다.

곁들여 `fold_history`의 *"근사가 아니라 등가"* 주장은 **사실이었다.**
무작위 200회로 전 구간 계산과 대조해 오차 0을 확인하고 계약으로 고정했다.
큰 주장을 적었으면 그 주장을 재는 검사도 함께 있어야 한다.

### ㊿+㊴ **못 한 것과 안 한 것을 장부가 구별해야 한다**

감사 201. `data/crypto.py`를 훑었다 — **제공자 자체에는 결함이 없었다.**
보조 거래소 사다리·빈 결과 검사·`end` 컷의 tz 처리 모두 제대로 되어 있다.
문제는 **받은 뒤**였다. 받는 쪽은 `df.empty`만 봤다.

그래서 보조 거래소가 10봉만 주는 날(상장 초기 코인, 그 페어의 이력이 짧은
거래소) 신호는 0으로 나오는데 장부에는 흔적이 없었다. 실측:

    B0가 400봉일 때 → 장부 "3종목 분산" · 실제 포지션 3개
    B0가  10봉일 때 → 장부 "3종목 분산" · 실제 포지션 **2개**   ← 거짓말

감사 59에서 *"데이터 실패로 빠진 종목을 계획 수로 세지 말 것"*을 고쳤다.
그건 **예외가 난 종목**만 잡는다. 데이터가 멀쩡히 온 뒤 표본이 모자라
아무것도 못 한 종목은 그 그물에 안 걸린다 — 감사 200과 정확히 같은 형태다
(무엇을 막는다고 적었으면 그 무엇이 지나는 문을 전부 셀 것).

**조용한 0과 판단해서 낸 0은 다르다.** 전자는 *못 한* 것이고 후자는 *안 한*
것이다. 값이 같다고 뜻이 같지 않다. 장부는 둘을 구별해야 하고, 구별하려면
'못 한' 쪽을 **명시적으로 만들어야** 한다 — 그냥 두면 영원히 0으로만 보인다.

그리고 이건 오디션-현실 격차이기도 하다. 챔피언을 400봉으로 선발해 놓고
실전에서 10봉으로 굴리면 선발 조건과 실전 조건이 다르다(감사 71 계열).

곁들여 두 번 넘어졌다:

**① 요구치를 두 배로 잡았다가 과했다.** 기본 챔피언의 `train_window=250`이
500봉 요구가 되어 정상 픽스처까지 전부 막혔다(검사 5건이 빨개져서 알았다).
파라미터가 전부 '창 길이'는 아니다 — 선언한 창이 **한 번** 채워지는 것까지가
근거 있는 요구고, 그 이상은 내 추측이다. *안전장치를 만들 때도 근거를 넘는
요구는 결함이다 — 지키는 것이 아니라 막는 것이 된다.*

**② 검사 시드를 `hash(sym)`으로 잡았다.** 파이썬 문자열 해시는 프로세스마다
랜덤화된다(PYTHONHASHSEED). 변이 시험이 한 번은 통과, 한 번은 '검사 자체
고장'을 내며 **번갈아** 나와서 알았다. ㊿+㉝(달력에 의존하는 검사)의
쌍둥이다 — *검사는 시계에도 무작위에도 기대면 안 된다.* 결과가 흔들리는
검사는 언젠가 "또 그거네" 하고 무시된다.

### ㊿+㊵ **잡을 수 없는 항목은 안전장치가 아니라 소음이다**

감사 202 · `reporting/attribution.py`. 이 파일은 **운영 경로에 배선되어 있지
않다**(export와 검사에서만 쓰인다) — live 결함이 아니라 공개 API의 구멍으로
적는다. 확인한 것:

    전략 하나의 샤프가 NaN → `max(nan, 0.0)`이 nan → 합계 nan
    → `total > 0`이 거짓 → **전 전략의 기여도 힌트가 사라진다**
    리포트에는 "샤프 nan · 총수익 nan%"가 사람에게 그대로 나간다
    NaN이 섞인 정렬은 순서가 뒤죽박죽이 된다

셋 다 고쳤다. 그런데 **첫 번째 것은 변이가 못 잡았다.** 이유를 파 보니 실제
`Backtester`에 관망·NaN 신호·한 봉짜리 신호를 태워도 샤프가 0.0이나 유한값으로
나온다 — 엔진이 위쪽에서 이미 막고 있어 **그 분기는 도달 불가**였다.

감사 183에서 `hrp.py`의 합계 검사가 정규화 뒤라 어떤 입력으로도 안 걸렸던
것과 같은 자리다. 그때 배운 것을 그대로 적용했다: 방어는 위험이 **실제로
도착하는 쪽**에 둔다(여기서는 바깥 dict를 받는 `attribution_report`). 그쪽은
변이로 지키고, 도달 불가한 보험은 그대로 두되 **변이 항목을 붙이지 않았다.**

규칙: 변이 목록은 "지켜지는 것"의 명세다. 잡을 수 없는 항목을 넣으면 야간
잡이 영원히 빨간 채로 돌고, 그러면 **진짜 실패도 같은 얼굴로 나타난다**
(㊿+㉝과 같은 결말: 안전장치는 삭제가 아니라 무시로 꺼진다). 도달 불가를
발견하면 항목을 빼고 **왜 뺐는지를 코드에 적을 것.**

곁들여 이번에도 손으로 만든 dict로 검사를 써서 변이가 빠져나갔다 — 이 파일이
스스로 그 함정을 경고해 두었는데도 그랬다(감사 179). *경고를 적어 두는 것과
그 경고를 지키는 것은 다른 일이다.*

### ㊿+㊶ **폴백은 가장 나쁜 날에만 쓰인다 — 거기서 조용하면 안 된다**

감사 203 · `data/synthetic.py`. `--timeframe`에는 `choices` 제한이 없어 아무
문자열이나 받는데, 합성 제공자는 **`30m`을 몰랐다.** `barclock`은 30분봉을
아는데 여기만 빠져서, `30m`을 요청하면 **아무 말 없이 일봉**이 돌아왔다.
오타도 마찬가지다.

    요청 30m  → 실제 봉 간격 1 days       (고치기 전)
    요청 3d   → 실제 봉 간격 1 days
    요청 오타  → 실제 봉 간격 1 days

받는 쪽은 요청한 타임프레임이라고 믿고 ATR·변동성·라벨을 만든다 — **24배 긴
봉으로.** 그리고 합성은 **폴백 경로**다. 즉 이 일은 하필 진짜 시세가 죽은
날에만 벌어진다. 평소에 안 보이다가 가장 나쁜 날에 나타나는 결함이다.

원인은 구조였다: 타임프레임 지식이 `_TIMEFRAME_TO_DELTA`와 `_TIMEFRAME_TO_FREQ`
**dict 두 개**로 나뉘어 있었고, 한쪽만 고치면 갈라지는 자리였다(㉞). 실제로는
더 나빴다 — **둘 다 `30m`을 빠뜨렸다.** 두 곳에 나눠 두면 갈라지기만 하는 게
아니라, 둘 다 놓치기도 쉬워진다. 하나로 합치고, 모르는 타임프레임은 조용히
일봉으로 떨어뜨리지 않고 거절한다('모름'은 기본값이 아니다 — 감사 195와 같은
규칙).

그리고 **시계가 아는 봉을 폴백이 모르면 안 된다**는 것을 검사로 못 박았다.
두 곳이 같은 지식을 들고 있는 한 언젠가 갈라지므로, 갈라짐 자체를 재는
검사가 있어야 한다 — 감사 199에서 "형제를 다 고쳤다는 문장 대신 셀 수 있는
검사"를 붙인 것과 같은 처리다.

### ㊿+㊷ **재는 자가 어긋나면 멀쩡한 것도 버려진다**

감사 204 · `data/barclock.py` + `_signal_frame`. 세 가지가 같은 뿌리였다.

**① 신호 프레임이 항상 일봉 자로 쟀다.** `_signal_frame`은 미완결 봉을 빼는
자리인데 `bar_status(market, 봉)`만 불렀다 — 타임프레임을 안 넘겼다. 같은
파일의 **다른 두 자리**(장부의 `bar_partial`, 기록용 `bs`)는 넘기고 있었다.
셋이 같은 판정을 부르면서 **하나만 갈라진 것**이다(㉞). 실측(1시간봉 운용,
3시간 전에 닫힌 봉):

    1h 자로 재면 → 1.0   (완성)
    1d 자로 재면 → 0.125 (진행 중)   ← 멀쩡한 완성 봉이 버려진다

**② 판정 불가와 완성됨이 같은 답(None)이었다.** 호출부는 None을 "완성"으로
읽고 미완결 봉 제거를 건너뛴다 — 모르는 타임프레임 하나로 **감사 71의 보호가
조용히 꺼진다.** 24시간 시장에서는 그게 곧 "항상 진행 중인 봉을 그대로 쓴다"다.

**③ 미래 봉이 '0% 진행 중'으로 적혔다.** 아직 시작도 안 한 봉을 받은 것은
데이터 소스가 어긋난 **사건**인데, 그럴듯한 숫자 하나로 덮였다(감사 181에서
status.json에 미래 날짜가 실렸던 것과 같은 계열).

셋을 관통하는 규칙: **측정에는 '재는 자'가 있고, 그 자를 안 넘기거나 모르면
그 사실 자체를 답에 남겨야 한다.** 숫자를 지어내면(0.0) 읽는 사람은 측정이
된 줄 안다. 지금 운영은 일봉이라 ①은 새는 곳이 아니지만, `--timeframe`을
바꾸는 순간 조용히 틀린다 — *설정을 바꿀 수 있게 만들어 놓고 기본값에만
맞춰 둔 코드는 결함이 예약된 것이다.*

곁들여 또 문자열 개수를 세던 검사가 걸렸다(`src.count("_signal_frame(market, df)")`).
감사 199에서 후보로 적어 둔 바로 그 자리다 — 이번엔 AST로 **호출 자체**를
세고, 겸사겸사 "타임프레임을 넘기는가"까지 못 박았다. 세 번째로 겪은 형태라
이제 규칙으로 적는다: **코드를 확인하려면 소스를 읽지 말고 파싱할 것.**

### ㊿+㊸ **`or`로 고른 값은 단위까지 갈아탄다**

감사 205 · `data/openinterest.py`. 미결제약정을 이렇게 읽고 있었다:

    v = row.get("openInterestAmount") or row.get("openInterestValue")

두 필드는 **단위가 다르다** — 계약수와 달러 명목값. 파이썬에서 `0.0`은
거짓이라, **미결제약정이 진짜 0인 하루**가 끼면 조용히 단위가 갈아탄다.
실측(계약수 1000 → 0 → 1100):

    파싱 결과   1,000 → **64,000,000** → 1,100
    하루 변화율 **+6,399,900%** → **-100%**

이 값은 ML 피처 `x_oi_chg5`로 들어간다. 하루짜리 0이 모델에 그런 점프를
먹인다 — **결측보다 나쁘다.** 결측은 NaN으로 걸러지지만 이건 '진짜 값'인
척한다.

`or` 폴백 결함은 이 저장소에서 세 번째다: 감사 183(값이 전부 0인 dict가
참이라 폴백이 안 걸림), 감사 195(`bool("false")`가 참), 그리고 이번.
**세 번 다 "0/빈 값이 거짓"이라는 같은 성질에서 나왔다.**

규칙: `A or B`는 **A와 B가 같은 뜻일 때만** 쓴다. 뜻이 다르면(단위·의미·
출처) `A is None`으로 명시할 것. 그리고 계열 데이터에서는 **행마다 고르지
말고 계열 단위로 한 번 고를 것** — 행마다 고르면 한 계열 안에서 단위가
섞이고, 섞인 순간 파생값은 전부 거짓이 된다.

형제 확인: `funding.py`는 `safe_amount(row.get("fundingRate"), ...)`로 이미
안전했다. 숫자 필드 사이의 `or`는 `web/app.py`의 `principal or 10000`
하나가 더 있었고(원금이 진짜 0이면 10000으로 읽힌다), 같이 고쳤다.
나머지 `X or {}` / `X or []`는 None 방어용 관용구라 해당 없음.

### ㊿+㊹ **같은 파일 안에서도 '모름'의 답이 갈라진다**

감사 206 · `strategies/regime.py`. 한 클래스 안에 필터가 둘인데 판정 불가를
**정반대로** 처리하고 있었다.

    추세  (MA=NaN)  → `| ma.isna()`로 **막는다**
                      (주석에도 "데이터 부족 구간도 진입 보류"라고 적혀 있다)
    변동성 (vol=NaN) → `vol > 한도` 하나뿐 → **통과**
                      (파이썬에서 NaN과의 비교는 전부 False)

실측(추세 필터를 끄고 변동성 필터만, 한도 0.5%·실제 1.0%):

    판정 불가(워밍업 20봉) → 신호 살아 있음 **20/20**
    판정 가능 구간         → 신호 살아 있음   0/220

**판정을 못 하는 구간에만 매매가 열려 있었다.** 그리고 장부에는
`{'open': True, 'reason': '필터 없음'}`이라고 적혔다 — 필터는 켜져 있었는데.

기본 설정(`use_trend=True`)에서는 추세 워밍업(200봉)이 변동성 워밍업(20봉)을
덮어 새지 않는다. 즉 지금 새는 곳은 아니고 **추세 필터를 끄는 순간 조용히
열린다** — 감사 204와 같은 형태다: *설정을 바꿀 수 있게 만들어 놓고 기본값에만
맞춰 둔 코드는 결함이 예약된 것이다.*

㉞은 "같은 판정을 **두 곳**에서 쓰면 갈라진다"였다. 여기서 배운 것은 그
'두 곳'이 **같은 함수 안**일 수도 있다는 것이다. 파일이 같다고 규칙이
같지 않다.

### ㊿+㊺ **재현성 도구가 자기 때문에 재현이 안 됐다**

감사 207 · `utils/manifest.py`. `data_checksum`은 *"그때 그 샤프가 왜 지금
재현이 안 되지?"*를 추적하려고 있다. 그런데 값을 `repr(v)`로 적었다:

    float      repr → '100.0'
    np.float64 repr → 'np.float64(100.0)'      (numpy 1.x에서는 '100.0')

pandas에서 꺼낸 종가는 `np.float64`다. **numpy를 올리는 것만으로 같은
데이터의 지문이 바뀐다** — 재현 실패를 추적하라고 만든 도구가 "데이터가
바뀌었다"는 거짓 신호를 낸다. 그 도구가 하는 일이 그 판정 하나인데.

이 함수의 독스트링은 **인덱스에 대해서는 그 위험을 이미 알고 있었다**:
*"인덱스는 str()로 넘기면 라이브러리 버전에 따른 repr 차이를 피할 수 있다."*
같은 위험이 종가에도 있는데 거기만 적혀 있었다 — 감사 200과 같은 형태다.

규칙: **지문·해시·서명에 `repr()`을 쓰지 말 것.** `repr`은 사람이 읽으라고
있는 것이고 라이브러리가 마음대로 바꾼다. 저장하거나 비교할 값은 **타입을
정규화해 직접 적을 것.**

(⚠️ `data_checksum`은 지금 아무 데서도 안 불린다 — 배선 안 된 부품이라
live 결함이 아니라 공개 API의 구멍으로 적는다.)

### ㊿+㊻ **바깥에서 온 이름이 안쪽 이름을 덮으면, 기록이 거짓말을 한다**

감사 208 · `utils/logging.py`. JSON 로그 포매터가 `payload.update(ctx)`였다.
그래서 컨텍스트에 `msg`·`time`이라는 **이름**이 있으면 진짜 메시지와 시각이
통째로 바뀌었다. 실측:

    log_event(log, "진짜 메시지", msg="가짜 메시지", time="가짜 시각")
    →  {"time": "가짜 시각", "msg": "가짜 메시지", ...}

로그가 **"언제 무슨 일이 있었는지"를 거짓으로 말한다** — 감사 추적용 기록에서
가장 나쁜 종류의 실패다. 값이 틀린 것보다 나쁘다: 틀린 값은 대조하면 드러나는데,
시각이 바뀌면 무엇과 대조해야 하는지조차 알 수 없다.

특히 고약한 구조: 유일한 호출부가 `**result`로 **임의의 dict를 통째로 펼친다.**
부르는 쪽은 무엇이 예약 키인지 알 수 없고, 하필 `"time"`은 이 저장소 장부가
어디서나 쓰는 이름이다. *바깥에서 온 dict를 안쪽 이름공간에 그대로 펼치는
코드는, 이름이 겹치는 날을 예약해 둔 것이다.*

고침은 덮어쓰기도 버리기도 아니다 — 충돌하는 키만 `ctx_` 접두어를 붙여
**둘 다 남긴다.** 값을 잃는 것도 조용한 손실이기 때문이다.

규칙: 바깥 값을 안쪽 구조에 합칠 때는 **이름 충돌을 먼저 정하고 합칠 것.**
`update()` 한 줄은 "충돌하면 바깥이 이긴다"를 말없이 선택한 것이고, 그건
거의 항상 틀린 선택이다.

(JSON 로깅은 `QUANT_LOG_JSON=1`일 때만 켜지고 지금은 검사에서만 켠다 —
live 결함이 아니라 잠재 구멍으로 적는다.)
