# --- 핵심 (필수) ---
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
scikit-learn>=1.3   # ML 전략(다음봉 방향 예측)

# --- 데이터 소스 (선택: 실데이터 사용 시) ---
ccxt>=4.0          # 암호화폐 거래소 (바이낸스, 업비트 등)
yfinance>=0.2.40   # 미국/국내(.KS/.KQ) 주식 데이터

# --- 국내주식 (선택) ---
# finance-datareader>=0.9.50   # 국내주식 대체 데이터
# pykrx>=1.0                    # KRX 데이터

# --- 시각화 (선택) ---
matplotlib>=3.7

# --- 테스트 ---
pytest>=7.0
