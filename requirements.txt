# 의존성 버전은 '보수적 범위'로 고정한다 — 어느 날 갑자기 메이저 업데이트가
# 설치돼 동작이 바뀌는 사고를 막는다(설치 재현성). 상한은 다음 메이저 직전.
#
# ⚠️ 2026-08-12 감사 130까지 이 파일은 **아무것도 막지 않았다.** 이 파일로
#    설치하는 워크플로가 ci.yml의 audit 잡 하나뿐이었고, 정작 돈을 굴리는
#    daily-paper·nightly-retrain·kr-live는 버전 없이 `pip install pandas …`로
#    그날 PyPI 최신을 받았다. 그래서
#      · 검사는 pandas 3.0.5, 실전은 pandas 2.3.3 — **서로 다른 세계**였고
#      · 한쪽에만 있는 결함은 다른 쪽에서 영원히 안 보였다
#        (감사 127: pandas 3에서만 풀링 학습이 통째로 죽어 있었다)
#      · 장부에는 매일 `lock:…` 해시가 찍혀 '고정돼 있다'는 인상만 줬다
#    지금은 실전 워크플로도 이 파일로 설치하고, tests/
#    test_declared_dependencies_match_reality.py가 **돌고 있는 환경이 이
#    범위 안인지** 확인한다. 범위를 넓힐 때는 넓힌 쪽을 실제로 검증할 것.

# --- 핵심 (필수) ---
pandas>=2.0,<4          # 2.x·3.x 양쪽에서 전체 검사 통과 확인(감사 130)
numpy>=1.24,<3
pyyaml>=6.0,<7
scikit-learn>=1.3,<2   # ML 전략(다음봉 방향 예측)
scipy>=1.10,<2         # HRP 배분(계층 군집화) — sklearn에 딸려 오지만 직접 쓴다

# --- 데이터 소스 (선택: 실데이터 사용 시) ---
ccxt>=4.0,<5             # 암호화폐 거래소 (바이낸스, 업비트 등)
yfinance>=0.2.40,<0.3    # 미국/국내(.KS/.KQ) 주식 데이터

# --- 국내주식 (선택) ---
# finance-datareader>=0.9.50   # 국내주식 대체 데이터
# pykrx>=1.0                    # KRX 데이터

# --- 거래소 달력 (선택: 없으면 요일·시간만 본다) ---
# 한국 공휴일은 음력(설·추석)이라 표에 박아 둘 수 없다. 증권사 API로 받을
# 수도 있지만 그러면 키 없는 환경에서 달력이 통째로 사라지고 그 사실이
# 조용하다 — 오프라인으로 같은 답을 주는 쪽을 골랐다(2026-08-14).
# 없어도 시스템은 예전대로 돈다(그때는 '모른다'고 화면에 밝힌다).
exchange_calendars>=4.5,<5

# --- 시각화 (선택) ---
matplotlib>=3.7,<4

# --- 라이선스 서명 (선택: Ed25519 비대칭 키 사용 시. 없으면 HMAC 폴백) ---
# cryptography>=41,<46

# --- 테스트 ---
pytest>=7.0,<10
