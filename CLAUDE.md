# CLAUDE.md

## 이 저장소

주식·코인 **퀀트 자동투자 시스템**(`quant/`). 저장소 이름(`insta-story-downloader`)은
내용과 무관합니다 — 실제 제품은 퀀트 트레이딩 프레임워크입니다.

전체 설명은 `README.md`를 보세요.

---

## 📌 Notion 동기화 규약 (필수)

이 제품은 Notion **유어팀 워크스페이스**에서 관리됩니다.
**구현이 완료될 때마다 Notion을 업데이트합니다.** 사용자가 매번 요청하지 않아도 수행하세요.

### ⚠️ 독자는 비개발자입니다 (가장 중요)

제품 페이지는 **신입 기획자 온보딩 문서**입니다. 투자·개발 지식이 없는 사람이 읽습니다.

- **전문용어를 쓰면 그 자리에서 풀어 씁니다.** 처음 등장하는 용어는 괄호나 한 줄 설명을 붙이세요.
- **코드 경로·함수명·환경변수는 본문에 넣지 않습니다.** 꼭 필요하면 `<details>` 접이식 안으로.
- 기능이 아니라 **"그래서 사용자에게 뭐가 달라지는가"**를 씁니다.
- 기존 문서의 톤(비유로 설명, 정직한 한계 병기)을 유지하세요.

### 대상 페이지 (검색하지 말고 아래 ID를 직접 사용)

| 대상 | ID / URL |
|---|---|
| 제품 페이지 (마스터 문서) | `3bb0adee-652b-8170-9435-d408a474b611` |
| 개발 및 제작 관리 DB (작업 항목) | `collection://2e30adee-652b-810e-8f87-000b403bf909` |
| 제품 목록 DB (제품 행) | `collection://2e30adee-652b-8145-a525-000bec085d5a` |
| 유어팀 메인페이지 | `2e30adee-652b-81bd-9ba4-df065fe6f39e` |
| 제품 & 서비스 페이지 | `2e30adee-652b-81a8-8af2-dfba625acb93` |

제품 페이지는 메인페이지의 **제품** 섹션과 제품 & 서비스 페이지의
**제품/서비스 목록**에 자동으로 함께 노출됩니다(같은 DB를 보는 뷰).

### 무엇을 언제 업데이트하는가

**① 기능 구현·버그 수정을 커밋할 때마다 — 제품 페이지 "13. 개발 업데이트 로그"**

`notion-update-page`의 `update_content` 명령으로, `## 13. 개발 업데이트 로그` 아래
콜아웃 **바로 다음**(= 기존 최상단 `<details>` 앞)에 새 항목을 끼워 넣습니다.
로그는 **최신이 위**입니다.

```
<details color="gray_bg">
<summary>**YYYY-MM-DD · 제목** — 한 줄 요약</summary>
	- 무엇이 바뀌었는지 (사용자 관점으로)
	- 왜 바꿨는지 / 무엇을 막았는지
</details>
```

**② 작업 단위가 새로 생기거나 끝날 때 — 개발 및 제작 관리 DB**

- 새 작업: `notion-create-pages`로 행 추가. `제품` 관계를 위 제품 페이지 ID로 연결.
- 완료: 해당 행의 `상태`를 `완료`로 (`시작 전` → `진행 중` → `완료`).
- 상태 값은 정확히 이 세 가지만 유효합니다.

**③ 현황 숫자가 바뀌었을 때 — 제품 페이지 "9. 현황 스냅샷"**

버전(`VERSION`), 테스트 파일 수, 운영 종목 수, 챔피언 설정(`state/champions.json`),
8마일 챌린지 잔고(`docs/social/latest.json`)가 바뀌면 표의 해당 행만 갱신합니다.
7장(8마일 챌린지)에도 같은 잔고 숫자가 있으니 함께 맞춥니다.

**④ 로드맵 항목이 끝났을 때 — 제품 페이지 "11. 로드맵"**

`### 남은 것`의 `- [ ]` 항목을 `### 완료`의 `- [x]`로 옮깁니다.

**⑤ 판매 상태가 바뀌면 — 제품 목록 DB의 `상태`**

`준비 중` → `판매 중` → `종료`. 실거래 개시나 판매 개시 시점에만 변경하며,
**변경 전 사용자에게 확인**하세요(법률 검토 항목과 얽혀 있습니다).

### 규칙

- **숫자는 저장소에서 확인한 값만** 씁니다. 추정치를 사실처럼 적지 않습니다.
- 문서 오타 수정, 포매팅 등 **사용자에게 보이지 않는 변경은 로그하지 않습니다.**
- '수익 보장'류 표현은 Notion에도 절대 쓰지 않습니다(사기죄 소지).
- 새 용어가 문서에 등장하면 **5. 용어 사전에도 추가**합니다. 사전에 없는 용어를 본문에서
  설명 없이 쓰면 온보딩 문서로서 실패입니다.
- 성과가 나쁜 기간도 **그대로 적습니다.** 좋은 숫자만 남기는 편집은 이 제품의 정체성
  (선택 편향 없는 공개 실험)을 무너뜨립니다.
- Notion MCP가 연결돼 있지 않으면 **작업을 중단하지 말고**, 커밋은 정상 진행한 뒤
  "Notion 업데이트는 MCP 미연결로 보류됨"을 사용자에게 알립니다.

---

## 📌 투자 로직은 **기계가 개선한다** (2026-08-27 사장님 지시)

> *"투자 로직의 경우에는 머신러닝으로 개선을 계속할 수 있게끔 해야지
> 너가 수동으로 고치는 방향 말고."*

### 이미 기계가 하고 있는 것 (저장소에서 확인한 값)

- **운용 중인 40종목 중 29종목이 ML**이다(2026-09-07 실측). 나머지 11종목은
  규칙 전략이다 — `ma_cross`(BNB·ETH·EWJ) · `cross_rank`(BTC) ·
  `buy_hold`(SOL) · `breakout`(XRP·051910.KS·228790.KS) · `turtle`(QQQ·SPY) ·
  `psar`(MSFT).

  ⚠️ **이 숫자는 계속 낡는다 — 오디션이 챔피언을 바꾸기 때문이다.**
  여기 적힌 값이 이미 **두 번** 틀렸다: 한때 "38종목이 ML"이었고(2026-09-03에
  정정), 그다음 "32종목이 ML"이었다(오늘 정정 — 실제는 29). 그러니 이 문장을
  근거로 설계하지 말고 **매번 다시 세라**:

  ```bash
  python -c "import json,collections;c=json.load(open('state/champions.json'));u={f'{m}:{s}' for m,s in json.load(open('state/universe.json'))['targets']};k=collections.Counter((v.get('spec') or v)['strategy'] for x,v in c.items() if x in u);print(k)"
  ```

  이 구별은 장식이 아니다: 규칙 전략은 **음수 신호를 아예 못 낸다**. 그래서
  아래 방향 관문의 모집단이 40이 아니라 29이고, **코인만 보면 5종목 중 0종목**
  이다(2026-09-07 — 코인 다섯이 전부 규칙 전략이 됐다). 그 사실을 몰라 첫
  설계를 코인 안에 가뒀다가 처음부터 판정 불가인 장치를 만들 뻔했다
  (2026-09-03). 지금은 그 설계였다면 **정말로** 판정 불가였을 것이다.
  ⚠️ `state/champions.json`은 42개인데 그중 둘은 **은퇴한 챔피언**이다(META·TSLA — 2026-08-22 시총 회전에서 유니버스를 떠났다). 파일 길이를 운용 종목 수로 세면 안 된다 — 내가 2026-08-27에 그렇게 잘못 셌다.
- ML 챔피언은 워크포워드로 **스스로 재학습**한다(`retrain_every` 기본 20봉).
- 밤 배치가 매일 **오디션을 돌려 자동 승격**한다. 누적 시행 11,721회, 승격 2회.
  승격은 2단계 관문(선발전 → 결승전)을 통과할 때만 일어난다.
- 후보도 일부는 **기계가 만든다**: `mutate_champion()`이 매일 밤 챔피언 주변을
  변형한 후보를 만들어 링에 세운다(언덕오르기, 날짜 시드로 결정적).
- 다중검정 보정도 **이미 걸려 있다**: 결승 문턱이 최근 시도 수에 로그 비례해
  올라간다(`confirm_threshold`). 후보를 많이 뽑을수록 관문이 저절로 빡빡해진다.

즉 "누가 이길지"도, "챔피언 주변 어디를 더 파 볼지"도 이미 기계가 정한다.

### 탐색 축은 이제 **표**다 — 그리고 스스로 넓어진다 (2026-08-27 구현)

예전에는 `mutate_champion()`이 `if axis == "model": … elif …` 사슬이었다.
그러면 **기계가 자기 탐색 공간을 읽을 수 없다.** 손잡이를 새로 만들 때마다
사람이 사슬에 가지를 쳐야 했고, 잊으면 그 축은 영원히 탐색되지 않는데
**아무 데도 빨간불이 안 떴다.**

실제로 그 상태였다(실측): 살아 있는 ML 챔피언 38종목 중 **6종목이 언덕오르기가
못 건드리는 손잡이 위에 앉아 있었다** — `meta` 3종목, `pool` 3종목. 고정 격자가
그 설정을 한 번 승격시키고 나면, 그 뒤로 "빼 보면 더 나은가"를 아무도 안 물었다.

이제 사슬을 표로 바꿨다(`ML_EXPLICIT_AXES`) 그리고 표에 없는 손잡이는
**관측에서 자동 유도한다**(`ml_search_axes`). 격자나 현재 챔피언에 새 손잡이가
나타나는 그 순간부터 언덕오르기가 그 축을 흔든다 — 사람이 기억할 필요가 없다.

- 명시 축(범위를 사람이 정하는 게 나은 것): `model` · `threshold` ·
  `train_window` · `retrain_every` · `label` · `sizing` · `sample_weight` ·
  `calibrate` · `derive`
- 관측 유도 축(자동으로 들어온 것): `pool` · `meta` · `top_features` ·
  `label_cost`
- 유도 축의 표본에는 **빼 보기**가 항상 들어간다. 더하는 것만 탐색하고 빼는
  것은 안 하면, 한번 붙은 설정은 영영 안 떨어진다.

⚠️ 이것은 후보 **수**를 늘리지 않는다(하루 4개 그대로). 넓어지는 것은 공간이지
시행 횟수가 아니므로 다중검정 부담이 늘지 않는다.

### 피처도 기계가 만든다 — 단, **조합**만이다 (2026-08-27 구현)

`quant/strategies/derive.py` — 이미 df에 있는 열끼리 곱·비·이동표준화·
이동순위·차분·지연으로 **새 피처를 지어낸다**. 언덕오르기의 `derive` 축이
그 목록을 매일 밤 한 걸음 흔든다(더하기·빼기·바꾸기).

⚠️ **자동화되는 것은 조합이지 재료가 아니다.**

- ⭕ **조합** — 있는 열을 섞는 일. 규칙이라 기계가 만들 수 있다.
- ❌ **재료** — VIX·펀딩비·외국인 수급 같은 **새 외부 데이터**. 기계는
  세상에 없는 데이터를 상상해 낼 수 없다. 새 소스를 붙이는 일은 사람이 한다.

⚠️ 조합 문법이 미래 값을 한 칸이라도 참조하면, 그 피처를 쓴 모델은
백테스트에서 정직하게 좋아 보이고 **오디션의 모든 관문을 통과한다** —
관문은 성적을 보지 데이터의 출처를 보지 않는다. 그래서 문법의 모든 연산자에
대해 "미래 행을 잘라도 과거 값이 안 바뀌는가"를 **행동으로** 검사한다.

한 설정이 들고 갈 수 있는 조합 피처는 `MAX_DERIVED`개까지다. 상한이 없으면
언덕오르기가 매일 하나씩 붙여 수십 개짜리 설정이 되고, 그 대가는 성적이
아니라 **판정의 신뢰도**에서 지불된다. 그리고 하루 후보 수는 그대로다 —
넓어지는 것은 공간이지 시행 횟수가 아니다.

### 언덕오르기의 헛수고를 없앴다 (2026-08-27 장부 실측)

밤마다 챔피언 주변을 흔들어 후보 **4개**를 만드는데, 실측해 보니 그중
**16.8%가 챔피언과 하는 일이 완전히 같았다**(실제 챔피언 × 14일 × 4개 =
2,240개 중 376개).

원인은 중복 판정을 **글자 그대로** 했기 때문이다. `sample_weight`의 기본값이
`None`이라 `{...}`와 `{..., "sample_weight": null}`은 **하는 일이 같은데**
글자가 달라 '새 후보'로 통과했다(`calibrate: null` · `label: "nextbar"` ·
`sizing: "proba"`도 같은 모양 — 축이 "이미 기본 위치인 손잡이를 기본
위치로 돌린" 경우다).

이제 전략 **생성자 기본값으로 정규화**한 뒤 중복을 판정한다(기본값은 손으로
적은 표가 아니라 클래스에게 직접 묻는다). 실측 16.8% → **0%**.

⚠️ 이건 성적을 올리는 장치가 아니라 **탐색 예산을 되찾는** 것이다. 후보
개수는 그대로다(검사가 못 박는다) — 개수가 늘면 다중검정 부담이 커져 이득의
일부를 관문 헐거워짐으로 돌려주게 된다.

⚠️ 고치면 그날의 링 구성이 달라지므로, 장부에 `challenger_version`을 남기고
재현 검증은 **그 기록이 적힌 세대의 규칙으로** 재생한다(`gate_version` ·
`select_folds`와 같은 방식). 과거 기록은 고치지 않는다.

### ⚠️ 링에는 **두 모양**이 섞여 있다 — 이걸 잊으면 장치가 조용히 죽는다

    온전형   {"strategy": "ml", "params": {"model": "gb", "pool": "peers"}}
    덧씌우기 {"model": "gb", "pool": "peers"}      ← 챔피언 위에 얹힌다

고정 격자의 ML 항목은 **덧씌우기형**이라 `params` 키가 아예 없다. 후보의
손잡이를 물을 때는 반드시 `effective_params(cand, champion)`을 쓴다 —
`cand["params"]`만 보면 덧씌우기형은 **언제나 빈 dict**로 보인다.

실제로 그 착각이 안전장치 하나를 통째로 잠재웠다(2026-08-27 장부 실측):
감사 297의 '잠든 후보 제외'가 `c.get("params")`만 봐서, **지키려던 바로 그
후보들**(고정 격자의 `pool="peers"`)을 한 번도 못 걸렀다. 장치를 붙인
2026-08-20 이후에도 그 후보는 매일 밤 거의 모든 종목에서 무동작으로 잡혔다
(8/26 32종목 중 30). 고장 난 안전장치와 조용한 실패가 서로를 가려 줬고,
화면에는 아무 빨간불도 안 떴다.

### 아직 사람이 하고 있는 것

- `DEFAULT_CHALLENGERS` 고정 격자 — 여전히 손으로 유지한다. 다만 이제 이
  격자는 **탐색 축의 씨앗**이기도 하다. 여기 한 줄을 적으면 그 손잡이가
  자동으로 탐색 대상이 된다.
- 피처의 **재료** — 새 외부 데이터 소스(`fs1`~`fs8`의 묶음)는 매번 손으로
  붙인다. 조합은 기계가 만들지만 재료는 아니다(위 참조).
- 가설 우선 후보(월말 수급·펀딩 과열 회피·PEAD 등) — 조사해서 손으로 쓴 규칙.

### 앞으로의 방침

- 투자 로직을 바꿔야겠다고 생각되면 **그 규칙을 직접 쓰지 않는다.**
  대신 **기계가 그 후보를 스스로 만들어 링에 올리도록** 만든다.
  손잡이 하나가 아니라 **손잡이를 만드는 장치**를 짠다.
- 손으로 링에 세우는 것은 **기계가 아직 만들 수 없는 종류**에 한한다.
  그때도 왜 자동 생성이 불가능한지를 기록에 남긴다.
- 화면·문구·배선·안전장치를 손으로 고치는 것은 이 방침과 무관하다.
  **이 방침은 '무엇을 사고파는가'를 정하는 로직에만 적용된다.**

### 수익은 수수료 뺀 뒤만 수익이다 — 그리고 회전은 **기계가 고른다** (2026-09-02 사장님 지시)

> *"각 수수료도 고려해서 수익을 생각해야지.. 이건 선물 말고도 다른 모든 투자
> 마찬가지야. 이런 것도 너가 할 게 아니라 머신러닝 차원에서 알아서 해야지."*

계기는 선물 실험이었다(실측): 열흘 −4.17% 중 수수료가 5.47%p — 전략 자체는
+1.42%였고 **손실의 131%가 수수료**였다. 그 수수료의 81%는 레버리지 관문
(8/26) 이전 사흘에 나갔다(하루 35~42회 리밸런싱). 그리고 최근 7일은 전략도
잃었다(총이득 −146 · 수수료 78).

전 트랙을 훑은 결과(2026-09-02 실측):

| 트랙 | 그때까지 | 지금 |
|---|---|---|
| 본 계좌(100만) · 오디션 · 장중 트랙 | 시장별 실측 비용 | 그대로 |
| **밤 검증 3종(DSR·PBO·CPCV)** | **비용 없음**(옵션 자체가 없었다) | 실측 비용, 기준을 기록에 남김 |
| 그림자 계좌 넷 | 시장 무관 10bp 고정 | 종목 시장의 실측 비용 |
| 선물 실험 | 프리셋 | 같은 실측 모델 |

**규칙 두 개:**

1. **모든 트랙은 비용 기준 하나를 쓴다** — `measured_cost_model(market)`.
   공개 자료(status·futures·그림자·검증)에는 `cost_basis`가 실린다. 없으면
   검사가 실패한다(`tests/test_every_return_carries_its_cost_basis.py`).
   새 트랙을 만들 때 수수료를 빠뜨리면 사람이 기억하지 않아도 빨간불이 뜬다.
2. **회전은 사람이 정하지 않는다.** 리밸런스 밴드는 그때까지 시장별 고정값
   하나라 **후보마다 같았다** — 비용은 물리고 있었지만 어떤 후보도 "덜 사고
   팔면 더 남는가"를 물어볼 수 없었다. 이제 밴드 배수(`band_mult` ∈ {0 ·
   0.5 · 1 · 2})가 언덕오르기의 탐색 축이다. 챔피언은 자기 배수로, 후보는
   자기 배수로 돌아 **같은 비용 아래서** 겨루고, 승격된 배수는 실거래
   체결기가 그대로 따른다(`_champion_band_rel`). 생성자가 모르는 실행 전용
   손잡이라 `build_strategy`가 빼고 넘기고, 중복 정규화는
   `EXEC_PARAM_DEFAULTS`로 한다(`band_mult: 1.0`은 안 적은 것과 같다).

⚠️ 후보 **수**는 그대로다(하루 4개). 넓어진 것은 공간이지 시행 횟수가
아니다 — 다중검정 부담이 늘지 않는다.

⚠️ 정직하게 — 비용을 넣어도 오늘 검증 판정이 뒤집히는 종목은 없다(비용
전에도 통과 0종목, DSR 최고 0.21 vs 기준 0.95). 고친 것은 "지금 틀린 판정"이
아니라 **"언젠가 통과하는 날 그 통과가 수수료 빼기 전 성적일 수 있다"**는
것과, 화면의 검증 숫자가 실제보다 후했다는 것이다.

### 선물 실험도 회전을 스스로 고른다 — 그리고 제안 ②는 **기준을 바꿔** 넣었다 (2026-09-02)

사장님 "모두 다 해"로 보류해 두었던 수수료 예산 관문을 착수했다. 그런데 원안
그대로는 넣을 수 없었다 — **장부로 재 보니 관문 구실을 못 한다.**

원안: "최근 7일 수수료가 **총이득의 50%**를 넘으면 리밸런싱 빈도 축소."

| 막히는 자리 | 실측 |
|---|---|
| 총이득이 **음수**면 비율이 정의되지 않는다 | 9/1·9/2의 7일 창이 실제로 −129·−153 |
| 양수인 날도 8/25 이후 **내내 150% 초과** | 443/291 · 485/309 · 500/218 · 509/123 |

가장 걱정되는 상태(잃고 있는 주)에서 하필 침묵하고, 나머지 날은 언제나 참이다.
**언제나 참인 조건은 관문이 아니라 상시 상태다.** 그래서 재는 대상을
**자산 대비**로 바꿨다 — 총이득의 부호와 무관하게 언제나 정의된다.

문턱 3%의 근거(같은 장부, 7일 창 수수료/자산): 폭주 사흘이 창에 있던
8/25~8/30이 **3.13~5.26%**, 레버리지 관문(8/26) 이후 회복기가 **2.33% → 1.00%**.
즉 폭주기를 잡고 회복기는 안 잡는 자리다. 연 환산 약 156% — 어떤 전략도
수수료로 연 150%를 내고 살아남지 못한다. 최적화한 손잡이가 아니라 **"명백히
병적인 상태"의 문턱**이고, 그래서 사람이 값을 적는 것이 방침에 어긋나지 않는다
(청산·레버리지 관문과 같은 안전장치다).

⚠️ **넘겨도 줄이는 거래는 막지 않는다.** 위험을 줄이는 길을 막는 안전장치는
그 자체가 위험이다(레버리지 관문과 같은 규약).

**그리고 근본 처방은 따로 있다 — 선물에는 밴드가 아예 없었다.**
본 계좌·장중은 비용 비례 밴드로 도는데 선물만 절대 금액 문턱(10 USDT 또는
자산의 0.5%)뿐이라, **같은 챔피언이 트랙마다 다른 회전으로 돌았다.** 이제
선물도 `champion_band_rel()` = 코인 실측 비용 밴드 × **그 종목 챔피언의
밴드 배수**로 돈다. 사람이 값을 정하는 자리가 없다 — 배수는 밤 오디션이 고른다.

⚠️ **정직하게 — 밴드는 수수료 처방으로는 작다**(실측): 밴드 0.15에서 체결이
**29% 줄지만 수수료는 3.2%만** 준다. 수수료의 **56%가 손익확정을 동반한
거래**(포지션을 뒤집거나 크게 줄이는 것)에서 나오고, 상위 25건이 40%다 —
전부 8/23~25 폭주 사흘이다. 그 폭주는 **레버리지 관문이 이미 잡았다**
(하루 수수료 243 → 8~14). 그러니 오늘 넣은 두 장치는 "지금 새는 것을 막는
것"이 아니라 ① 트랙 간 회전 규율을 맞추는 것과 ② 폭주가 다시 오면 잡는
것이다. **오늘 예산은 안 걸린다**(0.74% < 3%).

⚠️ **청산(목표 0)은 밴드와 무관하게 항상 실행한다** — 백테스트 엔진과 같은
규약이다. 갈리면 링에서 이긴 회전이 실계좌에서 재현되지 않고, 관망으로 바뀐
종목의 잔여 포지션이 영영 남는다.

⚠️ **밴드는 작은 조정만 거르는 것이 아니라 작은 신호의 진입도 막는다.**
실측(8/26 이후 신호 350개): 64.3%가 관망(0)이고, **0이 아닌 신호 125개 중
18.4%가 밴드 0.15 미만**이라 그 자리는 아예 포지션을 못 잡는다. 검사 하나가
그 사실을 드러내 줬다(확신도 눈금 검사의 대조군이 통째로 0이 됐다). 이건
고장이 아니라 밴드의 정의 그대로이지만, "작은 헛거래를 거른다"고만 적으면
절반만 말한 것이다.

### 하락장 대응 — 세 갈래를 다 재 봤다, 그리고 **비교 대상이 없었다** (2026-09-03)

> 사장님: *"하락장에선 대응 여력없이 수익을 못내네..? 특히 선물은 롱숏
> 포지션 다 가능한데 더 손해가 커."* → *"모두 봐야지"*

세 갈래를 전부 쟀다. 결론부터 — **셋 중 둘은 실측이 반대 방향을 가리켰고,
정작 진짜 구멍은 넷째 자리에 있었다.**

**① 하락에서 버는 자산을 담는다 → 실측이 내 제안을 뒤집었다**

유니버스에 이미 금·채권·달러·물가채·원자재·유틸리티가 있다. SPY가 −1% 이상
빠진 **78일** 동안 그것들이 실제로 어떻게 움직였나(스냅샷 실측):

| 자산 | 그날 평균 | 오른 날 | SPY와의 상관 |
|---|---|---|---|
| UUP(달러) | **+0.05%** | 63% | **−0.11** |
| IEF(중기채) | −0.04% | 44% | +0.10 |
| GLD(금) | −0.26% | 37% | +0.18 |
| XLU(유틸) | −0.57% | 33% | +0.32 |
| VNQ(리츠) | −1.01% | 19% | +0.52 |
| SLV(은) | −1.36% | 35% | +0.28 |

**주식이 빠지는 날 같이 오르는 것이 사실상 없다.** 상관이 음수인 것은 달러
하나이고 그마저 평균 +0.05%다. 금은 **−2% 이상 빠진 14일**에서만 +0.21%로
버틴다. 그러니 "방어자산을 담자"는 내 앞선 제안은 **실측이 뒷받침하지 않는다** —
담을 자산은 이미 다 담겨 있고, 담겨 있어도 하락을 안 막아 준다.

**② 본 계좌에 숏을 연다 → 제도가 막는다(코드가 아니라)**

100만원 계좌는 **현물**이다. 한국 개인의 대주, 국내 증권사 해외주식 공매도,
코인 현물 숏 — 셋 다 이 규모에서 재현 가능한 매매가 아니다. **재현할 수 없는
매매를 페이퍼 장부에 적는 것**이 이 제품에서 가장 비싼 거짓말이라, 여기는
열지 않는다. 숏이 실제로 가능한 곳은 **선물 트랙 하나**다.

**③ 현금이 놀지 않게 한다 → 작지만 사실이다**

본 계좌의 순노출은 최근 13영업일 평균 **33.6%**다(최저 12.8% · 최고 49.6%).
즉 **평균 66%가 0%로 놀고 있다.** 다만 그것은 고장이 아니라 신호가 관망이라
그런 것이고, 지금 계좌에 넣을 수 있는 "단기 파킹 상품"이 유니버스에 없다
(그건 조합이 아니라 **재료**라 사람이 붙여야 한다). 이번 라운드에서는 **안
건드렸다** — 실제로 받는 이자율의 근거 없이 숫자를 넣으면 수익률을 지어내는
일이 되고, 그건 이 저장소의 첫 번째 규칙을 어긴다.

**④ 그리고 진짜 구멍 — 선물 트랙에 비교 대상이 아예 없었다**

선물 모듈 첫머리는 이렇게 약속하고 있었다: *"믿지 않고 **나란히 돌려서 잰다** —
같은 규칙, 같은 비용, 같은 종목, 방향 허용 여부만 다르게."*
**나란히 도는 반대쪽이 없었다.** `build_two_sided()`가 챔피언 위에 방향 허용을
켜서 그 한 팔만 굴렸고, 같은 챔피언이 롱 전용이었으면 얼마였을지는 아무도
계산한 적이 없다. 열흘 −4.17%가 나온 뒤에도 "숏 때문인가"에 답할 근거가
장부에 **없었다.** 화면에는 롱 포지션 수와 숏 포지션 수가 나란히 떠 있어서,
마치 둘을 비교하고 있는 것처럼 보였다.

장부로 재 보면 숏은 범인이 아니다(실측):

| | 총이득 | 수수료 | 순 |
|---|---|---|---|
| 롱 | +256.5 | 432.6 | −176.1 |
| 숏 | −147.3 | 127.4 | −274.7 |

그런데 숏의 −147.3 중 **8/24 하루가 −197.8**이고 **나머지 날을 합치면
+50.5**다. 그날은 레버리지 1.2~2.4배로 XRP를 +2,867 → −4,028 → +3,941로
몇 시간 만에 뒤집던 날이고, **그 폭주는 8/26 레버리지 관문이 이미 잡았다.**
즉 "숏이라서 잃은 것"이 아니라 "관문 없이 크게 굴려서 잃은 것"이다.
⚠️ 그렇다고 숏이 좋다는 증거도 없다 — 숏 신호는 전체 **93회**뿐이고
관문 이후로는 **15회**다. 이 표본으로는 어느 쪽도 말할 수 없다.

### 방향(숏 허용)도 **기계가 판정한다** — `direction_gate` (2026-09-03 구현)

방침대로 손으로 "숏을 끄자"고 정하지 않는다. **그 손잡이를 판정하는 장치**를
놓는다. 밤 오디션이 종목을 도는 김에 각 종목에서 **'방향 허용 − 롱 전용'**을
한 번 더 재생하고, 날짜별 횡단 평균으로 패널 관문과 **같은 자**로 판정한다.

- 판정이 **유의하게 나쁠 때만**(t < −문턱) 선물 트랙이 롱 전용으로 돈다.
  판정이 없거나 유의하지 않으면 지금까지처럼 양방향이다 — **못 잰 것과 나쁜
  것은 다른 사건**이고, "아직 모른다"는 "끄라"가 아니다.
- 문턱은 **패널 관문에서 빌려 온다**(`PANEL_T_REF`). 여기서 따로 정하면
  관문이 아니라 편의다.
- 판정은 7밤이 지나면 안 쓴다 — 그 사이 챔피언이 바뀌면 옛 판정은 **다른
  규칙에 대한 판정**이다.
- 비용은 추가 백테스트 **1회/종목**뿐이다. `holdout_diffs`가 챔피언을 한 번만
  재생하고 후보마다 차이를 내므로, 이미 도는 재생에 한 줄을 얹는 것이다.

**⚠️ 처음 설계는 코인 안에 갇혀 있었고, 그 상태로는 처음부터 죽어 있었다.**
선물 트랙 안에서 코인 5종목만 재게 만들었다가 스냅샷으로 돌려 보고 알았다 —
코인 5종목 중 **숏을 낼 수 있는 챔피언은 2개**뿐이다(BTC·ETH·BNB는 규칙
전략이라 음수 신호를 아예 안 낸다). 패널 최소 종목 수는 5이므로 **영원히
판정 불가**다. 장부에 "재료 부족"만 매일 쌓이고 관문은 한 번도 안 열리는,
이 저장소가 가장 싫어하는 종류의 조용한 죽음이다. 질문 자체가 코인 것이
아니다("모델의 내림 예측에 정보가 있는가")—그래서 **운용 ML 챔피언 전부**로
모집단을 넓혔다.

**첫 판정이 이미 나왔다**(2026-09-02 스냅샷 · ML 챔피언 15종목 · 122일):

| | 패널 | 종목별(같은 자로 병기) |
|---|---|---|
| 점수 | t = **−0.26** (문턱 ±1.35) | 중앙 t **−0.376** |
| 판정 | 불통과 → **양방향 유지** | **0/15** 통과 |
| 부호 | 15종목 중 4종목만 양수(26.7%) | — |

평균 차이는 하루 **−0.0036%**(연 약 −0.9%)다. 즉 **내림에 거는 것은 지금
약간 손해지만 우연과 구별되지 않는다.** 그래서 관문은 아무것도 막지 않고,
실험은 계속된다 — 이것이 이 장치가 하기로 한 일 그대로다.

⚠️ **정직하게 — 이 비교에 펀딩비는 안 들어 있다.** 비용 기준은 다른 모든
트랙과 같은 하나(`measured_cost_model`)이고 거기에 **숏 차입료는 들어 있지만**
펀딩은 없다. 펀딩은 부호가 방향마다 반대라(양수 요율이면 롱이 내고 숏이 받는다)
대칭으로 물리면 숏이 부당하게 불리해지고, 실데이터 계열 없이 부호를 넣으면
숫자를 지어내는 일이 된다. 그래서 **빼고 재고, `funding_excluded`로 적는다.**
방향은 분명하다 — 이 관문은 **숏에 보수적인 쪽**이다.

⚠️ **본 계좌는 이 관문이 안 건드린다.** 위 ②의 이유로 100만원 계좌는 숏을
열지 않는다. 여기서 "숏이 좋다"가 나와도 마찬가지다.

### 조기 판정이 **두 계좌의 자산을 이어 붙인 수익률**을 쓰고 있었다 (2026-09-07)

그림자 장부의 계좌 섞임(위)을 고친 뒤, **그 장부를 읽는 쪽**도 확인했다.

`paired_daily_returns`의 `_series`는 *"하루 여러 회차면 마지막 값"*을 쓴다.
그런데 오염된 줄은 **더 과거 날짜인데 뒤에 적혀 있다.** 그래서 그 날짜의
자산이 **다른 계좌의 값**이 되고, 앞뒤 날과 이어 붙인 일수익은 두 계좌의
자산을 엮은 **지어낸 수익률**이 된다.

**실측**: 배분 사다리 짝비교 **16개 관측 중 2개**가 그랬다(최대 8.3bp).
누적합은 6번째 자리에서만 바뀌고 **오늘 판정은 안 바뀐다**(셋 다 표본 부족).

⚠️ **그래도 고친 이유는 날짜다.** 이 실험의 사전 등록 판정일은
**2026-12-17**이고, 보관 창이 400일이라 그 두 줄은 **판정일에도 창 안에
남아 있다.** 즉 고치지 않으면 사전 등록된 판정이 지어낸 수익률 위에서 난다.

**고친 것 — 뒤로 간 줄은 건너뛴다.**

⚠️ **같은 날짜의 여러 회차는 정상이다.** 장중 트랙은 하루에 수십 번 돈다 —
부등호가 `<=`가 되면 그 회차들이 통째로 날아간다. 뒤로 **간** 것만 사고다.

⚠️ 여기서 변이 하나를 잘못 짰다가 배웠다: `high = day`로 바꾸는 변이는 그
줄에 닿는 순간 이미 `day >= high`라 **동작이 같다** — 나쁜 변이지 검사
구멍이 아니었다. 진짜 함정은 부등호였다. (오늘만 세 번째다.)

■ 오늘 넓힌 교훈 — **장부를 고쳤으면 그 장부를 읽는 쪽도 전부 본다**

쓰는 쪽을 고쳐도 이미 들어온 줄은 남는다(옛 기록은 고치지 않으므로). 그러니
그 줄을 읽는 자리마다 **쓴 순서와 봉 순서가 갈릴 수 있다**는 사실을 알아야
한다. 오늘 그 자리가 셋이었다 — 화면의 대표 숫자(`latest_row`), 표본 일수
(`distinct_days`), 그리고 **판정의 수익률**(여기).

### 장중 트랙 둘이 **비용 기준을 공개 자료에 안 싣고 있었다** (2026-09-07)

2026-09-02 규칙은 이렇게 적혀 있다 — *"모든 트랙은 비용 기준 하나를 쓴다.
공개 자료에는 `cost_basis`가 실린다. **없으면 검사가 실패한다.**"*

훑어보니 **장중 트랙 둘(코인·미국)에는 한 번도 실린 적이 없었다**(실측:
`docs/intraday.json` · `docs/intraday_us.json` 둘 다 `cost_basis` 없음).

⚠️ **돈은 제대로 물리고 있었다** — 두 트랙 다 `measured_cost_model`로 매
체결마다 뗀다. 빠진 것은 **요율**이다. 그래서 읽는 사람은 `cost_paid`(낸
금액)만 보고 그 값이 실측 요율에서 나온 것인지 프리셋인지 알 수 없었고,
나중에 누가 프리셋으로 바꿔도 아무 빨간불이 안 뜬다.

**왜 안 걸렸나 — 계약 검사가 트랙을 손으로 나열하고 있었다.**

그 검사는 `gen2` · `unshackled` · `alloc_ladder` · `diversity` · `futures` ·
본 계좌를 하나씩 적어 두었고, 장중 둘은 목록 밖이었다. 이 저장소가 반복해서
배운 것 그대로다 — **명단을 손으로 유지하면 잊는다**(고정 격자, 반려 장부,
탐색 축 사슬… 전부 같은 얼굴이었다).

**고친 것**

- 두 장중 트랙의 공개 자료가 `cost_basis_bp`를 싣는다. 화면에도 낸 금액
  옆에 **편도 요율**을 함께 적는다(금액만 있으면 반쪽이다).
- 계약 검사가 명단을 유지하는 대신 **자기 문서를 내는 트랙을 센다** —
  `quant/live/*.py`에서 `write_public_report`를 가진 모듈을 걷어 등록
  목록과 대조한다. 새 트랙이 생기면 "등록되지 않은 트랙"이라고 먼저 말한다.

⚠️ **오늘 성적은 한 칸도 안 바뀐다** — 물리던 비용이 그대로다. 고친 것은
**읽는 사람이 그 요율을 확인할 수 있는가**와, 다음에 누가 빠뜨렸을 때
빨간불이 뜨는가다.

⚠️ `docs/risk.json`은 예외로 **그대로 둔다** — 그 자료는 스스로 *"거래
비용·슬리피지 미반영 — 낙폭·변동성 측정 목적. 수익 배수를 성과로 읽지 말
것"*이라고 적고 있다. 정직하게 선언한 예외는 누락이 아니다.

### 계측기가 **31봉짜리 재료를 '붙었다'고 말하고 있었다** (2026-09-07)

수집 라운드에서 후보 하나(강제청산 소진 되돌림)를 재려고 미결제약정의 깊이를
세다가 나왔다. 그 후보는 재료가 짧아 반려됐는데, **짧다는 사실 자체가 어디에도
안 적히고 있었다.**

`optional_features_from_df`는 **열이 있는가**만 본다(`"oi" in cols`). 그래서
열은 살아 있지만 대부분의 봉이 비어 있는 재료도 "붙었다"로 세어졌다
(2026-09-07 스냅샷 · 코인 5종목 × 800봉):

| 재료 | 채움률 |
|---|---|
| `x_funding` · `x_fng` · `x_usd_chg5` · `x_hy_spread` | 100.0% |
| `x_btc_ret5` | 99.4% |
| `x_kimchi` | **25.0%** (200봉) |
| `x_oi_chg5` | **3.9%** (31봉) |

그날 밤 XRP의 장부에는 `features_used`에 둘 다 실렸고, 유실 경보는 열이 통째로
없는 것(`x_frgn5,x_inst5`)만 말했다.

**그리고 학습은 `fillna(0.0)`이다.** 즉 나머지 769봉에 대해 모델은
**"미결제약정이 안 변했다"**는 구체적 주장을 배운다 — 못 받은 것과 0은 다른
사건인데 이 자리에서 같아진다.

⚠️ 이 모듈의 첫머리가 같은 병을 **이미 두 번** 적어 두었다(감사 105·106:
계측기가 만들어지지도 않는 이름을 세고 있었고, 그 값을 아무 화면에도 안
보여줘서 고장난 계측기와 보이지 않는 계측기가 서로를 가려 줬다). 이번은 세
번째 얼굴이다 — **이름은 맞는데 내용이 없다.**

**고친 것 — 재는 쪽뿐이다**

`optional_feature_fill`(채움률) · `thin_features`(문턱 아래만, 깨끗하면 빈
dict) · 밤 장부의 `features_thin` · 배치 건강의 `thin` · 경보
`features_thin:`. 문턱 0.5는 실측으로 정했다 — 건강한 재료 99.4~100%와
문제 25%·3.9% 사이다.

⚠️ **얇은 재료를 빼지 않는다.** 뺄지 말지는 오디션의 `top_features` 축이
정한다(사장님 2026-08-27 방침). 여기서 한 일은 **아무도 못 보던 사실을
보이게 하는 것**뿐이고, 신호는 한 칸도 안 바뀐다.

⚠️ 경보 열쇠는 **이름 목록**이라 한 번 알리고 조용해진다 — 소스 깊이는 대개
그 상태로 오래 가고, 매일 울리면 꺼진 경보다(`features_missing`과 같은 규약).

⚠️ **그리고 이 경보는 처음에 죽은 배선이었다 — 같은 날 내가 잡았다.**
`status["feature_health"]`를 읽게 짰는데 **그 자리는 채워지지 않는다.** 배치는
건강 블록을 본 계좌 장부의 **그날 줄 안에** 넣는다(기존 `features_missing`
경보는 처음부터 거기서 읽고 있었다). 검사는 초록이었다 — 내가 검사에
**있지도 않은 모양**을 먹였기 때문이다.

교훈을 하나 더 넓힌다 — **경보를 붙일 때는 재료가 실제로 어디에 적히는지를
장부에서 확인한다.** 그리고 그 경보의 검사는 **배치가 만드는 진짜 모양**으로
먹인다. 손으로 지은 모양으로 먹이면 검사는 경보가 아니라 **문장 서식**을
확인할 뿐이고, 그 경보는 검사에서만 울린다.

### 그 계측기를 **화면에는 안 붙였다** — 계약이 한 겹만 보고 있었다 (2026-09-07)

바로 위 계측기를 고친 그날, **같은 날 안에 감사 105를 되풀이했다.** 채움률
칸(`thin`)을 장부와 경보에는 붙이고 **화면에는 안 붙였다.** 감사 105의 정의가
정확히 그것이다 — *"장부는 `feature_health`를 매일 남기는데 화면 어디에도
없었다."*

**왜 안 걸렸나 — 계약은 이미 있었고, 그날도 초록이었다.**

감사 98이 만든 계약(`test_ledger_fields_reach_the_screen`)이 매 PR마다 돈다:
*"장부에 쓰는 필드는 화면에 나오거나, 왜 안 나와도 되는지 적히거나."*
그 검사가 보는 것은 **기록의 최상위 칸**이다.

    record["feature_health"] = {...}      ← 계약이 여기까지만 본다
                               └ thin · thinnest …  ← 아무 계약도 없다

`feature_health`는 최상위 칸이고 화면이 그 이름을 읽으니 계약은 지켜졌다.
그 **안쪽**에서는 칸이 조용히 늘어났고, 훑어보니 둘이 아무에게도 안 읽히고
있었다(실측): `thin`(오늘 내가 만든 것)과 `thinnest`(**소비자 0곳**).

**고친 것**

- 화면이 얇은 재료를 말한다 — 이름만이 아니라 **채움률과 문턱**을 함께.
  이름만 적으면 3.9%와 49%가 같아 보인다. 깨끗한 날에는 아예 안 뜬다.
- `thinnest`(가장 빈약한 종목)에 읽는 자리를 줬다 — 결손 경보가 뜰 때
  "어느 종목이냐"에 답한다. 평소에는 안 띄운다.
- 같은 계약을 **한 겹 더 안쪽**에 건다: 배치가 `feature_health`에 적는 칸을
  **소스에서 세어** 화면·경보와 대조한다. 내일 칸을 하나 더 만들면 그 칸을
  아무도 안 읽는다는 사실을 CI가 먼저 말한다.

⚠️ **모든 중첩 칸에 걸지는 않았다.** 기록에는 중첩 딕셔너리가 여럿이고 전부에
걸면 수십 개의 사유를 한꺼번에 요구하게 된다 — 그건 검토를 강제하는 것이
아니라 **사유 목록을 부풀리는** 일이다. `feature_health`를 먼저 고른 이유는
이 칸의 **존재 이유가 '보여지는 것'**이고, 실제로 세 번 고장 난 자리이기
때문이다.

⚠️ **오늘 신호는 한 칸도 안 바뀐다.** 고친 것은 읽는 사람이 그 사실을 볼 수
있는가와, 다음에 누가 빠뜨렸을 때 빨간불이 뜨는가다.

■ 오늘 넓힌 교훈 — **계약이 몇 겹까지 보는지를 확인한다**

초록인 계약은 "이 자리는 지켜지고 있다"가 아니라 **"이 계약이 보는 범위
안에서는 지켜지고 있다"**는 뜻이다. 그 범위가 어디서 끝나는지는 계약 자체를
읽어야 알 수 있고, 끝나는 자리 바로 바깥은 **아무 빨간불도 없는 채로**
자란다. 오늘은 그 자리가 최상위 칸의 한 겹 안쪽이었다.

### 그날 배선을 **반만 했다** — 한국 ETF가 다섯 자리에서 2.15배를 물고 있었다 (2026-09-08)

2026-09-03에 증권거래세를 빼면서 이 문서에 이렇게 적었다 —

> 체결·밴드·오디션·공개 자료가 **전부 종목을 넘겨받는다.**
> 표만 고치고 배선을 안 하면 장부는 그대로 틀린다.

**그 문장을 쓴 날 배선을 반만 했다.** 실측(2026-09-08): `measured_cost_model`
호출 자리 중 종목을 넘기던 곳은 **오디션 하나뿐**이었다.

| 종목을 버리던 자리 | 무엇을 정하는가 |
|---|---|
| 검증 3종(DSR·PBO·CPCV) | **비중 게이트** — 실패하면 그 종목 비중이 깎인다 |
| 배분 사다리 · 2세대 · 무제약 · 다양성 그림자 | **사전 등록된 비교**(판정일 11~12월) |

`is_etf`는 종목을 모르면 주식으로 본다(비싼 쪽 = 보수적). 그래서 운용 한국
12종목 중 **ETF 6종목이 6.5bp 대신 14.0bp — 2.15배**를 물었다
(069500 · 132030 · 133690 · 148070 · 228790 · 273130).

**크기(실측)**: 본 계좌 목표 비중 21일치로 재면 총 회전 5.047 중 한국 ETF가
0.411(**8.1%**)이라, 그림자 계좌 한 곳당 더 문 몫이 **약 308원**(자산의
0.031%)이다. 2026-09-03에 본 계좌에서 쟀던 313원과 같은 크기다.

⚠️ **오늘 뒤집히는 판정은 없다** — 그 6종목의 DSR 최고가 0.070인데 기준이
0.95다. 고친 것은 "지금 틀린 판정"이 아니라 **판정일이 왔을 때 잘못된 요율
위에서 난다**는 것이다.

**고친 것 세 가지**

① 다섯 자리가 종목을 끝까지 넘긴다.
② 그림자 넷이 **글자까지 똑같은** `_turnover_cost`를 각자 하나씩 들고
   있었다(네 벌 동일). 한 곳(`quant/live/shadow_cost.py`)으로 합쳤다 —
   같은 셈이 여러 벌이면 고칠 때 **반드시 하나를 빠뜨린다.** 이 저장소가
   이미 적어 둔 것 그대로다(*"찾는 방법은 one_of 한 곳에만"*).
③ 다음에 빠뜨리면 CI가 먼저 말한다. 종목을 안 넘기는 자리는 사유를 남기되
   **산문이 아니라 표에 대고 확인할 수 있는 주장**으로 쓴다:

       # cost-model: 종목 무관(crypto)   ← `crypto_etf` 프리셋이 없어야 참
       # cost-model: 시장 단위 요약      ← 시장별 표를 만드는 자리에서만

   누가 `us_stock_etf` 프리셋을 만드는 순간 그 사유가 거짓이 되고, **그날
   검사가 먼저 말한다.** 산문으로 두면 그렇게 못 한다.

■ 오늘 넓힌 교훈 — **사유는 검사할 수 있는 모양으로 적는다**

"여긴 종목이 필요 없다"는 말은 오늘은 참이고 내일은 거짓일 수 있다. 그런
말을 주석에 산문으로 남기면 그 말이 낡는 것을 아무도 모른다. 같은 말을
**표에 대고 확인할 수 있게** 쓰면, 표가 바뀌는 날 CI가 대신 기억해 준다.

⚠️ 그리고 이 자리가 늦게 잡힌 이유를 한 번 더 적는다 — **방향이 보수적이라
빨간불이 안 떴다.** 보수적인 것과 옳은 것은 다르다(2026-09-03에 같은 문장을
적고도 이 자리를 놓쳤다).

### 같은 병의 네 번째·다섯 번째 얼굴 — 그리고 **완역 선언이 반만 참이었다** (2026-09-08)

위 교훈("자료가 있어야만 뜨는 화면 줄은 붙인 날 검사가 못 본다")을 쓴 날
같은 자리를 **두 번 더** 봤다. 둘 다 내가 만든 자리다.

**④ 조기 판정 진도표 — 두 시간 전에 내가 붙인 표다.**

같은 날 오전 실기록에 붙인 표의 한국어 **26개 조각 중 영어가 0개**였다.
그 표는 등록된 비교 14개 중 **그날 자료가 쌓인 것만** 그린다 — 그날 그려진
것은 6개였고 나머지 8개는 아무 검사에도 안 세어졌다. 실기록은 완역
대상(`DONE`)이 아니라 충족률 하한(0.92)으로 관리되므로 아무 계약도 이것을
요구하지 않았다.

**⑤ 그리고 완역 선언한 7페이지에도 같은 구멍이 있었다.**

`DONE`은 *"영어로 보면 한국어가 하나도 안 남는다"*는 선언인데, 그 검사는
**그날 렌더된 것만** 본다. 실측(2026-09-08): 조건부 조각 **40개**에 영어가
없었다 — 미국 2 · 코인 단타 11 · 선물 11 · 주간 4 · ML 5 · 오늘 3 · 검증 4.
빈 상태 안내 · 불러오기 실패 · **강제 청산** · 하드 스톱 같은 사고 문구가
전부 여기 있었다. 영어 독자가 가장 읽어야 할 날의 문장들이다.

⚠️ **이건 선언된 미완이 아니다 — ②(첫 화면)와 다른 점이 여기다.** 첫 화면은
스스로 '영어 미완'이라고 적어 두었지만, 이 일곱은 **완역됐다고 말하고
있었다.** 선언과 실제의 불일치다.

**고친 것 — 세는 자를 번역기와 같게 맞췄다**

`tests/test_the_quiet_screens_read_in_english_too.py`가 소스에서 센다.
앞선 두 검사와 다른 점은 **텍스트 런을 복원한다**는 것이다:

- 화면 문장은 대개 `+`로 여러 줄에 걸쳐 이어 붙고 사이에 값이 낀다.
  리터럴 하나를 텍스트 노드 하나로 세면 **실제로는 없는 문장을 사전에
  요구한다**(처음에 그렇게 짜서 부풀린 숫자를 봤다 — 42개가 실제로는 40개).
- 번역기는 통째로 못 찾으면 ` · `·` — `로 끊어 절마다 다시 찾는다. 그래서
  검사도 그렇게 센다 — **다른 자로 재면 그 차이만큼 거짓 경보가 난다.**
- ⚠️ `console.error`의 인자는 **화면이 아니다.** 세면 사전만 부푼다.

**그리고 그 검사가 처음에 거짓 경보를 냈다 — 브라우저 검사가 잡았다.**

값 자리(`${…}`)에는 한국어가 들어 있을 수 있는데(예: "오늘 13종목 보유 /
후보 42종목") 소스에서 세는 검사는 그것을 못 본다. 그래서 이미 덮여 있던
문장을 '빠졌다'고 말했고, **나는 그 거짓 경보를 믿고 넓은 규칙을 위에
얹었다.** 사전 규칙은 앞의 것이 이기므로 아래에 있던 정확한 규칙이 죽었고,
화면에 "1,000,000원"의 '원'이 한국어로 남았다.

⚠️ **소스 검사가 브라우저 검사를 대신하지 못한다.** 소스 검사는 *"안 뜨는
줄"*을 보고, 브라우저 검사는 *"실제로 무슨 글자가 나갔는가"*를 본다. 이번엔
후자가 전자의 오진을 잡았다 — **둘 다 있어야 한다.**

⚠️ **매매 판단은 한 칸도 안 바뀐다.** 바뀐 것은 그 상태가 오는 날 영어
독자가 무슨 일인지 읽을 수 있는가와, 다음에 빠뜨렸을 때 빨간불이 뜨는가다.

■ 오늘 넓힌 교훈 — **새로 쓴 화면 글자는 그날 사전에 같이 넣는다**

④가 증명한다: 교훈을 적어 두는 것으로는 안 막힌다. 두 시간 뒤 같은 손이
같은 자리를 만들었다. 막는 것은 **소스에서 세는 계약**뿐이다. 그리고 계약을
붙일 때는 ⑤가 준 단서를 본다 — **"이 검사가 무엇을 화면으로 세는가"와
"번역기가 무엇을 텍스트 노드로 보는가"가 같아야 한다.**

### 자료가 있어야만 뜨는 화면 줄은 **붙인 날 검사가 못 본다** (2026-09-08)

CI가 하루 만에 같은 병을 두 얼굴로 보여 줬다.

**① 어제 붙인 '편도 요율' 줄에 영어가 없었다.** 장중 트랙 둘의 "이미 낸 비용"
옆에 요율을 붙였는데, 그 줄은 **장부에 요율이 실린 뒤에야** 화면에 뜬다.
붙인 날에는 값이 없어 줄 자체가 안 떴고, 그래서 영어 누락이 안 드러났다.
밤 배치가 값을 채운 **다음 날** CI가 잡았다.

    그날의 초록은 "이 줄이 영어로 나온다"가 아니라 **"이 줄이 아직 안 뜬다"**였다.

⚠️ 같은 커밋의 두 회차가 결과가 갈려서 원인이 분명해졌다 — **PR 회차만
브랜치를 main에 병합한 상태로 검사한다.** 즉 밤 배치의 자료를 함께 본다.
push 회차는 낡은 자료로 도니까 초록이었다. 재현하려면 **그 자료를 가져와야**
한다(그래서 main을 병합해 고쳤다).

**② 그리고 그보다 나쁜 자리가 있었다 — 첫 화면의 경보 문구.**
그 줄들은 장부에 **사고가 적혀야만** 뜬다(장부가 묵음 · 사람의 개입 ·
합성 데이터 폴백 · 현금 부족 · 묵은 봉…). 실측(2026-09-08): 경보 32개의
한국어 조각 **81개 중 54개가 영어 사전에 아예 없었다.** 영어 독자가 가장
읽어야 할 문장이, 하필 그날 한국어로 나갔을 것이다.

⚠️ **정직하게 — 이건 숨어 있던 고장이 아니라 선언된 미완이다.** 사전
머리말이 *"첫 화면·실기록은 분량이 커서 다음 차례"*라고 적어 두었고, 첫
화면은 완역 대상(`DONE`)이 아니라 **충족률 하한**(0.85)으로 관리된다.
다만 그 하한은 **조용한 날의 화면**에서 재므로, 사고가 난 날 영어 독자가
실제로 받는 화면은 그 숫자보다 나쁘다. 그래서 경보부터 옮겼다.

**고친 것** — 경보 32개의 조각 81개에 영어를 채우고, **실행해 보지 않고
잡는 검사**를 붙였다(`tests/test_the_alarms_read_in_english_too.py`).
소스에서 경보 문구를 세어 사전과 대조한다. 브라우저 검사는 그 사고가 실제로
난 날에만 이 줄을 볼 수 있고, **그날은 이미 늦다.**

⚠️ 태그에서 **자른다**(지우지 않는다). 번역기는 텍스트 노드 단위로 움직이므로
`<b>제목</b> — 본문`은 두 조각이다. 지워서 이어 붙이면 실제로는 없는 문장을
사전에 요구하게 된다(처음에 그렇게 짜서 오탐 26건을 봤다).

⚠️ 견줄 때 숫자·날짜·이름을 지운다 — 그 값들은 **일부러 열쇠에 안 박는다**
(장부에서 오므로 곧 만료된다). 이 규칙을 어기지 않았는지도 함께 검사한다.

■ 오늘 넓힌 교훈 — **"검사가 초록"과 "그 줄이 안 뜬다"를 구별한다**

조건부로 뜨는 화면 줄은 그 조건이 참인 날에만 검사에 보인다. 그러니 그런
줄을 붙인 날의 초록은 **아무것도 증명하지 않는다.** 이 저장소는 같은 것을
이미 종목 이름에서 배웠다(2026-08-26: *"브라우저 검사는 그날 장부가 들고
있는 종목만 그린다"*). 처방도 같다 — **실행해 보지 않고, 소스에서 세어
대조한다.**

### 사전 등록된 조기 판정이 **등록대로 돌지 않고 있었다** (2026-09-07)

사전 등록은 이 제품의 정직성의 뿌리다 — *"결과를 보고 기준을 정하면 그 선택
자체가 결과에 오염된다."* 그 자리에 오늘의 렌즈(**계약이 몇 겹까지 보는가**)를
댔더니 등록과 실제가 **세 군데** 갈라져 있었다.

**① 등록된 본페로니 보정이 경계에 안 걸려 있었다.**

배분 사다리·주기 사다리는 등록에 `본페로니 3`이라고 적혀 있는데, 조기 판정
경계는 α=0.05를 **그대로** 썼다. 보정이 빠지면 경계가 **낮아진다** — 거짓
승리 쪽이다. 이 장치의 존재 이유가 *"매일 들여다봐도 거짓 승리 확률이 5%를
안 넘는다"*인데 그 약속이 지켜지지 않고 있었다.

⚠️ **오늘 뒤집히는 판정은 없다**(실측): 가장 가까운 쌍이 진도 0.566 →
0.489로 내려갈 뿐 어느 쪽도 경계를 안 넘는다. 고친 것은 "지금 틀린 판정"이
아니라 **"넘는 날이 왔을 때 보정 없이 넘을 수 있다"**는 것이다.

**② `applies_to` 6개 중 4개가 한 번도 계산된 적이 없었다.**

지정가 그림자 둘 · 2세대 집중 · 사이징 사다리. 그중 2세대 집중은 등록 문구가
**"조기 판정 경계 적용"**이라고 직접 적고 있다. 주기 사다리도 등록된 3쌍 중
2쌍만 돌았다(`15m-5m` 누락) — **재지도 않는 비교의 보정 값을 치르면서**
정작 그 비교는 없는 상태였다.

붙이자마자 **하나가 이미 경계를 넘어 있었다**(실측): 지정가 그림자,
n=20 · 누적 |0.01230| vs 경계 0.01225 · 하루 평균 0.062%p로 **지정가가
우세**. 즉 사전 등록된 실험 하나가 조기 판정에 도달해 있었는데 **아무도
계산하지 않아 몰랐다.**

⚠️ **그런데 채택은 안 된다 — 등록이 미리 심어 둔 조건에 걸린다.**
등록 원문: *"지정가가 이겨도 **미체결율 20% 초과면 채택 보류** — 체결 안 되는
이득은 이득이 아닙니다."* 실측 미체결율 **36.8%**(체결 364 · 미체결 212)로
초과다. 골대를 먼저 심어 둔 것이 그대로 작동했다.

⚠️ 그래서 판정과 **채택 조건을 함께** 싣는다. '우세'만 화면에 띄우면 곧
채택으로 읽힌다. 사이징 사다리·2세대의 낙폭 조항도 같이 실었다 — 그리고
그 조항은 장부의 `낙폭` 칸이 아니라 **자산 곡선에서 직접** 잰다(장부 칸은
'그날의 낙폭'이지 최대낙폭이 아니다: 본 계좌 계열이 0 → −0.53 → −0.28로
오르내린다).

**③ 반대로, 등록에 없는데 도는 것이 있었다.** 미국 주기 사다리다. 등록은
그것을 *"참고 진단"*이라 부른다 — 등록된 판정과 같은 칸에 이름표 없이 두면
읽는 사람이 구별할 수 없다. 이제 이름표를 달고, 보정은 형제(주기 사다리)의
것을 빌린다(참고라고 더 느슨한 자를 쓰면 그 숫자가 더 자주 '이겼다'고 한다).

**그리고 ⑨ 다양성 가중 그림자는 공개 페이지에만 등록돼 있었다.**

trust 페이지는 2026-08-23부터 판정일·트랙 수·정직 사항까지 실어 두었는데
**저장소 등록부에는 없었다.** 같은 페이지가 *"등록 원문은 저장소 코드와
status.json에 그대로 실려 있습니다"*라고 말하는 동안에. 조건을 한 글자도
바꾸지 않고 **옮겨 적었다**(`transcribed_on`).

⚠️ **조기 판정에는 넣지 않았다.** 2026-08-23 등록은 그 권리를 선언하지
않았고, 지금 넣으면 **등록에 없던 훔쳐볼 권리를 뒤늦게 주는 것**이 된다.

⚠️ 같은 페이지에서 ⑧⑨가 사전 등록 블록이 아니라 **'정직한 한계' 목록 안**에
붙어 있었다(닫는 `</li>` 누락으로 딸려 들어갔다). 자리를 바로잡고, 닫는
문장의 "일곱 실험"을 "아홉 실험"으로 고치되 **정정 사실을 날짜와 함께 병기**
했다 — 조용히 고치면 그 자체가 이 페이지가 금지하는 일이다.

■ 오늘 넓힌 교훈 — **손 명단은 숫자가 아니라 문구에서 세게 한다**

유의수준을 코드에 손으로 적으면 등록과 갈린다. 그래서 보정 수를
**등록 문구에서 센다**(`registered_comparisons`). 그리고 등록된 실험은
빠짐없이 계산을 지나며, 재료가 없으면 `missing`에 **이름을 남긴다** —
조용히 빠지면 "그런 비교가 원래 없었다"로 읽힌다. 실제로 그랬다.

### 그림자 실험 장부 열 개가 **두 계좌의 줄을 섞어 담고 있었다** (2026-09-07)

일일 배치의 통합 계좌 함수는 하루에 **두 번** 돈다 — 본 계좌(오디션이 고른
챔피언)와 섀도 대조군(진화 없이 최초 기본 챔피언으로 고정). 대조군은 "오디션이
실제로 가치를 더하는가"를 재려고 만든 계좌라 **신호 자체가 다르다.**

그런데 배분 사다리·사이징 사다리·2세대·무제약 네 실험이 **같은 장부 파일**에
썼다. 판정일이 같은 평일에는 멱등 가드(`== bar`)가 두 번째 줄을 삼켜 아무
일도 안 일어난다 — 그래서 몇 주 동안 아무 표시도 없었다. 갈리는 것은 주말이다:
본 계좌는 코인 봉으로 09-05·09-06을 여는데 대조군은 표본이 모자란 종목이 빠져
09-04에 머문다.

    ... 09-04 · 09-05 · **09-04** · 09-06 · **09-04**
                          ↑ 대조군의 줄이 그대로 끼어든다

실측: 장부 **10개 × 2줄**(되감김 2 · 중복 2). 그리고 오염된 줄이 매번
**마지막 줄**이라, `history[-1]`을 읽는 화면의 대표 숫자 **10개가 전부 다른
계좌의 값**이었다(예: 배분 사다리 자본균등 1,000,825원 → 본 계좌 1,000,975원).
그 옆에는 "본 계좌와 **같은 신호**를 받되…"라고 적혀 있었다 — 이 저장소가
반복해서 잡아 온 병(선언과 실제의 불일치)의 또 다른 얼굴이다.

**다양성 그림자 둘만 깨끗했다**(되감김 0). 그 재료를 줍는 자리(`mix_pair`)가
이미 `use_champions` 안에 있었기 때문이다 — **필요한 가드가 어떤 모양인지는
저장소가 이미 알고 있었고, 나머지 넷에 안 붙였을 뿐이다.**

**고친 것 세 가지**

① **재료를 안 준다**(근본 처방) — 호출부가 본 계좌 회차에서만 네 실험을
   돌린다. 다양성 그림자와 **같은 가드**다.
② **뒤를 받치는 시계**(`quant/live/shadow_clock.py`) — 본 계좌는 2026-08-16
   실전 사고(감사 262) 뒤에 "판정일이 과거로 가면 멈춘다"는 관문을 얻었는데
   **그림자 장부에는 그 관문이 없었다.** 이제 다섯 장부가 한 규칙을 같이 쓴다.
   같은 봉은 '이미 했다'(정상)이고 과거로 가는 봉은 '입력이 고장났다'(사고)다 —
   둘을 같은 가지에 두면 사고가 정상으로 보인다.
③ 이미 들어온 줄은 **고치지 않는다.** 대신 장부가 스스로 세어 `mixed_rows`로
   말하고, 화면의 대표 숫자는 마지막으로 **쓴** 줄이 아니라 가장 최신 **봉**의
   줄을 고른다. 표본 일수도 줄 수가 아니라 **서로 다른 날짜의 수**로 센다
   (19줄 → 17일 · 16줄 → 14일 · 18줄 → 16일).

⚠️ **되돌아온 자산 숫자는 작다**(90~1,325원, 자산의 0.13% 이하)**이고 트랙
   순위도 안 바뀐다**(hrp < inv_vol < erc < equal, 양쪽 다). 고친 것은 성적이
   아니라 **비교의 자격**이다 — 이 실험들의 존재 이유가 "같은 신호에 X만
   바꿔서 견준다"인데 그 전제가 깨져 있었다.

⚠️ **이 병은 `asof` 사고와 형제다.** 그때 얻은 교훈은 "밤을 세는 자리에서
   `asof`도 `today()`도 의심한다"였다. 여기서 하나 넓힌다 — **한 파일에 쓰는
   손이 둘 이상인지도 의심한다.** 손이 둘이면 멱등 가드가 사고를 정상으로
   가려 주고, 숫자는 끝까지 멀쩡해 보인다.

### 패널 관문이 **다중검정 값을 안 치르고 있었다** (2026-09-07)

`shared_panel_specs`에 코드가 스스로 이렇게 적어 두었다:

> *"⚠️ 나중에 이것이 관문이 될 때 다시 볼 것(작업 #56): 지금 다중검정 보정은
> 그날 밤 명단 크기에만 걸린다. … 승격을 패널로 정하는 순간에는 날짜를
> 가로질러 누적된 시도 수로 보정해야 한다."*

**그 순간은 2026-09-02에 왔다**(패널이 AND 관문, gate_version 4). 그런데 열어
보니 승격 조회기는 `pass`(= t > `PANEL_T_REF`) **하나만** 읽고 있었고, 밤마다
계산되던 부트스트랩(`reality_check`)은 **콘솔 로그로만 나가고 아무 관문도 안
읽었다.** 즉 이 문서가 *"패널의 다중검정 보정은 문턱이 아니라 부트스트랩이
맡는다"*고 적어 둔 그 장치가 **관문에 연결돼 있지 않았다.**

**고친 것** — 밤 단위 읽기가 그 밤의 동시검정을 함께 들고 오고
(`_night_reality_check`, 회차가 둘이면 **가장 보수적인 값**), 조회기가 그것을
본다. 규약은 결승 관문과 같다:

- **재려다 실패**했으면 막는다 — 관문을 못 건 채로 승격시키지 않는다.
- **표본이 짧아 생략**했으면 안 막는다 — 못 잰 것을 위반으로 세지 않는다.
  다만 그 사실을 판정과 함께 실어 보낸다.

관문이 바뀌었으므로 `gate_version`을 **5**로 올렸다. 재현은 그대로다 —
재현기가 되먹이는 것은 그날의 **판정 결과**이지 판정 과정이 아니다.

⚠️ **오늘 이것이 막는 것은 없다**(실측): 패널 판정 15건에 **통과 0건**이라
부트스트랩을 볼 차례 자체가 온 적이 없다. 고친 것은 "지금 틀린 판정"이 아니라
**"통과하는 날이 왔을 때 보정 없이 통과할 수 있다"**는 것이다.

⚠️ **정직하게 — 이 보정은 부분적이다.** 부트스트랩은 그 밤에 담긴 설정
(하루 3개)을 센다. 회전 때문에 며칠에 걸쳐 46개를 다 재므로 **누적 시도 수로는
여전히 덜 센다.** 그 한계를 `n_cand`로 판정과 함께 남긴다 — 숫자로 안 적으면
"보정이 걸려 있다"는 말만 남는다.

### 겹친 종목 하나로 **그 밤의 패널을 통째로 잃고 있었다** (2026-09-07)

밤의 두 회차를 합칠 때 같은 종목이 양쪽에 있으면 두 번 세어져 t가 거짓으로
커진다. 그래서 합치기가 **그 밤을 통째로 버린다**(`merge_daily_terms` →
`overlap`). 방어 자체는 옳다 — 문제는 **너무 무디다**는 것이다.

실측: 설정-밤 18건 중 **3건(17%)**이 겹침으로 버려졌고, 그 3건은 **전부
2026-09-07 한 밤**이다(그 밤 설정 3개가 모두 사라졌다).

왜 겹치나 — 멱등 가드는 "새 봉이 있으면 할 일이 남았다"로 보는데, **코인은
회차 사이(약 한 시간)에 새 봉이 생긴다.** 그래서 2회차가 같은 코인을 다시 본다.

**고친 것 — 겹치기 전에 뺀다.** 2회차는 앞 회차가 이미 기록한 종목을 담지
않는다. 남는 두 조각은 서로 겹치지 않으므로 합치기가 정확해지고, 그 밤의
나머지 종목이 살아난다(검사에서 5+5 → 합집합 10종목).

⚠️ **판정 규칙은 그대로다** — 문턱도 최소 종목 수도 안 건드렸다. 달라지는
것은 재료를 모으는 방식뿐이라 `gate_version`을 올리지 않는다.

⚠️ **뺀 것은 장부에 적는다**(`deduped`). 조용히 빼면 "그 밤 그 종목이 원래
없었다"와 구별되지 않는다.

⚠️ **기존 겹침 방어는 그대로 둔다** — 장부 읽기 실패처럼 이 장치가 못 잡는
길이 남아 있고, 그때 뒤를 받치는 것이 그 방어다. 그리고 읽기에 실패하면
**아무것도 빼지 않는다**: 한 번의 읽기 실패가 그 밤의 측정을 통째로 없애면 안 된다.

⚠️ 이 변경이 기존 검사 하나를 깨뜨렸는데, **그 검사의 전제가 낡은 것이었다** —
같은 장부에 같은 날짜로 두 번 적어 "독립인 두 시나리오"를 흉내 내고 있었다.
이제는 그것이 "한 밤의 두 회차"로 해석되므로 장부를 나눴다. 검사가 보려던
것(다중검정이 설정 개수를 센다)은 그대로다.

### 장부가 종목을 잃고, 굶는 종목에는 **빨간불이 없었다** (2026-09-07)

두 가지를 고쳤다. 둘 다 **숫자가 멀쩡해 보이는** 종류라 화면에 아무 이상이
안 뜬다.

**① 목록 자르기가 '못 돈 종목'을 부풀린다 (재현됨).**

같은 밤의 두 회차를 합칠 때 앞 회차 결과를 `set(prev["skipped_keys"])`로
복원하는데, 그 목록이 **20개까지만** 저장돼 있었다. 잘려 나간 종목은 그
순간 사라지고, `not_reached`는 명단에서 빼서 구하므로 그 종목들이 **'한 번도
손대지 않은 종목'으로 둔갑한다.**

| | 건너뜀 | 못 돈 종목 |
|---|---|---|
| 1회차(명단 40 · 성공 9 · 건너뜀 31) | 31 | 0 |
| 2회차(예산이 일찍 끊겨 5종목만 보고) | **20** | **11** ← 없던 사고 |

⚠️ 2026-09-02에 잡은 UTC 열쇠 사고와 **증상이 똑같다**(못 돈 종목이
부풀려진다). 그때는 열쇠가 원인이었고 이번엔 목록 자르기가 원인이다.
**같은 증상에 원인이 둘 이상 있을 수 있다** — 한 번 고쳤다고 그 자리를
다시 안 보면 이렇게 남는다.

이제 병합이 읽는 목록은 자르지 않는다(`KEYS_CAP = 500`). 상한을 없애지는
않되 **걸리면 `keys_truncated`로 말한다** — 세는 수와 목록 길이가 조용히
어긋나는 것이 이 사고의 뿌리였다.

**② 굶는 종목에 경보가 없었다 — 작업 #67이 남긴 숙제.**

작업 #67은 '못 돈 종목' 칸을 만들면서 이렇게 적어 두었다: *"예산이 더
조여져 한 종목이 계속 뒤로 밀리기 시작하면 그때는 아무 빨간불도 안 뜬다."*
**칸만 만들고 빨간불은 안 붙였다.**

⚠️ 한 밤의 `not_reached`로 울리면 안 된다 — 이어달리기는 **설계상** 한 밤에
명단을 다 못 돌아 그 값이 거의 매일 0이 아니다. 매일 울리는 경보는 꺼진
경보와 같다. 물어야 할 것은 "오늘 다 돌았나"가 아니라 **"이 종목이 마지막으로
심사받은 게 언제인가"**다(`audition_gaps`).

문턱 5밤의 근거는 실측이다(장부 35밤 · 명단 40종목): **최악 간격 2밤 ·
중앙 1밤**, 최근 3밤 안에 40종목이 전부 한 번씩 받았다. 그래서 **오늘 이
경보는 침묵한다** — 그게 맞다.

⚠️ 세는 대상은 **지금 명단에 있는 종목만**이다. 장부에는 은퇴한 챔피언
(META·TSLA)이 **17밤째** 남아 있어서, 그것까지 세면 경보가 켜지는 첫날부터
꺼진 경보가 된다.

⚠️ **못 잰 것도 적는다.** 안 적으면 "굶는 종목이 없다"와 "재는 장치가 고장
났다"가 화면에서 똑같이 조용하다.

### 방향은 고르는데 **방향별 성적이 기록에 없었다** (2026-09-07 사장님 질문)

> *"선물투자 시, 롱숏 포지션 고려도 잘 하는거지?"*

장부를 열어 보니 **고르는 장치는 다 돌고 있었다**: `direction_gate`가 밤마다
판정하고(2026-09-06 실측 — 14종목 × 120일, t = **−0.37** vs 문턱 ±1.35 →
양방향 유지), 자금조달 부호가 방향마다 맞고, 숏 차입료가 비용에 들어 있고,
청산·레버리지 관문이 방향과 무관하게 걸린다.

**그런데 방향별 성적은 어디에도 기록돼 있지 않았다.** 화면에는 롱 개수와
숏 개수가 나란히 떠 있었다 — **나란히 보이는 것이 비교는 아니다.** 이 트랙이
이미 한 번 걸린 바로 그 모양이다(2026-09-03: "나란히 돌려서 잰다"고 적어
놓고 반대쪽이 없던 일). 개수는 "숏을 몇 번 잡았나"이지 "숏이 돈이 됐나"가
아니다.

**고친 것 — 회차마다 적는다.**

- 회차 기록에 **마크 가격**을 남긴다. 이것이 없으면 방향별 성적을 나중에
  되살릴 수 없고, 지나간 시세는 다시 받아올 수 없다(작업 #64와 같은 종류의
  재료다).
- **평가**로 가른다(확정만 세지 않는다). 확정만 세면 아직 안 판 자리의 성적이
  통째로 빠져, **회전이 심한 방향이 성적이 있는 방향처럼** 보인다.
- 총이득과 순이득을 **둘 다** 적는다(2026-09-02 지시의 요지).
- 수수료는 **닫는 몫(옛 방향)과 여는 몫(새 방향)**으로 갈라 단다. 뒤집는
  주문을 통째로 새 방향에 달면 숏 수수료가 부풀고 롱이 공짜로 돌아 보인다.
  ⚠️ 체결 기록의 `direction`은 **체결 뒤** 방향이라 통째 청산이면 "청산"이다 —
  그걸로 세면 그 수수료가 **어느 방향에도 안 달린다.**
- 자기 검산이 하나 있다: **자산 변화 = 롱 + 숏 − 수수료 − 자금조달.**
  잔차가 0이 아니면 어느 한쪽이 틀린 것이고, 검사가 그것을 본다.

**실측 (221회차, 재구성):**

| | 수수료 전 | 수수료 | 자금조달 | 최종 |
|---|---|---|---|---|
| 롱 | +172.5 | 459.3 | 8.2 | **−295.0** |
| 숏 | −144.9 | 139.7 | 1.1 | **−285.8** |

⚠️ **재구성한 값이고, 그렇게 적는다.** 옛 회차에는 마크 가격이 없어 그 회차에
**체결이 있었던 종목의 체결가**를 쓴다. 체결이 없던 종목은 가격을 모르므로
0으로 세지 않고 빼 둔다 — 그래서 **못 가른 몫이 174.7 USDT**다(자산 변화
−406.1 중). 그 숫자를 화면에 그대로 적는다. 한계를 주의 문구로만 남기면
읽는 사람이 크기를 알 수 없고, **이 값이 0으로 수렴하는 것이 이 작업이
끝났다는 유일한 증거**다.

⚠️ **정정(2026-09-07 실측) — 지금은 5개 중 0개다.** 여기 "5개 중 1개(XRP)"
라고 적혀 있었는데 그 사이 오디션이 XRP 챔피언을 `breakout`으로 바꿔서, 이제
코인 챔피언 **다섯이 전부** 규칙 전략이다(`cross_rank`·`ma_cross`×2·
`buy_hold`·`breakout`). 규칙 전략은 **음수 신호를 구조적으로 안 낸다.**

즉 방향 관문이 "양방향 좋다"고 해도 **오늘 숏이 나올 수 있는 자리는 없다.**
그런데 화면 머리글은 *"지금은 오를 때와 내릴 때 모두 겁니다"*라고 크게 말하고
있었고, 롱 전용 목록은 다섯 이름을 나열할 뿐 **분수를 말하지 않았다** — 코인이
다섯 종목이라는 것을 아는 사람만 그게 0/5임을 안다.

이제 장부와 화면이 그 분수를 적는다(`shortable: {n, of}`). 그리고 **관문이
껐을 때와 낼 종목이 없을 때의 문장이 다르다** — 둘은 다른 사건이다.

⚠️ **이것은 전략을 바꾸는 일이 아니다.** 숏을 낼 수 있느냐는 그 종목 챔피언이
정하고, 챔피언은 오디션이 정한다. 여기서 한 일은 그 사실을 숫자로 말하게 한
것뿐이다.

⚠️ 분모는 **그 회차가 실제로 본 종목**이다. 상수 목록을 쓰면 시세를 못 받아
건너뛴 날 분모가 부풀어, **못 본 것이 "숏을 낼 수 없는 것"으로 둔갑한다.**

⚠️ 위 표의 숏 숫자를 "숏이 나쁘다"로 읽으면 안 된다 — 표본이 작고, 숏 손실의
대부분은 레버리지 관문(8/26) 이전 폭주 사흘에 몰려 있다. 그리고 지금은 숏이
아예 안 나오므로 그 표는 **더 이상 자라지 않는다.**

### 놀고 있는 현금 — 승인은 받았는데 **실측이 붙이지 말라고 했다** (2026-09-03)

사장님 승인("승인할게")으로 단기 파킹 상품을 유니버스에 붙이려 했다. 붙이기
전에 재 봤더니 **그대로 하면 손해다.**

본 계좌는 순노출 평균 33.6%로 돌아 **평균 66%가 0%로 놀고 있다.** 그 몫을
CD금리 ETF에 넣으면 얼마가 남는가(13영업일 실측, 한국 ETF 실측 편도 6.5bp,
CD금리 연 2.5% 가정):

| 방식 | 이자 | 비용 | 순 |
|---|---|---|---|
| 매일 정확히 맞추기 | +0.078% | 0.121% | **−0.044%** |
| 밴드 10% | +0.080% | 0.106% | **−0.026%** |
| 바닥만 담기 + 밴드 10% | +0.070% | 0.067% | **+0.003%** |

**이자는 하루 약 1bp인데 왕복 비용이 13bp다** — 한 번 고쳐 잡을 때마다
열사흘치 이자가 날아간다. 그런데 위험 장부의 노출이 12.8% ↔ 49.6%로 흔들려서
파킹 슬리브가 그만큼 따라 움직여야 한다. 가장 나은 설계(바닥만 담기)조차
13일에 **+0.003%** — 잡음과 구별되지 않는다.

이건 사장님 방침("각 수수료도 고려해서 수익을 생각해야지")이 정확히 예고한
결과다. **그래서 안 붙였다** — 붙이면 화면에 "현금을 굴린다"가 뜨는데 실제로는
수수료를 더 내는 상태가 된다.

⚠️ 표본이 13영업일뿐이라 **부호만 읽고 크기는 믿지 않는다.** 다만 부호를
뒤집으려면 이자가 비용을 이겨야 하고, 그러려면 회전이 지금의 몇 분의 일이어야
한다.

⚠️ **진짜 답은 아마 ETF가 아니다.** 실제 증권 계좌의 **예수금 이용료**는
거래가 0회라 비용이 없다. 다만 그 이율은 증권사·계좌마다 다른 **계약상의
숫자**이고, 저장소에 근거가 없는 값을 넣으면 수익률을 지어내는 일이 된다
(이 저장소의 첫 번째 규칙). 그래서 **사장님이 실제로 쓸 계좌의 이용료율**이
있어야 넣을 수 있다 — 재료는 사람이 붙인다는 규약 그대로다.

### 한국 ETF가 **안 내는 세금**을 내고 있었다 (2026-09-03)

위 측정을 하다가 별개의 결함을 찾았다. 증권거래세는 **주권 양도**에 붙는
세금이라 ETF(수익증권)는 매도 시 비과세인데, 비용 표가 **시장만 보고**
세금을 물리고 있었다.

    운용 한국 12종목 중 **6종목이 ETF**다
    (KODEX 200 · 나스닥100 · 금 · 국고채10년 · 화장품 · 종합채권).
    편도 14bp로 잡히던 것이 실제로는 **6.5bp**다.
    실측: 지금까지 체결 3건 · 41.7만원 → 더 낸 돈 **313원**(자산의 0.031%).

**금액은 작은데 왜 고치나 — 이 값이 세 곳으로 흘러가기 때문이다:**

  ① 페이퍼 브로커의 실제 체결 수수료 → 장부 손익이 실제와 달라진다.
  ② **리밸런스 밴드가 비용에 비례한다** → 비싸게 잡으면 밴드가 넓어져
     기계가 고쳐 잡아야 할 자리를 안 고친다.
  ③ 오디션이 고회전 한국 후보를 부당하게 떨어뜨린다.

방향이 '보수적'이라 늦게 잡혔다. **보수적인 것과 옳은 것은 다르다** — 이건
이 저장소가 반복해서 잡아 온 병(선언과 실제의 불일치)의 다른 얼굴이다.

- 프리셋에 `kr_stock_etf`를 두고 `CostModel.for_market(..., is_etf=True)`가
  그것을 고른다. ⚠️ **`market`은 그대로 남긴다** — 호가 단위(틱) 하한이
  시장을 보고 계산되므로, 갈아타며 시장을 잃으면 다른 안전장치가 조용히 꺼진다.
- 종목을 모르면 **주식으로 본다**(비싼 쪽 = 보수적). 새 ETF를 넣고 분류를
  빠뜨려도 조용히 싸지지 않는다.
- 체결·밴드·오디션·공개 자료가 전부 종목을 넘겨받는다. 표만 고치고 배선을
  안 하면 장부는 그대로 틀린다.
- 공개 비용 기준(`cost_basis_bp`)이 한국을 **두 줄**로 적는다 — 한 숫자로
  적으면 ETF와 주식 중 하나는 반드시 거짓말이 된다.

⚠️ **오늘 밴드는 안 바뀐다** — 한국 밴드는 이미 실측(표본 97건)으로 돌고
있고 하한에 붙어 있다. 교정이 실제로 작동하는 것은 **가정으로 도는 동안**과
새 시장·새 종목이다. 실측 표본에는 ETF와 주식이 섞여 있는데, 그건 이 함수의
결함이 아니라 표본의 성질이다(실측은 실측이다).

### 관문을 종목 하나에서 **패널(종목 횡단)**으로 (2026-08-27 사장님 ①안)

승격이 사실상 안 일어나던 이유는 관문이 까다로워서가 아니라 **재는 자가
짧아서**였다(실측): 홀드아웃 실효 표본 중앙 **63봉**인데, 탈락 후보가
"우연이 아니다"를 보이려면 중앙 **734봉**이 필요했다. 결승을 이긴 20건 중
19건이 동시검정에서 막혔고 그 p값 중앙은 **0.477** — 아깝게 걸린 게 아니다.

`quant/live/panel_gate.py` — 한 **설정**을 전 종목에 돌려 **날짜별 횡단
평균을 관측 하나**로 보고 t를 낸다.

- 실측 이득(스냅샷 32종목 × 125일): 분산 **16.2배** 감소 → t **약 4배**.
  종목 간 **초과수익** 상관 중앙값 **0.002** — 시장 움직임이 '챌린저 −
  챔피언' 차이에서 상쇄되기 때문이다.
- **관측 단위는 날짜다.** "40종목 × 63봉 = 2,500 표본"으로 세면 관문이
  거짓으로 열린다. 종목 수로 날짜 수를 대신할 수 없다.
- 다중검정 보정은 **설정 개수**에 걸린다(종목 수가 아니다).
- 사장님 지시대로 **기존 종목별 관문도 계속 계산해 장부에 나란히 남긴다** —
  "관문을 바꿔서 결과가 달라진 건가"를 나중에 검증할 수 있어야 한다.

**지금 어디까지 와 있나 (2026-09-02 — 승격에 붙었다)**

기다리던 **대조가 왔다**(2026-09-02 장부 실측). 같은 밤 · 같은 설정 3건 ·
22종목 · 161일:

| | 패널 | 종목별 |
|---|---|---|
| 판정 | t = −0.27 · −1.31 · −2.01 (문턱 1.35) → **전부 불통과** | 셋 다 **1/22 통과** |

**세 판정이 전부 갈렸다(3/3).** 방향도 패널을 만든 이유 그대로다 — 종목별
관문은 22종목 중 하나를 통과시키는데, 같은 설정을 전 종목에 놓고 보면
도움이 되기는커녕 평균이 음수다.

그래서 패널을 승격에 **AND로** 붙였다(`panel_gate_lookup` → `nightly_retrain`
의 마지막 관문). 갈아 끼운 것이 **아니다** — 선발전·결승전·동시검정은 그대로
돌고 그대로 기록된다(①안의 조건). 방향이 안전한 쪽이라(틀려도 "승격이 덜
되는 것"이지 "나쁜 후보가 승격되는 것"이 아니다) 1밤치 대조로도 걸 수 있다.

⚠️ **판정이 없으면 막지 않는다.** 패널은 전 종목에 똑같이 서는 고정 설정만
재고(하룻밤 3개 회전), 언덕오르기 변이는 종목마다 달라 애초에 담기지 않는다.
없는 것을 위반으로 세면 변이 후보가 영영 승격 못 하고 **언덕오르기가 죽는다.**
같은 이유로 **못 잰 밤**(패널에 5종목 미만)도 막지 않는다 — 못 잰 것과 나쁜
것은 다른 사건이다.

⚠️ **오늘 밤 판정을 오늘 밤 승격에 쓸 수 없다.** 패널 판정은 그 밤의 종목을
다 돈 뒤에 나오는데 승격은 종목을 돌면서 그때그때 결정된다. 그래서 **직전까지
쌓인 장부**를 보고, 판정이 `PANEL_VERDICT_MAX_AGE_NIGHTS`(7밤)보다 낡으면
쓰지 않는다 — 명단이 한 바퀴 도는 데 약 15밤이 걸리므로 그 절반이다.

**배선 실측 확인 (2026-09-03 밤 배치 뒤)** — 다섯 자리 모두 설계대로 돈다:

| 확인한 것 | 실측 |
|---|---|
| 밤 열쇠(한국 날짜) | `runs 2` · 심사 22 + 건너뜀 4 + 못 돈 14 = 명단 40 ✓ |
| 회전 축이 실제로 흔들리나 | `band_mult`가 **22종목 중 9종목**에서 흔들렸다(가장 많이 흔들린 축). 후보 수는 4개 그대로 |
| 비용 기준 | 그림자 넷 · 검증 42종목 · 선물 전부에 `cost_basis_bp` 실림 |
| 패널 두 회차 합치기 | 11 + 11 → **22종목 · 122일**로 합쳐짐 |
| 처리 속도 | 9/2 22종목 → 9/3 22종목(안 줄었다). 굶는 종목 0 — 최악 간격 **1일** |

⚠️ **패널 관문이 아직 무엇을 막은 적은 없다.** 그 밤 22줄 전부 `panel_gate`가
비어 있는데, 후보들이 그 앞 관문(결승)에서 이미 떨어졌기 때문이다 — 배선이
안 된 것과 다르다(칸 자체는 22/22 줄에 있다). 그리고 같은 밤 패널은 설정 셋을
전부 불통과시켰고 그중 하나는 종목별 관문이 1종목을 통과시켰다 — **이틀 연속
두 관문이 갈렸다.**

⚠️ 관문이 바뀌었으므로 `gate_version`을 **4**로 올렸고, 재현은 **그날 장부에
적힌 판정을 되먹인다**(오늘의 패널 장부로 어제 결정을 재생하면 그 사이 명단이
회전해 판정이 달라진다). 옛 기록(v1~v3)은 이 관문이 없던 세계 그대로 재현한다.

아래는 그 전(2026-08-27 배선 완료) 시점의 기록이다 — 승격은 종목별 관문이
정했다. 사장님 ①안의 조건("기존 관문도 나란히
계속 기록한다")을 지키려면, 먼저 두 관문이 같은 밤에 각각 뭐라고 하는지가
며칠 쌓여야 한다. 그 대조 없이 갈아 끼우면 나중에 성적이 변했을 때
**관문 때문인지 시장 때문인지 구별할 방법이 없다.**

- 재료로 쓰는 것은 **여러 종목에 똑같이 서는 고정 격자 설정**뿐이다(28개).
  `mutate_champion()`의 변형은 종목마다 다르므로 한 통에 담으면
  "서로 다른 설정들의 평균"이 되고 아무 뜻도 없다.
  ⚠️ 격자의 ML 항목은 `{"model": "gb", ...}` 같은 **덧씌우기 형태**라
  오디션이 *그 종목 챔피언 위에* 얹어 해석한다 — 그대로 담으면 종목마다
  다른 설정이 되고, 그 자체로는 전략을 만들 수도 없어 홀드아웃 재생이
  **종목마다 조용히 실패**한다(2026-08-27 실제 스냅샷 연기시험에서 발견).
  그래서 패널 명단은 **기본 챔피언 위에** 얹어 절대 설정으로 못 박는다.
- 패널에 서는 종목 수는 **그날 실제로 돈 종목 수**다. 이어달리기(시간 예산)
  때문에 하룻밤에 전 종목을 못 돌므로, 장부에 그 숫자를 그대로 적는다.
- 명단은 **고정 격자 + 고정 전략형 후보 둘 다**에서 나온다(46개). 처음에는
  앞의 것만 봐서 **가설 규칙 여섯 개**(월말 수급·PEAD·만기 주간·FOMC 표류·
  펀딩 과열 회피·월말 강제 리밸런싱)가 명단 밖에 있었다 — 이들은 파라미터까지
  전 종목이 같아서 패널에 **가장 잘 어울리는** 후보인데 정작 안 재고 있었다.
  링과 패널이 같은 목록(`FIXED_CHALLENGERS`)을 본다.
- **재료가 0인 밤에도 장부에 줄을 남긴다.** 안 남기면 "패널이 못 쟀다"(고장)와
  "밤 배치가 안 돌았다"(다른 경보의 일)가 장부에서 똑같이 보인다. 그 밤
  오디션을 실제로 연 종목 수를 함께 적어 둘을 구별한다.
- ⚠️ **하룻밤에 재는 설정은 3개다**(명단을 날짜로 회전). 크기는 짐작이
  아니라 실측으로 정했다(2026-08-27, 한국주식, 종목당 오디션 ~71초):

  | 하룻밤 명단 | 종목당 추가 시간 | 예산 1800초 처리 종목 |
  |---|---|---|
  | 전체(28개) | **+109%** | 26 → **12** (반토막) |
  | 6개 | +37% | 25 → 18 |
  | **3개** | **+8%** | 25 → **23** ← 채택 |

  ⚠️ 비용은 뽑히는 설정에 따라 **고르지 않다** — 풀링(pool) ML이 섞인 밤이
  몇 배 비싸다. 명단을 키울 거면 **다시 재고** 키운다.
  ⚠️ 지금 패널은 기록만 하므로 이 8%는 **아직 이득 없이 내는 비용**이다.
  **시드는 날짜만 쓴다** — 종목별로 뽑으면 설정마다 참여 종목이 쪼개져
  비용만 쓰고 아무것도 못 잰다.
- ⚠️ **"날짜만 쓴다"로는 부족했다 — 어느 날짜냐가 남아 있었다**
  (2026-08-29 장부 실측). 명단을 그 **종목의 마지막 봉 날짜**로 뽑고 있었다.
  날짜이긴 한데 종목마다 다르다 — 코인은 주말에도 봉이 생기고 주식은 안
  생긴다. 그래서 봉 날짜가 갈리는 밤에는 한 통에 **서로 다른 명단 둘**이
  담겼고, 세 가지를 한꺼번에 잃었다:

  | | 쪼개진 밤(8/29) | 안 쪼개진 밤(8/28) |
  |---|---|---|
  | 담긴 설정 | 6개(명단은 3개인데) | 3개 |
  | 설정당 종목 | 5 · 7 | 12 |
  | 동시검정에 쓴 날짜 | **79일** | **120일** |

  ① 횡단 폭이 반으로 준다(패널의 이득이 나오는 자리다) ② 부트스트랩이
  설정을 6개로 세어 다중검정 벌이 두 배가 된다 ③ 표를 세로로 맞출 때
  날짜를 교집합하므로 **자가 34% 짧아진다** — 패널을 만든 이유가 "재는
  자가 짧다"였는데 그 자를 도로 잘랐다.

  이제 **밤 시작 시각에 날짜 하나를 정해 모든 종목에 같은 것을 준다.**
  그 날짜는 장부의 `roster_asof`에 남는다(회전은 날짜만 보므로 이 한 칸이면
  그날 명단 전체가 되살아난다). 그리고 담긴 설정이 명단 크기를 넘으면
  장부가 스스로 `roster_split`이라고 적는다 — 쪼개진 밤의 숫자는 **성적이
  나쁜 밤과 똑같이 생겨서**, 장부가 말하지 않으면 시장 탓으로 읽힌다.

  ⚠️ 이 자리에는 이미 검사가 서 있었는데 **틀린 것을 지키고 있었다**
  (`arg == "asof"`). "날짜로 뽑는가"는 초록이었고, 정작 필요한 것은
  "**밤 전체가 같은 값을 쓰는가**"였다. 형식을 검사하고 성질을 안 검사하면
  이런 모양이 된다.
- ⚠️ **밤 배치는 하루에 두 번 돈다** — 그래서 패널 장부에도 그날 줄이 둘이다
  (실측 2026-08-29: 1회차·2회차 합쳐 심사 18 · 건너뜀 22 · 못 돈 종목 0).
  두 번째 회차는 앞 회차가 이미 심사한 종목을 멱등 가드로 건너뛰므로,
  **각 줄은 그 회차가 실제로 연 종목만** 담는다(각각 12~15종목). 즉 지금
  패널은 한 밤에 40종목이 아니라 그 절반 정도를 본다. 이건 거짓말이 아니라
  기록된 사실이고(`n_symbols_seen`), 승격을 패널로 옮길 때(작업 #56)
  **같은 밤의 줄들을 합쳐서** 재야 한다는 뜻이다.
  ⚠️ **그런데 그 두 줄이 같은 명단을 보지 않고 있었다**(2026-08-30 실측).
  명단 날짜를 **UTC 달력일**로 정하는데, 러너가 밀리면 두 번째 회차가
  **UTC 자정을 넘어간다.** 패널 장부가 쌓인 3밤의 실제 실행 시각:

  | 밤 | 1회차 · 2회차 (UTC) | UTC 날짜 | 한국 날짜 |
  |---|---|---|---|
  | 8/28 | 05:53 · 06:52 | 같음 | 같음 |
  | 8/29 | 03:40 · 04:22 | 같음 | 같음 |
  | 8/30 | **23:57 · 01:15** | **다름** | 같음 |

  UTC로 묶으면 두 회차가 한 밤인 경우가 **1/3**, 한국 날짜로는 **3/3**이다.
  그래서 열쇠를 **한국 달력일**로 바꿨다(`night_key()`, 시간대는
  `market_hours.KST` 하나만 쓴다). 한국 자정(15:00 UTC)은 배치 창
  (06:15~12:00 KST)에서 가장 멀리 떨어진 자리라 경계가 배치를 안 가른다.

  ⚠️ **오늘 당장 잃는 것은 없다** — 회차마다 명단이 하나씩이라 쪼개짐은
  안 난다. 막히는 것은 위에 적은 **작업 #56**이다: 같은 밤의 두 줄을 합쳐
  종목 폭을 넓히려면 두 줄이 같은 명단이어야 한다.
  ⚠️ **정정(2026-09-01 실측) — 시간 예산은 병목이 맞다.** 여기 원래
  "예산은 지금 병목이 아니다 — 못 돈 종목이 0이다"라고 적혀 있었는데
  **틀렸다.** 그날 밤 배치의 실제 기록은 `명단 40 · 심사 24 · 실패 0 ·
  건너뜀 0`이었고, 커서에는 못 돈 종목이 **16개** 남아 있었다. 최근 다섯
  밤의 심사 수도 27 · 24 · 21 · 17 · 24로 전부 40에 못 미친다 — 그 문장을
  쓴 밤(8/30, 21종목)도 이미 그랬다. 그러니 그 문장에 기대어 적었던
  "8% 비용이 처리 종목 수를 줄이지 않았다"도 **근거가 없다.** 위 표의
  "예산 1800초 처리 종목"이 여전히 지금 상태다.

  ⚠️ 다만 **굶는 종목은 없다.** 40종목 전부가 최근 다섯 밤 안에 오디션을
  받았고 최악의 간격은 3밤이다(6종목). 이어달리기가 설계대로 돌고 있다 —
  "예산에 걸린다"와 "종목이 굶는다"는 다른 사건이고, 지금은 앞의 것만
  참이다.

  ⚠️ 그리고 그 사실이 **장부에 안 적히고 있었다.** `run_health`에는 못 돈
  종목을 담는 칸이 없어 `ok 24 · failed 0 · skipped 0`만 남았다 — 24 ≠ 40
  인데 산술이 안 맞는다는 말을 아무도 안 했다. 이제 명단을 함께 넘겨
  `roster` · `not_reached` · `not_reached_keys`를 남기고, 다 돈 밤은
  **0이라고 말한다**(칸이 없는 것과 0은 다른 사건이다). 붙이는 김에 드러난
  것 하나 더 — 예산이 첫 종목 전에 지나면 배치가 `전 종목 재학습 실패: {}`를
  던지고 있었다. 실패한 종목이 하나도 없는데 전멸이라고 말하면 진짜 전멸과
  구별이 안 된다.
- ⚠️ **①안의 병기가 반만 되고 있었다**(2026-09-01 실측). 장부에 남던 것은
  패널 t와 **부호만 센 승률**뿐이고, "같은 설정을 종목별 관문이 어떻게
  봤나"(종목별 t가 문턱을 넘었나)는 없었다.

  그 사이 실측(밤 열쇠 수정 후 4밤): **패널 판정 15건 통과 0 · 종목별 심사
  27건 승격 0.** 두 관문이 내내 똑같이 "아니오"만 했다. 그러니 **승격을
  기다려 대조를 얻으려는 계획은 영영 안 온다** — 종목별 관문의 역대 승격은
  시행 11,721회 중 2회다. 대조는 승격이 아니라 **판정 그 자체**에서 나와야
  한다.

  ⚠️ 내가 작업 #56의 착수 기준을 "3~4밤 쌓이면"이라고 **날짜 수**로 잡았던
  것이 틀렸다. 필요한 것은 기간이 아니라 **두 관문이 갈린 장면**이고, 지금
  구조에서는 그것이 저절로 오지 않는다.

  그래서 `symbol_t_stats`가 같은 설정을 종목마다 다시 재서
  `symbol_t_median` · `symbol_pass` · `symbol_pass_rate`를 장부에 나란히
  적는다. 추가 백테스트는 없다(이미 모아 둔 계열을 다시 셀 뿐이다).
  ⚠️ 이것은 밤 오디션의 **재연이 아니다** — 진짜 종목별 관문은 그 종목·그날의
  시도 수로 문턱을 정하고 부트스트랩까지 건다. 여기서는 **패널과 같은
  기준선**을 종목마다 그대로 적용한다. 다른 자로 잰 두 숫자를 나란히 놓으면
  대조가 아니라 착시다.
- ⚠️ **작업 #56의 ⓑ(같은 밤의 두 줄 합치기)를 할 재료가 장부에 없었다**
  (2026-09-01 실측). 밤의 두 회차는 서로 **겹치지 않는 종목**을 본다:

  | 밤 | 1회차 | 2회차 | 합치면 |
  |---|---|---|---|
  | 8/31 | 12종목 | 5종목 | **17** |
  | 9/1 | 13종목 | 11종목 | **24** |

  합치면 횡단 폭이 거의 두 배다 — 패널 관문이 존재하는 이유 그 자체다.
  그런데 장부에 남던 것은 설정별 **요약**(평균·t·날짜 수)뿐이라 합칠 방법이
  없다. 서로 다른 종목 집합에서 나온 두 t로 union의 t를 만들 수는 없기
  때문이다. 필요한 것은 **날짜별 합과 개수**다:

      union 평균[날짜] = (합₁[날짜] + 합₂[날짜]) / (개수₁ + 개수₂)

  ⚠️ 그리고 이 재료는 **지나가면 되살릴 수 없다.** 그 밤의 백테스트를 통째로
  다시 돌려야 하는데 챔피언은 그 사이 바뀐다. 그래서 승격을 옮기기 전에
  **재료부터 남긴다** — #56의 착수 조건(두 관문이 갈린 장면)은 아직 안 왔지만,
  기다리는 동안 지나가는 밤은 그만큼 영영 못 합친다.
  - 거르기(최소 종목 수)는 **합친 뒤에 한 번만** 한다. 회차마다 걸면, 각각
    3·4종목이라 버린 날이 합쳐서 7종목이 되어도 안 살아난다.
  - 합산은 두 회차가 겹치지 않을 때만 옳다. 겹치면 그 종목이 **두 표를
    행사**해 패널 평균이 그쪽으로 기운다 — 그래서 설정마다 종목 열쇠도 남기고,
    겹치면 합치지 않고 **겹쳤다고 말한다**.
  - 값: 장부 한 줄이 **17.8KB**(설정 3 × 종목 13 × 날짜 158), 밤 2회차면
    35.6KB — 1년 12.7MB다. 지금 `retrain_history.jsonl`이 밤당 62KB이니
    상태 파일의 밤당 기록량이 대략 **1.5배**가 된다. 싸지 않지만, 못 되살리는
    것을 남기는 값으로는 낸다.
- ⚠️ **장부를 `asof`로 묶으면 안 된다 — 밤의 열쇠는 `roster_asof`다**
  (2026-09-01 실측). `asof`는 그 회차가 실제로 돈 종목들의 마지막 봉 날짜
  **중 최댓값**이라 회차마다 다르다. 그래서 두 가지가 한꺼번에 틀린다:
  ① 한 밤이 **쪼개진다**(두 회차를 가진 밤 2/2가 asof가 서로 달랐다),
  ② 두 밤이 **붙는다**(`asof=2026-08-31` 한 칸에 밤 8/31과 밤 9/1의 줄이
  같이 들어 있었다). 나도 그렇게 세어 상황 보고를 한 번 틀리게 냈다 —
  겉보기엔 멀쩡한 숫자가 나오므로 **틀렸다는 신호가 어디에도 안 뜬다.**
  이제 `panel_nights()`가 유일한 읽는 길이고, 옛 줄(재료 없음)은 합치지 않고
  **못 합쳤다고 말한다**(조용히 빼면 "그 밤엔 그 설정이 없었다"와 구별 불가).
- ⚠️ **같은 착각이 첫 화면에도 있었다 — 배너가 "어젯밤 N종목"을 언제나 5로
  적고 있었다**(2026-09-01 실측). 재학습 장부에는 밤 열쇠가 아예 없어서
  배너가 `asof`(그 종목의 마지막 봉 날짜)의 최댓값으로 밤을 골랐다. 그런데
  매일 봉이 생기는 시장은 코인뿐이라 그 최댓값은 **언제나 코인 날짜**다.

  | 밤 | 실제 심사 | 배너가 센 수 |
  |---|---|---|
  | 8/29 | 24 (한국 15 · 미국 4 · 코인 5) | **5** |
  | 8/30 | 21 (전부 미국) | 그 밤 줄이 하나도 안 잡힘 |
  | 8/31 | 17 | **5** |
  | 9/1 | 24 (미국 19 · 코인 5) | **5** |

  수가 5분의 1로 줄어드는 것보다 나쁜 것은 **승격이 숨는 것**이다. 주식에서
  챔피언이 바뀌면 그 줄의 `asof`는 금요일이라 그날 목록에서 빠지고 화면에는
  "전원 챔피언 유지"가 나간다 — 배너가 존재하는 이유 그 자체를 놓친다.
  이제 재학습 기록도 밤 열쇠(`night` = 밤 배치가 한 번 정한 명단 날짜)를
  남기고 배너가 그것으로 묶는다. 옛 줄은 `asof`로 되돌아간다(고치지 않는다).

  ⚠️ 교훈은 하나다 — **`asof`가 있는 장부를 밤 단위로 읽는 자리는 전부
  의심한다.** 이름이 날짜라서 밤처럼 보이지만 아니고, 틀려도 숫자가
  멀쩡해서 빨간불이 안 뜬다.

  그래서 훑어봤더니 **두 곳이 더 있었고, 한 곳은 공개 게시물이었다**
  (2026-09-01). SNS 카드가 그날 오디션을 `r.asof===date`로 골라 숫자가
  **양방향으로** 틀렸다(최근 120줄 창):

  | 날 | 카드가 발표한 후보 | 그 밤 실제 |
  |---|---|---|
  | 8/28 | 2,283명 | 1,332명 (한 칸에 네 밤이 섞여 과대) |
  | 8/29 | **247명** | **1,183명** (5분의 1로 축소) |
  | 8/31 | 1,199명 | 846명 |

  그리고 미국주식만 돈 밤에는 일치하는 줄이 없어 카드가 **"오늘은 오디션이
  없었습니다"**를 공개했다 — 21종목을 심사한 밤에 대해서다. 이 계정의
  정체성이 *선택 편향 없는 공개 실험*이라 이 자리는 화면 오류보다 무겁다.

  `flag_watch`의 공회전 경보도 같은 모양이었다. ⚠️ **정직하게 — 여기서
  측정된 피해는 없다**(장부 493줄 중 공회전 2줄, 둘 다 8월 중순). 같은
  뿌리라 함께 고친 것이지 실측된 사고가 있어서가 아니다.

  세 곳 모두 `night` 하나로 묶는다. 옛 줄은 `asof`로 되돌아간다.

  ⚠️ **네 번째 자리는 `asof`가 아니라 `date.today()`였다**(2026-09-02 실측).
  배치 건강 기록(`run_health`)의 병합 열쇠가 UTC 달력일이라, 밤 9/2의
  두 회차가 23:52 → 01:05 UTC로 자정을 넘기자 2회차가 1회차를 **덮어썼다**
  — 장부·패널은 22종목인데 건강 기록은 `runs 1 · ok 12 · not_reached 28`.
  **하루 전에 내가 붙인 '못 돈 종목' 칸이 옛 열쇠 때문에 10종목을 부풀려
  말했다.** 이제 밤 배치와 같은 열쇠(`night_key`, 한국 달력일)를 쓴다.
  교훈을 넓힌다 — **밤을 세는 자리에서 `asof`도 `today()`도 의심한다.**
  둘 다 이름이 날짜라 밤처럼 보이고, 둘 다 틀려도 숫자가 멀쩡하다.
- 패널의 다중검정 보정은 문턱이 아니라 **부트스트랩**이 맡는다 — 설정을
  늘리면 귀무 세계의 최고 t가 같이 커져 p가 정직하게 커진다.

⚠️ **이건 관문이 느슨해지는 것이 아니라 재는 대상이 바뀌는 것이다.** 패널은
한 설정이 여러 종목에서 **같은 방향으로** 도움이 될 때만 점수를 준다. 종목마다
부호가 갈리면 상쇄돼 오히려 t가 떨어진다(실측: 종목별 |t| 중앙 0.583 vs
패널 0.487). 한 종목에서만 좋아 보이는 설정은 대개 잡음이고, 그걸 거르는
것이 이 관문의 존재 이유다.

### ⚠️ 정직하게 — 탐색을 넓히는 것은 공짜가 아니다

후보를 늘릴수록 **우연히 좋아 보이는 후보도 늘어난다**. 다행히 이 저장소는
그 대가를 이미 문턱에 반영하고 있다(`confirm_threshold`). 그러므로 탐색을
넓힐 때 지킬 것은 하나다 — **넓힌 만큼 관문이 따라 올라가는지 매번 확인한다.**
그 연결이 끊긴 자동화는 개선 장치가 아니라 과최적화 기계이고, 그건 이 제품의
정체성(선택 편향 없는 공개 실험)을 정면으로 무너뜨린다.

또한 ML이 정확도를 계속 올려 주지도 않는다. 방향 예측의 현실적 상한은
대개 52~55%다. 이 방침의 목적은 **성적을 올린다는 약속이 아니라, 개선을
찾는 일 자체를 사람 손에서 기계로 옮기는 것**이다.

---

## 개발 명령어

```bash
pip install -r requirements.txt   # 또는: make install
pytest -q                          # 전체 테스트 (또는: make test)
python -m quant web --open         # 웹 조종석
python -m quant validate --strategy ma_cross   # 과최적화 검증 3종
```

CI가 Python 3.11/3.12에서 매 push·PR마다 전체 테스트를 실행합니다.
