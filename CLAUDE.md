# CLAUDE.md

## 이 저장소

주식·코인 **퀀트 자동투자 시스템**(`quant/`). 저장소 이름(`insta-story-downloader`)은
내용과 무관합니다 — 실제 제품은 퀀트 트레이딩 프레임워크입니다.

전체 설명은 `README.md`를 보세요.

---

## 📌 Notion 동기화 규약 (필수)

이 제품은 Notion **유어팀 워크스페이스**에서 관리됩니다.
**구현이 완료될 때마다 Notion을 업데이트합니다.** 사용자가 매번 요청하지 않아도 수행하세요.

### ⚠️ 독자는 비개발자입니다 (가장 중요)

제품 페이지는 **신입 기획자 온보딩 문서**입니다. 투자·개발 지식이 없는 사람이 읽습니다.

- **전문용어를 쓰면 그 자리에서 풀어 씁니다.** 처음 등장하는 용어는 괄호나 한 줄 설명을 붙이세요.
- **코드 경로·함수명·환경변수는 본문에 넣지 않습니다.** 꼭 필요하면 `<details>` 접이식 안으로.
- 기능이 아니라 **"그래서 사용자에게 뭐가 달라지는가"**를 씁니다.
- 기존 문서의 톤(비유로 설명, 정직한 한계 병기)을 유지하세요.

### 대상 페이지 (검색하지 말고 아래 ID를 직접 사용)

| 대상 | ID / URL |
|---|---|
| 제품 페이지 (마스터 문서) | `3bb0adee-652b-8170-9435-d408a474b611` |
| 개발 및 제작 관리 DB (작업 항목) | `collection://2e30adee-652b-810e-8f87-000b403bf909` |
| 제품 목록 DB (제품 행) | `collection://2e30adee-652b-8145-a525-000bec085d5a` |
| 유어팀 메인페이지 | `2e30adee-652b-81bd-9ba4-df065fe6f39e` |
| 제품 & 서비스 페이지 | `2e30adee-652b-81a8-8af2-dfba625acb93` |

제품 페이지는 메인페이지의 **제품** 섹션과 제품 & 서비스 페이지의
**제품/서비스 목록**에 자동으로 함께 노출됩니다(같은 DB를 보는 뷰).

### 무엇을 언제 업데이트하는가

**① 기능 구현·버그 수정을 커밋할 때마다 — 제품 페이지 "13. 개발 업데이트 로그"**

`notion-update-page`의 `update_content` 명령으로, `## 13. 개발 업데이트 로그` 아래
콜아웃 **바로 다음**(= 기존 최상단 `<details>` 앞)에 새 항목을 끼워 넣습니다.
로그는 **최신이 위**입니다.

```
<details color="gray_bg">
<summary>**YYYY-MM-DD · 제목** — 한 줄 요약</summary>
	- 무엇이 바뀌었는지 (사용자 관점으로)
	- 왜 바꿨는지 / 무엇을 막았는지
</details>
```

**② 작업 단위가 새로 생기거나 끝날 때 — 개발 및 제작 관리 DB**

- 새 작업: `notion-create-pages`로 행 추가. `제품` 관계를 위 제품 페이지 ID로 연결.
- 완료: 해당 행의 `상태`를 `완료`로 (`시작 전` → `진행 중` → `완료`).
- 상태 값은 정확히 이 세 가지만 유효합니다.

**③ 현황 숫자가 바뀌었을 때 — 제품 페이지 "9. 현황 스냅샷"**

버전(`VERSION`), 테스트 파일 수, 운영 종목 수, 챔피언 설정(`state/champions.json`),
8마일 챌린지 잔고(`docs/social/latest.json`)가 바뀌면 표의 해당 행만 갱신합니다.
7장(8마일 챌린지)에도 같은 잔고 숫자가 있으니 함께 맞춥니다.

**④ 로드맵 항목이 끝났을 때 — 제품 페이지 "11. 로드맵"**

`### 남은 것`의 `- [ ]` 항목을 `### 완료`의 `- [x]`로 옮깁니다.

**⑤ 판매 상태가 바뀌면 — 제품 목록 DB의 `상태`**

`준비 중` → `판매 중` → `종료`. 실거래 개시나 판매 개시 시점에만 변경하며,
**변경 전 사용자에게 확인**하세요(법률 검토 항목과 얽혀 있습니다).

### 규칙

- **숫자는 저장소에서 확인한 값만** 씁니다. 추정치를 사실처럼 적지 않습니다.
- 문서 오타 수정, 포매팅 등 **사용자에게 보이지 않는 변경은 로그하지 않습니다.**
- '수익 보장'류 표현은 Notion에도 절대 쓰지 않습니다(사기죄 소지).
- 새 용어가 문서에 등장하면 **5. 용어 사전에도 추가**합니다. 사전에 없는 용어를 본문에서
  설명 없이 쓰면 온보딩 문서로서 실패입니다.
- 성과가 나쁜 기간도 **그대로 적습니다.** 좋은 숫자만 남기는 편집은 이 제품의 정체성
  (선택 편향 없는 공개 실험)을 무너뜨립니다.
- Notion MCP가 연결돼 있지 않으면 **작업을 중단하지 말고**, 커밋은 정상 진행한 뒤
  "Notion 업데이트는 MCP 미연결로 보류됨"을 사용자에게 알립니다.

---

## 📌 투자 로직은 **기계가 개선한다** (2026-08-27 사장님 지시)

> *"투자 로직의 경우에는 머신러닝으로 개선을 계속할 수 있게끔 해야지
> 너가 수동으로 고치는 방향 말고."*

### 이미 기계가 하고 있는 것 (저장소에서 확인한 값)

- **운용 중인 40종목 중 38종목이 ML**이다(나머지 둘은 `ma_cross` — ETH·BNB).
  ⚠️ `state/champions.json`은 42개인데 그중 둘은 **은퇴한 챔피언**이다(META·TSLA — 2026-08-22 시총 회전에서 유니버스를 떠났다). 파일 길이를 운용 종목 수로 세면 안 된다 — 내가 2026-08-27에 그렇게 잘못 셌다.
- ML 챔피언은 워크포워드로 **스스로 재학습**한다(`retrain_every` 기본 20봉).
- 밤 배치가 매일 **오디션을 돌려 자동 승격**한다. 누적 시행 11,721회, 승격 2회.
  승격은 2단계 관문(선발전 → 결승전)을 통과할 때만 일어난다.
- 후보도 일부는 **기계가 만든다**: `mutate_champion()`이 매일 밤 챔피언 주변을
  변형한 후보를 만들어 링에 세운다(언덕오르기, 날짜 시드로 결정적).
- 다중검정 보정도 **이미 걸려 있다**: 결승 문턱이 최근 시도 수에 로그 비례해
  올라간다(`confirm_threshold`). 후보를 많이 뽑을수록 관문이 저절로 빡빡해진다.

즉 "누가 이길지"도, "챔피언 주변 어디를 더 파 볼지"도 이미 기계가 정한다.

### 탐색 축은 이제 **표**다 — 그리고 스스로 넓어진다 (2026-08-27 구현)

예전에는 `mutate_champion()`이 `if axis == "model": … elif …` 사슬이었다.
그러면 **기계가 자기 탐색 공간을 읽을 수 없다.** 손잡이를 새로 만들 때마다
사람이 사슬에 가지를 쳐야 했고, 잊으면 그 축은 영원히 탐색되지 않는데
**아무 데도 빨간불이 안 떴다.**

실제로 그 상태였다(실측): 살아 있는 ML 챔피언 38종목 중 **6종목이 언덕오르기가
못 건드리는 손잡이 위에 앉아 있었다** — `meta` 3종목, `pool` 3종목. 고정 격자가
그 설정을 한 번 승격시키고 나면, 그 뒤로 "빼 보면 더 나은가"를 아무도 안 물었다.

이제 사슬을 표로 바꿨다(`ML_EXPLICIT_AXES`) 그리고 표에 없는 손잡이는
**관측에서 자동 유도한다**(`ml_search_axes`). 격자나 현재 챔피언에 새 손잡이가
나타나는 그 순간부터 언덕오르기가 그 축을 흔든다 — 사람이 기억할 필요가 없다.

- 명시 축(범위를 사람이 정하는 게 나은 것): `model` · `threshold` ·
  `train_window` · `retrain_every` · `label` · `sizing` · `sample_weight` ·
  `calibrate`
- 관측 유도 축(자동으로 들어온 것): `pool` · `meta` · `top_features` ·
  `label_cost`
- 유도 축의 표본에는 **빼 보기**가 항상 들어간다. 더하는 것만 탐색하고 빼는
  것은 안 하면, 한번 붙은 설정은 영영 안 떨어진다.

⚠️ 이것은 후보 **수**를 늘리지 않는다(하루 4개 그대로). 넓어지는 것은 공간이지
시행 횟수가 아니므로 다중검정 부담이 늘지 않는다.

### 아직 사람이 하고 있는 것

- `DEFAULT_CHALLENGERS` 고정 격자 — 여전히 손으로 유지한다. 다만 이제 이
  격자는 **탐색 축의 씨앗**이기도 하다. 여기 한 줄을 적으면 그 손잡이가
  자동으로 탐색 대상이 된다.
- 피처셋 `fs1`~`fs7` — 새 피처 묶음은 매번 손으로 추가한다.
- 가설 우선 후보(월말 수급·펀딩 과열 회피·PEAD 등) — 조사해서 손으로 쓴 규칙.

### 앞으로의 방침

- 투자 로직을 바꿔야겠다고 생각되면 **그 규칙을 직접 쓰지 않는다.**
  대신 **기계가 그 후보를 스스로 만들어 링에 올리도록** 만든다.
  손잡이 하나가 아니라 **손잡이를 만드는 장치**를 짠다.
- 손으로 링에 세우는 것은 **기계가 아직 만들 수 없는 종류**에 한한다.
  그때도 왜 자동 생성이 불가능한지를 기록에 남긴다.
- 화면·문구·배선·안전장치를 손으로 고치는 것은 이 방침과 무관하다.
  **이 방침은 '무엇을 사고파는가'를 정하는 로직에만 적용된다.**

### ⚠️ 정직하게 — 탐색을 넓히는 것은 공짜가 아니다

후보를 늘릴수록 **우연히 좋아 보이는 후보도 늘어난다**. 다행히 이 저장소는
그 대가를 이미 문턱에 반영하고 있다(`confirm_threshold`). 그러므로 탐색을
넓힐 때 지킬 것은 하나다 — **넓힌 만큼 관문이 따라 올라가는지 매번 확인한다.**
그 연결이 끊긴 자동화는 개선 장치가 아니라 과최적화 기계이고, 그건 이 제품의
정체성(선택 편향 없는 공개 실험)을 정면으로 무너뜨린다.

또한 ML이 정확도를 계속 올려 주지도 않는다. 방향 예측의 현실적 상한은
대개 52~55%다. 이 방침의 목적은 **성적을 올린다는 약속이 아니라, 개선을
찾는 일 자체를 사람 손에서 기계로 옮기는 것**이다.

---

## 개발 명령어

```bash
pip install -r requirements.txt   # 또는: make install
pytest -q                          # 전체 테스트 (또는: make test)
python -m quant web --open         # 웹 조종석
python -m quant validate --strategy ma_cross   # 과최적화 검증 3종
```

CI가 Python 3.11/3.12에서 매 push·PR마다 전체 테스트를 실행합니다.
